# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
.installed.cfg
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
env/
ENV/

# uv
uv.lock.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*.swn
.cursor/
.claude/

# Build / dist
build/
dist/
target/

# Logs / runtime
*.log
logs/
*.pid
*.sock

# Test artifacts
tests/.artifacts/

# Local secrets / env
.env
.env.local
.env.*.local

# Postgres data (volume mount)
data/

# Docker volumes
.docker-data/

# Skill pack trust store — priv keys never enter the repo
# `**/.eos/...` matches anywhere in the tree (backend/.eos/, frontend/.eos/,
# or a future top-level .eos/). The inner `**` then matches nested
# trust subdirs (e.g. backend/.eos/skill-trust/*.priv.pem). Public keys
# (*.pub.pem) remain trackable so shared dev trust stores can be committed.
**/.eos/**/*.priv.pem
**/.eos/**/*.priv