[project]
name = "eos-kernel"
version = "0.1.0"
description = "Enterprise Agent OS — kernel: principal, contexts, error hierarchy"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eos_kernel"]

[tool.ruff]
src = ["src"]