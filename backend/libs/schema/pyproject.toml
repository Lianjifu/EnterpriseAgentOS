[project]
name = "eos-schema"
version = "0.1.0"
description = "Cross-module IDs and DTOs shared between libs and modules"
requires-python = ">=3.12"
dependencies = [
    "eos-kernel",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eos_schema"]

[tool.ruff]
src = ["src"]