[project]
name = "eos-vector"
version = "0.1.0"
description = "Vector store abstractions (pgvector + milvus)"
requires-python = ">=3.12"
dependencies = [
    "eos-kernel",
    "eos-schema",
    "eos-persistence",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eos_vector"]

[tool.ruff]
src = ["src"]