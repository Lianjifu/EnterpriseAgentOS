# 02 · 仓库结构

> 完整的目录骨架、模块划分、命名规范、依赖关系。

> **基于 docs/python-refactor/02-仓库结构.md 模板**。
> 与原模板差异：14 个能力模块以 Agent 能力命名；新增 `libs/llm`、`libs/sandbox`、`libs/vector`；横切模块以 `observability_module` 命名避免与 libs 同名冲突。

---

## 2.1 顶层目录

```
enterprise-agent-os/
├── README.md                          # 项目入口
├── pyproject.toml                     # uv workspace 根
├── uv.lock
├── Makefile                           # make run-app / test / db-upgrade 等
├── docker-compose.yml
├── docker-compose.dev.yml
├── .importlinter.ini
├── .github/workflows/                 # CI
│   ├── backend-contract.yml
│   ├── backend-test.yml
│   └── e2e-smoke.yml
├── docs/                              # 文档根
│   └── enterprise-agent-os/           # 本方案文档集
├── libs/                              # 共享内核（10 包）
├── modules/                           # 14 个能力模块
├── composition/                       # 组合根
│   └── eos_app/
├── gateways/                          # 可选网关
│   └── eos_gateway/
├── runtimes/                          # 远程 runtime（独立部署的能力进程）
│   ├── eos_skill_runtime/
│   ├── eos_tool_runtime/
│   └── eos_embedding_runtime/
├── migrations/                        # Alembic
├── tests/                             # 跨模块共享测试
│   ├── contract/
│   ├── e2e/
│   ├── perf/
│   └── shared/
├── deploy/
│   ├── Dockerfile
│   ├── compose.yml
│   └── k8s/                           # 远期
└── scripts/
    ├── dev-stack/
    └── seed/
```

---

## 2.2 顶层职责

| 路径 | 职责 |
|---|---|
| `pyproject.toml` | uv workspace 根；声明 workspace 成员与共享 tool 配置 |
| `Makefile` | 安装 / 运行 / 测试 / 迁移 / 守则等命令入口 |
| `docker-compose.yml` | 本地起 PG / Redis / MinIO + app |
| `.importlinter.ini` | 架构强约束（domain / application 纯洁性、层依赖、模块间依赖矩阵） |
| `docs/` | 方案与运维文档 |
| `libs/` | 跨模块共享的基础设施（10 个包） |
| `modules/` | 14 个能力模块，每个都是 hexagonal 内聚单元 |
| `composition/` | 应用组合根（create_app 工厂 + DI 容器 + lifespan） |
| `gateways/` | 可选 API 网关（路由 / 限流 / 鉴权代理） |
| `runtimes/` | 需要独立部署的能力进程（沙箱 / MCP / embedding） |
| `migrations/` | Alembic 迁移脚本 |
| `tests/` | 跨模块契约测试、e2e、性能基准 |
| `deploy/` | 部署清单与镜像 |

---

## 2.3 `libs/*` 共享内核（10 个包）

| 包 | 路径 | 职责 |
|---|---|---|
| `kernel` | `libs/kernel/src/eos_kernel/` | `Principal` / `TenantContext` / `WorkspaceContext` / `contextvars` |
| `auth` | `libs/auth/src/eos_auth/` | JWT 签发 / 校验 / `APIKeyGuard` / `JWTGuardMiddleware` |
| `observability` | `libs/observability/src/eos_obs/` | structlog / OTel / Prometheus / trace_id |
| `persistence` | `libs/persistence/src/eos_db/` | `Base` / `SessionFactory` / `UnitOfWork` |
| `messaging` | `libs/messaging/src/eos_msg/` | `EventBus` 协议 + `InProcessBus` / `RedisStreamBus` |
| `http` | `libs/http/src/eos_http/` | 中间件 / 错误 envelope / 健康检查 / 限流 |
| `schema` | `libs/schema/src/eos_schema/` | 跨模块共享 Pydantic DTO / ID 类型 |
| `llm` | `libs/llm/src/eos_llm/` | LLM provider 抽象（OpenAI 兼容 + 自定义） |
| `sandbox` | `libs/sandbox/src/eos_sandbox/` | 沙箱执行抽象（local / docker / e2b / firecracker） |
| `vector` | `libs/vector/src/eos_vector/` | 向量存储抽象（pgvector / milvus） |

每个包结构：

```
libs/<name>/src/<import_root>/
├── __init__.py
├── <submodules>.py
└── py.typed           # PEP 561 类型标记
```

`libs` 共同约束（`libs` 不允许 import 任何 `modules`）：

```ini
# .importlinter.ini
[importlinter:contract:no-business-in-libs]
type = forbidden
source_modules = libs.*
forbidden_modules = modules.*
```

---

## 2.4 `modules/*` 14 个能力模块

### 2.4.1 命名与职责

| 模块 | 路径 | 职责 | 主要领域对象 |
|---|---|---|---|
| **identity** | `modules/identity/` | 租户、用户、工作区、角色、API Key | Tenant / User / Workspace / ApiKey |
| **agent_factory** | `modules/agent_factory/` | Agent 定义、版本、模板、发布 | AgentTemplate / AgentVersion / Release |
| **agent_runtime** | `modules/agent_runtime/` | Agent 实例化、运行、会话、上下文 | Session / Turn / Context / Message |
| **orchestration** | `modules/orchestration/` | 多 Agent / 流程 / 任务编排 | Plan / Step / WorkflowRun |
| **skill** | `modules/skill/` | 技能包注册、安装、调用、RunToken | SkillPackage / SkillInstall / SkillInvocation |
| **tool** | `modules/tool/` | 工具注册、协议（MCP / OpenAPI / 自定义） | Tool / ToolCall / ToolResult |
| **knowledge** | `modules/knowledge/` | 知识包、向量化、检索、注入 | KnowledgePackage / KnowledgeAsset / Retrieval |
| **model** | `modules/model/` | 模型注册、凭据、路由、配额 | Model / ModelCredential / RoutingPolicy |
| **memory** | `modules/memory/` | 长期记忆、撤销、归档 | MemoryEntry / MemoryScope |
| **channel** | `modules/channel/` | 渠道适配（飞书 / 钉钉 / 企微 / Web） | Channel / ChannelDelivery |
| **governance** | `modules/governance/` | 策略、审批、配额、审计订阅 | PolicyRule / Approval / Quota |
| **observability_module** | `modules/observability_module/` | trace / metrics / log / cost / quality 业务埋点 | RunRecord / CostRecord / QualityScore |
| **evaluation** | `modules/evaluation/` | 评测数据集、评分、门禁 | EvalDataset / EvalRun / EvalScore |
| **platform** | `modules/platform/` | 平台级设置、计费、扩展 | Plan / Subscription / Extension |

> ⚠️ `observability_module` 命名是为了避免与 `libs/observability` 同名冲突。
> 也可考虑改名为 `observe` 或 `audit_trail`，但为表意清晰首选 `_module` 后缀。

### 2.4.2 模块标准布局

每个模块统一为 hexagonal 三层：

```
modules/<m>/
├── pyproject.toml
├── src/deos/modules/<m>/
│   ├── __init__.py
│   ├── domain/
│   │   ├── entities.py            # 聚合根 / 实体
│   │   ├── value_objects.py       # 值对象
│   │   ├── events.py              # 领域事件
│   │   └── errors.py              # DomainError 体系
│   ├── application/
│   │   ├── services.py            # 应用服务
│   │   ├── use_cases/             # 单一用例
│   │   └── ports/                 # 端口（Protocol）
│   └── adapter/
│       ├── http/                  # FastAPI 路由
│       │   ├── routers.py
│       │   └── dto.py
│       ├── persistence/           # SQLAlchemy 实现
│       │   ├── models.py
│       │   └── repos.py
│       ├── events/                # 事件订阅 / 发布实现
│       │   ├── publishers.py
│       │   └── subscribers.py
│       └── external/              # 外部系统适配
│           └── <provider>.py
└── tests/
    ├── unit/
    │   ├── domain/
    │   └── application/
    └── integration/
        ├── http/
        └── persistence/
```

### 2.4.3 起步 vs 扩展（核心闭环 4 模块 + 治理 / 观测横切）

| 阶段 | 模块 | 状态 |
|---|---|---|
| **P0** | libs/kernel / libs/auth / libs/observability / libs/persistence / libs/messaging / libs/http / libs/schema | 基础设施 |
| | modules/identity | 起步必备（鉴权） |
| **P1–P4**（起步闭环） | modules/agent_runtime / modules/skill / modules/tool / modules/memory | **核心** |
| **P5** | modules/governance | 治理横切 |
| **P6** | modules/model + modules/channel | 扩展 |
| **P7** | modules/orchestration + modules/knowledge | 扩展 |
| **P8** | modules/agent_factory + modules/evaluation | 扩展 |
| **P9** | modules/observability_module + modules/platform | 平台化 |

---

## 2.5 `composition/eos_app/` 组合根

```
composition/eos_app/src/deos/composition/
├── __init__.py
├── main.py                # create_app() 工厂
├── lifespan.py            # 启动 / 关闭钩子
├── container.py           # DI 容器装配
├── settings.py            # 强类型 Settings
├── ensure.py              # 启动 ensure（建通用 Agent / 通用 Skill）
└── middleware.py          # 跨切中间件聚合
```

`create_app()` 工厂：

```python
# composition/eos_app/src/deos/composition/main.py
from fastapi import FastAPI
from libs.http import install_default_middleware
from .container import build_container
from .lifespan import lifespan


def create_app() -> FastAPI:
    container = build_container()
    app = FastAPI(
        title="Enterprise-Agent-OS",
        version="0.1.0",
        lifespan=lifespan(container),
        docs_url="/docs",
    )
    install_default_middleware(app, container)
    mount_module_routers(app, container)
    mount_health_routers(app, container)
    return app
```

---

## 2.6 `gateways/eos_gateway/` 可选网关

```
gateways/eos_gateway/src/deos/gateway/
├── main.py                # create_gateway()
├── routing.py             # 路径前缀 → 上游映射
├── ratelimit.py           # 全局限流
├── auth.py                # 网关级鉴权（可选）
└── proxy.py               # httpx 反向代理
```

> 网关非必需；单体直接对外暴露即可。生产环境（多服务 / 多租户 / 跨域）才需要。

---

## 2.7 `runtimes/*` 远程 Runtime

需要独立部署的能力进程（沙箱隔离、可独立扩缩容）：

| Runtime | 端口 | 职责 |
|---|---|---|
| `eos_skill_runtime` | 8093 | 沙箱内执行 Skill；返回产物 |
| `eos_tool_runtime` | 8094 | MCP / OpenAPI 工具适配 |
| `eos_embedding_runtime` | 8095 | 文本向量化（统一管理 API Key） |

每个 runtime 与主 app 通过 HTTP/RPC 通信；由 `composition/eos_app/container.py` 选择注入：
- `local`：app 进程内执行（开发）
- `remote`：调用 runtime（生产）

---

## 2.8 命名规范

| 类别 | 规范 | 示例 |
|---|---|---|
| **包名** | snake_case | `agent_runtime` |
| **模块名** | snake_case | `session_repo` |
| **类名** | PascalCase | `SessionService` |
| **函数 / 方法** | snake_case | `create_session` |
| **常量** | UPPER_SNAKE | `MAX_TOKENS` |
| **事件** | 过去时 PascalCase | `SessionStarted` `ToolInvoked` |
| **错误** | PascalCase + Error 后缀 | `ActionDeniedError` |
| **端口** | PascalCase + 行为名词 | `LLMPort` `ToolPort` |
| **环境变量** | `EOS_` 前缀 + UPPER_SNAKE | `EOS_TENANT_ID` |
| **HTTP 路径** | `/v1/<resource>` | `/v1/sessions/{sid}/turn` |
| **ID 类型** | PascalCase + Id 后缀 | `SessionId` `TurnId` |
| **ORM 模型** | PascalCase + Orm 后缀（私有） | `SessionOrm` |

---

## 2.9 Go → Python 概念映射（用于参考既有 Go 实现）

| Go 概念 | Python 等价 |
|---|---|
| `internal/service` | `modules/<m>/application/services.py` |
| `internal/repo` | `modules/<m>/adapter/persistence/repos.py` |
| `internal/handler` | `modules/<m>/adapter/http/routers.py` |
| `internal/types` | `modules/<m>/domain/entities.py` |
| `pkg/eventbus` | `libs/messaging` |
| `pkg/middleware` | `libs/http` |
| `pkg/auth` | `libs/auth` |
| `pkg/observability` | `libs/observability` |
| `proto/...` | `libs/schema`（Pydantic） |
| `cmd/.../main.go` | `composition/eos_app/main.py` |
| `migrations/*.sql` | `migrations/versions/*.py`（Alembic） |

---

## 2.10 依赖矩阵

`import-linter` 配置关键约束（完整版见 [10-测试与CI.md](10-测试与CI.md)）：

```ini
# 起步四模块可互相依赖（端口），其他模块严格隔离
[importlinter:contract:layered-modules]
type = layers
layers =
    modules.identity
    modules.governance | modules.observability_module
    modules.model
    modules.agent_runtime
    modules.skill | modules.tool | modules.memory
    modules.knowledge | modules.orchestration | modules.channel
    modules.agent_factory | modules.evaluation
    modules.platform
```

每个模块可向下依赖（端口），不可向上依赖。

---

## 2.11 与原模板的差异（速查）

| 项 | 原模板 | 本项目 |
|---|---|---|
| 模块数 | 8 | 14 |
| 起步范围 | 全模块渐进 | 4 模块闭环 + 治理 / 观测 |
| 多租户 | 仅 Workspace | Tenant + Workspace |
| Agent 抽象 | 隐式 | 独立 `agent_runtime` + `agent_factory` |
| 模型 | 隐藏在 cap | 独立 `model` 模块 |
| 知识 / 渠道 | 隐藏在 cap | 独立 `knowledge` / `channel` |
| 编排 | workflow | `orchestration`（可调其他 Agent） |
| 治理 | policy / audit 分离 | 合并 `governance`（审计是订阅者） |
| libs | 9 包 | 10 包（新增 `llm` `sandbox` `vector`） |
| Runtime | skill / agent / rag | skill / tool / embedding（tool 独立） |
