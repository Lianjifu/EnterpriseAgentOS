# 03 · 共享内核

> 10 个共享内核包的详细设计：职责、关键接口、可复用构件。

> **基于 docs/python-refactor/03-共享内核.md 模板**。
> 与原模板差异：新增 `llm` / `sandbox` / `vector` 三个 Agent 平台特有包；`auth` 扩展 API Key；`kernel` 引入 TenantContext。

---

## 3.1 总览

| 包 | 关键导出 | 使用者 |
|---|---|---|
| `kernel` | `Principal` / `TenantContext` / `WorkspaceContext` / `trace_id_var` | 所有模块 |
| `auth` | `JWTGuardMiddleware` / `APIKeyGuard` / `sign_token` / `verify_token` | API 入口 |
| `observability` | `log` / `setup_logging` / `setup_tracing` / `MetricsMiddleware` / `/metrics` | 入口 + 业务埋点 |
| `persistence` | `Base` / `SessionFactory` / `UnitOfWork` | adapter/persistence |
| `messaging` | `EventBus` (Protocol) / `InProcessBus` / `RedisStreamBus` / `DomainEvent` | 业务模块 + composition |
| `http` | `install_default_middleware` / `error_envelope` / `RateLimitMiddleware` / `healthz_router` | composition |
| `schema` | `SessionId` / `TurnId` / `TenantId` / 跨模块 DTO | 业务模块互调 |
| `llm` | `LLMClient` (Protocol) / `OpenAICompatibleClient` / `ModelSpec` | agent_runtime |
| `sandbox` | `Sandbox` (Protocol) / `LocalSandbox` / `DockerSandbox` / `E2BSandbox` | skill |
| `vector` | `VectorStore` (Protocol) / `PgVectorStore` / `MilvusStore` | memory / knowledge |

---

## 3.2 `libs/kernel` — 上下文与身份

### 3.2.1 `Principal`

```python
# libs/kernel/src/eos_kernel/principal.py
from dataclasses import dataclass
from typing import Iterable


@dataclass(frozen=True)
class Principal:
    user_id: str
    tenant_id: str
    workspace_ids: tuple[str, ...]
    roles: tuple[str, ...]  # admin / user / auditor
    api_key_id: str | None = None  # M2M 调用时填
    trace_id: str = "-"

    def has_role(self, role: str) -> bool:
        return role in self.roles

    def in_workspace(self, workspace_id: str) -> bool:
        return workspace_id in self.workspace_ids

    def has_any_role(self, roles: Iterable[str]) -> bool:
        return any(r in self.roles for r in roles)
```

### 3.2.2 `TenantContext` / `WorkspaceContext`

```python
# libs/kernel/src/eos_kernel/context.py
from dataclasses import dataclass


@dataclass(frozen=True)
class TenantContext:
    tenant_id: str
    plan: str = "free"  # free / pro / enterprise


@dataclass(frozen=True)
class WorkspaceContext:
    workspace_id: str
    tenant_id: str
    name: str
```

> 多租户硬隔离：所有持久化 / 缓存 / 事件查询都必须携带 `tenant_id`。
> `WorkspaceGuard` 在请求入口校验 `X-Workspace-Id` ∈ `Principal.workspace_ids`。

### 3.2.3 contextvars

```python
# libs/kernel/src/eos_kernel/contextvars.py
import contextvars

principal_var: contextvars.ContextVar[Principal] = contextvars.ContextVar("principal", default=None)
tenant_var: contextvars.ContextVar[TenantContext] = contextvars.ContextVar("tenant", default=None)
workspace_var: contextvars.ContextVar[WorkspaceContext] = contextvars.ContextVar(
    "workspace", default=None
)
trace_id_var: contextvars.ContextVar[str] = contextvars.ContextVar("trace_id", default="-")


def current_principal() -> Principal: ...
def current_tenant() -> TenantContext: ...
def current_workspace() -> WorkspaceContext: ...
```

---

## 3.3 `libs/auth` — JWT + API Key

### 3.3.1 双轨鉴权

```python
# libs/auth/src/eos_auth/jwt_guard.py
from starlette.middleware.base import BaseHTTPMiddleware
from .api_key import verify_api_key
from .jwt import decode_token


class AuthMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        # 优先 API Key（M2M）
        api_key = request.headers.get("x-api-key")
        if api_key:
            principal = verify_api_key(api_key)
            principal_var.set(principal)
            return await call_next(request)

        # 其次 JWT
        auth = request.headers.get("authorization")
        if auth and auth.startswith("Bearer "):
            token = auth[7:]
            principal = decode_token(token)
            if settings.ban_mock_token and token.startswith("mock-"):
                raise IdentityMockForbidden()
            principal_var.set(principal)
            return await call_next(request)

        raise Unauthorized()
```

### 3.3.2 API Key 仓储

```python
# libs/auth/src/eos_auth/api_key.py
from typing import Protocol


class APIKeyRepo(Protocol):
    async def get(self, key_hash: str) -> APIKeyRecord | None: ...
    async def touch_last_used(self, key_id: str) -> None: ...
```

API Key 设计：
- 形式：`eos_pk_<tenant>_<random>`；存哈希
- 绑定 tenant；不可跨租户使用
- 限速：每分钟 1000 次（默认）

### 3.3.3 角色装饰器

```python
# libs/auth/src/eos_auth/rbac.py
from functools import wraps


def require_role(*roles: str):
    def deco(fn):
        @wraps(fn)
        async def wrapper(*args, **kwargs):
            p = current_principal()
            if not p or not p.has_any_role(roles):
                raise Forbidden()
            return await fn(*args, **kwargs)

        return wrapper

    return deco


def require_workspace():
    """校验 X-Workspace-Id ∈ Principal.workspace_ids"""
    ...
```

---

## 3.4 `libs/observability` — 日志 / 追踪 / 指标

### 3.4.1 结构化日志

```python
# libs/observability/src/eos_obs/logging.py
import structlog

log = structlog.get_logger()


def setup_logging(env: str, level: str = "INFO") -> None:
    # 见 python-refactor 09.2.1；dev 彩色、生产 JSON
    ...
```

### 3.4.2 TraceId 中间件

```python
# libs/observability/src/eos_obs/middleware.py
class TraceIdMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        trace_id = request.headers.get("x-trace-id") or uuid4().hex[:16]
        trace_id_var.set(trace_id)
        response = await call_next(request)
        response.headers["X-Trace-Id"] = trace_id
        return response
```

### 3.4.3 Prometheus 指标（业务专属）

```python
# libs/observability/src/eos_obs/metrics.py
from prometheus_client import Counter, Histogram, Gauge

LLM_TOKENS_TOTAL = Counter(
    "eos_llm_tokens_total",
    "LLM tokens consumed",
    ["model", "direction", "tenant_id"],
)

AGENT_TURNS_TOTAL = Counter(
    "eos_agent_turns_total",
    "Agent turns",
    ["agent_id", "status", "tenant_id"],
)

TOOL_CALLS_TOTAL = Counter(
    "eos_tool_calls_total",
    "Tool calls",
    ["tool_name", "status", "tenant_id"],
)

SKILL_INVOCATIONS_TOTAL = Counter(
    "eos_skill_invocations_total",
    "Skill invocations",
    ["skill_id", "status", "tenant_id"],
)

MEMORY_OPERATIONS_TOTAL = Counter(
    "eos_memory_operations_total",
    "Memory operations",
    ["operation", "tenant_id"],  # write / search / revoke
)

ACTIVE_SESSIONS = Gauge(
    "eos_active_sessions",
    "Active sessions",
    ["tenant_id"],
)

AGENT_TURN_DURATION = Histogram(
    "eos_agent_turn_duration_seconds",
    "Agent turn duration",
    ["agent_id", "status"],
    buckets=(0.1, 0.5, 1, 2.5, 5, 10, 30, 60, 120, 300),
)
```

> 所有指标都带 `tenant_id` label，便于按租户拆分成本与限流。

---

## 3.5 `libs/persistence` — SQLAlchemy 封装

### 3.5.1 Base / SessionFactory / UnitOfWork

```python
# libs/persistence/src/eos_db/base.py
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass


# libs/persistence/src/eos_db/session_factory.py
class SessionFactory:
    def __init__(self, url: str, *, pool_size=20, max_overflow=10, echo=False):
        self.engine = create_async_engine(
            url, pool_size=pool_size, max_overflow=max_overflow, echo=echo
        )
        self.sessionmaker = async_sessionmaker(self.engine, expire_on_commit=False)

    def session(self):
        return self.sessionmaker()

    async def close(self):
        await self.engine.dispose()


# libs/persistence/src/eos_db/unit_of_work.py
class UnitOfWork:
    def __init__(self, factory: SessionFactory):
        self._factory = factory

    async def __aenter__(self):
        self.session = self._factory.session()
        return self

    async def __aexit__(self, exc_type, exc, tb):
        try:
            if exc_type is None:
                await self.session.commit()
            else:
                await self.session.rollback()
        finally:
            await self.session.close()
```

### 3.5.2 多租户查询守卫

```python
# libs/persistence/src/eos_db/tenant_query.py
from sqlalchemy import event
from .context import current_tenant


@event.listens_for(Session, "do_orm_execute")
def _filter_by_tenant(state):
    if not state.is_select:
        return
    tenant = current_tenant()
    if tenant is None:
        return
    # 自动注入 tenant_id 过滤（约定 ORM 模型有 tenant_id 列）
    for desc in state.statement.column_descriptions:
        entity = desc.get("entity")
        if entity and hasattr(entity, "tenant_id"):
            state.statement = state.statement.where(entity.tenant_id == tenant.tenant_id)
```

> 这是"约定优于配置"：ORM 模型必须有 `tenant_id` 列。
> 不满足约定的查询会被 lint 规则警告。

---

## 3.6 `libs/messaging` — 事件总线

### 3.6.1 协议

```python
# libs/messaging/src/eos_msg/bus.py
from typing import Protocol, Type, Callable
from .event import DomainEvent


class EventBus(Protocol):
    async def publish(self, event: DomainEvent) -> None: ...
    def subscribe(self, event_type: Type[DomainEvent], handler: Callable) -> None: ...
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
```

### 3.6.2 进程内实现

```python
# libs/messaging/src/eos_msg/in_process.py
import asyncio


class InProcessBus:
    def __init__(self, queue_size: int = 1000):
        self._queue: asyncio.Queue = asyncio.Queue(maxsize=queue_size)
        self._subs: dict[Type, list[Callable]] = {}
        self._tasks: list[asyncio.Task] = []

    async def start(self):
        self._tasks.append(asyncio.create_task(self._dispatch_loop()))

    async def stop(self):
        for t in self._tasks:
            t.cancel()
        await asyncio.gather(*self._tasks, return_exceptions=True)

    async def publish(self, event):
        await self._queue.put(event)

    def subscribe(self, event_type, handler):
        self._subs.setdefault(event_type, []).append(handler)

    async def _dispatch_loop(self):
        while True:
            event = await self._queue.get()
            for handler in self._subs.get(type(event), []):
                try:
                    await handler(event)
                except Exception:
                    log.exception("event.handler.failed", event=type(event).__name__)
            self._queue.task_done()
```

### 3.6.3 Redis Stream 实现（生产）

```python
# libs/messaging/src/eos_msg/redis_stream.py
class RedisStreamBus:
    def __init__(
        self, redis, stream_key="eos:events", consumer_group="eos-app", consumer_name="local-1"
    ):
        self._redis = redis
        self._stream_key = stream_key
        self._group = consumer_group
        self._name = consumer_name

    async def publish(self, event):
        await self._redis.xadd(self._stream_key, {"data": event.model_dump_json()})

    def subscribe(self, event_type, handler):
        # 由独立 worker 进程消费
        ...
```

> 单体部署用 `InProcessBus`；多副本切 `RedisStreamBus`。

---

## 3.7 `libs/http` — 中间件 / 错误 / 健康检查

### 3.7.1 默认中间件链

```python
# libs/http/src/eos_http/middleware.py
from starlette.middleware.base import BaseHTTPMiddleware


class ErrorEnvelopeMiddleware(BaseHTTPMiddleware):
    """统一错误响应：{'error': {'code', 'message', 'details'}}"""


class RateLimitMiddleware(BaseHTTPMiddleware):
    """基于 Redis token bucket，按 tenant 限流"""


class RequestLogMiddleware(BaseHTTPMiddleware):
    """访问日志 + trace_id 绑定"""


class CORSMiddleware(BaseHTTPMiddleware):
    """跨域头"""


def install_default_middleware(app, container):
    app.add_middleware(ErrorEnvelopeMiddleware)
    app.add_middleware(RequestLogMiddleware)
    app.add_middleware(TraceIdMiddleware)
    app.add_middleware(MetricsMiddleware)
    app.add_middleware(RateLimitMiddleware, container=container)
    app.add_middleware(CORSMiddleware)
```

### 3.7.2 错误码 → HTTP 状态映射

```python
# libs/http/src/eos_http/errors.py
ERROR_MAP = {
    BadRequestError: (400, "BAD_REQUEST"),
    UnauthorizedError: (401, "UNAUTHORIZED"),
    ForbiddenError: (403, "FORBIDDEN"),
    NotFoundError: (404, "NOT_FOUND"),
    ConflictError: (409, "CONFLICT"),
    GoneError: (410, "GONE"),
    PreconditionFailedError: (412, "PRECONDITION_FAILED"),
    UnprocessableError: (422, "UNPROCESSABLE"),
    RateLimitedError: (429, "RATE_LIMITED"),
    ActionDeniedError: (403, "ACTION_DENIED"),
    WorkspaceDeniedError: (403, "WORKSPACE_DENIED"),
    TenantDeniedError: (403, "TENANT_DENIED"),
    QuotaExceededError: (429, "QUOTA_EXCEEDED"),
    EvalGateFailedError: (422, "EVAL_GATE_FAILED"),
}
```

详见 [06-接口规范.md](06-接口规范.md)。

### 3.7.3 健康检查

```python
# libs/http/src/eos_http/healthz_router.py
@router.get("/livez", include_in_schema=False)
async def livez():
    return {"status": "live"}


@router.get("/healthz", include_in_schema=False)
async def healthz(container):
    # PG / Redis / Vector Store / LLM 可达
    ...


@router.get("/readyz", include_in_schema=False)
async def readyz(container):
    # ensure 完成 + 可服务
    ...
```

---

## 3.8 `libs/schema` — 跨模块 DTO

```python
# libs/schema/src/eos_schema/ids.py
from typing import NewType
from pydantic import BaseModel

TenantId = NewType("TenantId", str)
WorkspaceId = NewType("WorkspaceId", str)
UserId = NewType("UserId", str)
AgentId = NewType("AgentId", str)
AgentVersionId = NewType("AgentVersionId", str)
SessionId = NewType("SessionId", str)
TurnId = NewType("TurnId", str)
ToolCallId = NewType("ToolCallId", str)
SkillInvocationId = NewType("SkillInvocationId", str)
MemoryEntryId = NewType("MemoryEntryId", str)


# libs/schema/src/eos_schema/dto.py
class ToolCall(BaseModel):
    id: ToolCallId
    tool_name: str
    arguments: dict
    result: dict | None = None
    error: str | None = None
    started_at: datetime
    finished_at: datetime | None = None


class TurnChunk(BaseModel):
    """SSE 单个 chunk；用于跨模块流式传递"""

    event: str  # start / chunk / tool_call / tool_result / done / error
    turn_id: TurnId
    content: str | None = None
    tool_call: ToolCall | None = None
    error: str | None = None
```

---

## 3.9 `libs/llm` — LLM Provider 抽象

### 3.9.1 协议

```python
# libs/llm/src/eos_llm/client.py
from typing import Protocol, AsyncIterator
from .types import LLMRequest, LLMResponse, LLMChunk


class LLMClient(Protocol):
    async def chat(self, req: LLMRequest) -> LLMResponse: ...
    def stream(self, req: LLMRequest) -> AsyncIterator[LLMChunk]: ...
    async def embed(self, texts: list[str]) -> list[list[float]]: ...  # 远期可下沉到 vector
```

### 3.9.2 OpenAI 兼容实现

```python
# libs/llm/src/eos_llm/openai_compatible.py
class OpenAICompatibleClient:
    def __init__(self, base_url: str, api_key: str, timeout: int = 60):
        self._client = httpx.AsyncClient(
            base_url=base_url, headers={"Authorization": f"Bearer {api_key}"}, timeout=timeout
        )

    async def chat(self, req):
        r = await self._client.post("/chat/completions", json=req.to_openai_dict())
        return LLMResponse.from_openai(r.json())

    async def stream(self, req):
        async with self._client.stream(
            "POST", "/chat/completions", json={**req.to_openai_dict(), "stream": True}
        ) as r:
            async for line in r.aiter_lines():
                if line.startswith("data: "):
                    yield LLMChunk.from_openai_sse(line[6:])
```

### 3.9.3 模型路由

```python
# libs/llm/src/eos_llm/router.py
class LLMRouter:
    """按 agent 配置选择 provider；支持 failover"""

    def __init__(self, registry: ModelRegistry, default: str):
        self._registry = registry
        self._default = default

    async def chat(self, req: LLMRequest, *, preferred: str | None = None) -> LLMResponse:
        model_id = preferred or self._default
        client = self._registry.get(model_id)
        try:
            return await client.chat(req)
        except LLMRateLimited:
            backup = self._registry.get(model_id, role="backup")
            if backup:
                return await backup.chat(req)
            raise
```

---

## 3.10 `libs/sandbox` — 沙箱抽象

### 3.10.1 协议

```python
# libs/sandbox/src/eos_sandbox/runner.py
from typing import Protocol, AsyncIterator
from .types import SandboxSpec, RunResult, RunChunk


class Sandbox(Protocol):
    async def run(self, spec: SandboxSpec, *, timeout: int) -> RunResult: ...
    def stream(self, spec: SandboxSpec, *, timeout: int) -> AsyncIterator[RunChunk]: ...
    async def cancel(self, run_id: str) -> None: ...
```

### 3.10.2 实现

```python
# libs/sandbox/src/eos_sandbox/local.py
class LocalSandbox:
    """进程内 subprocess 执行（仅 dev）"""


# libs/sandbox/src/eos_sandbox/docker.py
class DockerSandbox:
    """docker run 执行（生产起步）"""


# libs/sandbox/src/eos_sandbox/e2b.py
class E2BSandbox:
    """E2B 托管（远期）"""
```

`SandboxSpec`：

```python
@dataclass
class SandboxSpec:
    image: str  # python:3.12-slim / eos-skill-runtime:v1
    command: list[str]
    env: dict[str, str]
    files: dict[str, bytes]  # 上传到沙箱
    run_token: str  # HMAC 签名
    network_policy: str = "default"  # default / none / unrestricted
```

---

## 3.11 `libs/vector` — 向量存储抽象

### 3.11.1 协议

```python
# libs/vector/src/eos_vector/store.py
from typing import Protocol
from .types import VectorItem, SearchQuery, SearchResult


class VectorStore(Protocol):
    async def upsert(self, items: list[VectorItem]) -> None: ...
    async def search(self, query: SearchQuery) -> list[SearchResult]: ...
    async def delete(self, ids: list[str]) -> None: ...
```

### 3.11.2 实现

```python
# libs/vector/src/eos_vector/pgvector.py
class PgVectorStore:
    """pgvector 扩展（默认；与主库同库）"""


# libs/vector/src/eos_vector/milvus.py
class MilvusStore:
    """Milvus 独立集群（远期大规模）"""
```

`VectorItem`：

```python
@dataclass
class VectorItem:
    id: str
    embedding: list[float]
    metadata: dict
    tenant_id: str
    workspace_id: str
```

> 所有向量条目强制带 `tenant_id` + `workspace_id`，多租户隔离。

---

## 3.12 与原模板差异速查

| 包 | 原模板 | 本项目 |
|---|---|---|
| kernel | Principal 单层 | + TenantContext + 多级 contextvars |
| auth | JWT only | + API Key + 双轨 `AuthMiddleware` |
| observability | trace / metrics / log | + 业务专属 counter（按 tenant_id 维度） |
| persistence | Base / SessionFactory / UoW | + 多租户查询守卫 |
| messaging | InProcess + RedisStream | 同 |
| http | 中间件 + 错误 | + tenant 维度的 RateLimitMiddleware |
| schema | 基础 ID | + `TurnChunk` 等 Agent 平台专属 DTO |
| **llm** | （无） | **新增**：provider 抽象 + router |
| **sandbox** | （无） | **新增**：local / docker / e2b 抽象 |
| **vector** | （无） | **新增**：pgvector / milvus 抽象 |
