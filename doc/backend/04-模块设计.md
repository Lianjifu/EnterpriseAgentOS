# 04 · 模块设计

> 14 个能力模块的详细设计：依赖矩阵、hexagonal 布局、领域对象、端口、事件、关键路由与状态码。

> **基于 docs/python-refactor/04-模块设计.md 模板**。
> 与原模板差异：14 个模块（起步 4 + 治理 + 横切）；Agent 平台视角重新组织；明确起步阶段（P0-P4）的模块范围。

---

## 4.1 模块全景

### 4.1.1 模块清单

| 模块 | 阶段 | 类别 | 核心职责 |
|---|---|---|---|
| `identity` | P0 | 横切 | 租户、用户、工作区、角色、API Key |
| `agent_factory` | P8 | 业务 | Agent 模板、版本、发布 |
| **`agent_runtime`** | **P1** | **核心** | **Session / Turn / Context / SSE 流式** |
| `orchestration` | P7 | 业务 | 多 Agent 编排、流程 |
| **`skill`** | **P3** | **核心** | **技能包、沙箱、RunToken** |
| **`tool`** | **P2** | **核心** | **工具注册、MCP/OpenAPI 协议、调用** |
| `knowledge` | P7 | 业务 | 知识包、检索、注入 |
| `model` | P6 | 业务 | 模型注册、路由、配额 |
| **`memory`** | **P4** | **核心** | **长期记忆、撤销** |
| `channel` | P6 | 业务 | 飞书 / 钉钉 / 企微 / Web 渠道 |
| `governance` | P5 | 横切 | 策略、审批、配额、审计 |
| `observability_module` | P9 | 横切 | 运行记录、成本、质量埋点 |
| `evaluation` | P8 | 业务 | 评测数据集、评分、门禁 |
| `platform` | P9 | 平台 | 平台设置、计费、扩展 |

> **加粗** = 起步四模块，构成 P0-P4 完整闭环。

### 4.1.2 依赖矩阵

|  | id | af | ar | or | sk | tl | kn | md | mm | ch | gv | ob | ev | pl |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **identity (id)** | — | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| **agent_factory (af)** | | — | ✓ | ✓ | | | | ✓ | | | ✓ | | ✓ | |
| **agent_runtime (ar)** | | | — | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| **orchestration (or)** | | | ✓ | — | | ✓ | ✓ | ✓ | ✓ | | ✓ | ✓ | | |
| **skill (sk)** | | | | | — | | | | | | ✓ | ✓ | | |
| **tool (tl)** | | | | | | — | | | | | ✓ | ✓ | | |
| **knowledge (kn)** | | | | | | | — | | ✓ | | ✓ | ✓ | | |
| **model (md)** | | | | | | | | — | | | ✓ | ✓ | | |
| **memory (mm)** | | | | | | | ✓ | | — | | ✓ | ✓ | ✓ | |
| **channel (ch)** | | | ✓ | | | | | ✓ | | — | ✓ | ✓ | | |
| **governance (gv)** | | | | | | | | | | | — | ✓ | | |
| **observability_module (ob)** | | | | | | | | | | | ✓ | — | | |
| **evaluation (ev)** | | | ✓ | | | | ✓ | ✓ | ✓ | | ✓ | ✓ | — | |
| **platform (pl)** | | | | | | | | | | | ✓ | ✓ | | — |

✓ = 该模块可调用（通过端口）该模块的能力。
依赖只允许**向下**或**横切**（identity / governance / observability_module）。

---

## 4.2 横切模块

### 4.2.1 `identity` 身份与多租户

**职责**：租户、工作区、用户、角色、API Key 的 CRUD + 校验。

**领域对象**：

```python
# modules/identity/src/deos/modules/identity/domain/entities.py
@dataclass
class Tenant:
    id: TenantId
    name: str
    plan: str  # free / pro / enterprise
    created_at: datetime


@dataclass
class Workspace:
    id: WorkspaceId
    tenant_id: TenantId
    name: str


@dataclass
class User:
    id: UserId
    tenant_id: TenantId
    email: str
    workspace_ids: list[WorkspaceId]
    roles: list[str]
    api_keys: list[APIKey] = field(default_factory=list)
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/identity/tenants` | POST | 201 |
| `/v1/identity/workspaces` | POST | 201 |
| `/v1/identity/users` | POST / GET | 201 / 200 |
| `/v1/identity/users/{uid}/api-keys` | POST | 201 |
| `/v1/identity/login` | POST | 200 (返回 JWT) |

**事件**：`TenantCreated` `WorkspaceCreated` `APIKeyIssued` `APIKeyRevoked`。

### 4.2.2 `governance` 治理（策略 / 审批 / 配额 / 审计）

**职责**：在所有 agent / tool / skill / memory 调用前评估 policy；处理 approval；quota 计数；订阅审计事件。

**领域对象**：

```python
@dataclass
class PolicyRule:
    id: PolicyId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    resource: str  # agent / tool / skill / memory
    action: str  # invoke / write / read
    effect: str  # allow / deny / approval
    conditions: dict  # e.g. {"max_amount": 10000}
    priority: int


@dataclass
class Approval:
    id: ApprovalId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    requester_id: UserId
    action: str  # action being approved
    context: dict  # 携带被审核操作的上下文
    status: str  # pending / approved / denied / expired
    expires_at: datetime
    approver_id: UserId | None = None
```

**关键端口**：

```python
class PolicyPort(Protocol):
    async def evaluate(self, principal: Principal, action: Action) -> Decision: ...


class QuotaPort(Protocol):
    async def check(self, principal: Principal, metric: str, value: float) -> None: ...
    async def consume(self, principal: Principal, metric: str, value: float) -> None: ...
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/governance/policies` | POST / GET / PATCH / DELETE | 201 / 200 / 200 / 204 |
| `/v1/governance/approvals` | POST | 201 |
| `/v1/governance/approvals/{id}` | GET | 200 / 404 |
| `/v1/governance/approvals/{id}/approve` | POST | 200 / 409（已处理） |
| `/v1/governance/approvals/{id}/deny` | POST | 200 / 409 |

**事件订阅**：`SessionStarted` `ToolInvoked` `SkillInvoked` `MemoryWritten` `QuotaExceeded` → 全部落审计表 + 触发 quota 计数。

### 4.2.3 `observability_module` 运行观测

**职责**：业务级运行记录（RunRecord）、成本计量（CostRecord）、质量评分（QualityScore）。

**领域对象**：

```python
@dataclass
class RunRecord:
    id: RunId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    trace_id: str
    session_id: SessionId | None
    agent_id: AgentId
    started_at: datetime
    finished_at: datetime | None
    status: str
    input_tokens: int
    output_tokens: int
    cost_usd: float
    metadata: dict


@dataclass
class CostRecord:
    id: CostId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    metric: str  # llm_input / llm_output / tool_call / skill_invoke
    quantity: float
    cost_usd: float
    occurred_at: datetime
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/observability/runs` | GET | 200 |
| `/v1/observability/runs/{rid}` | GET | 200 / 404 |
| `/v1/observability/costs` | GET | 200 |
| `/v1/observability/quality` | GET | 200 |

---

## 4.3 核心闭环四模块（起步重点）

### 4.3.1 `agent_runtime` Agent 运行时 ⭐

**职责**：Agent 实例化、Session 管理、Turn 执行、上下文累积、SSE 流式输出。**平台核心**。

**领域对象**：

```python
# modules/agent_runtime/src/deos/modules/agent_runtime/domain/entities.py
@dataclass
class Session:
    id: SessionId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    owner_id: UserId
    agent_id: AgentId
    agent_version: str
    status: str  # open / closed
    created_at: datetime
    closed_at: datetime | None
    metadata: dict


@dataclass
class Turn:
    id: TurnId
    session_id: SessionId
    user_input: str
    status: str  # running / succeeded / failed / denied
    started_at: datetime
    finished_at: datetime | None
    tool_calls: list[ToolCall]
    skill_invocations: list[SkillInvocation]
    memory_writes: list[MemoryEntryId]
    final_response: str | None


@dataclass
class Context:
    """Agent 上下文累积；含 system prompt + 历史 + 工具结果"""

    session_id: SessionId
    system_prompt: str
    messages: list[Message]  # user / assistant / tool
    tool_results: dict[str, ToolResult]
    memory_refs: list[MemoryEntryId]
```

**关键端口**：

```python
# modules/agent_runtime/src/deos/modules/agent_runtime/application/ports.py
class LLMPort(Protocol):
    async def stream(self, ctx: Context) -> AsyncIterator[LLMChunk]: ...


class ToolPort(Protocol):
    async def invoke(self, call: ToolCall) -> ToolResult: ...


class SkillPort(Protocol):
    async def invoke(self, invocation: SkillInvocation) -> SkillResult: ...


class MemoryPort(Protocol):
    async def search(self, query: str, *, top_k: int) -> list[MemoryEntry]: ...
    async def write(self, entry: MemoryEntry) -> None: ...


class KnowledgePort(Protocol):
    async def retrieve(self, query: str, *, top_k: int) -> list[KnowledgeChunk]: ...
```

**关键路由**：

| 路由 | 方法 | 状态码 | 备注 |
|---|---|---|---|
| `/v1/agents/{aid}/sessions` | POST | 201 + Location | 创建会话 |
| `/v1/sessions/{sid}` | GET | 200 / 404 | 会话详情 |
| `/v1/sessions/{sid}/turn` | POST | 202 | 异步 turn（带 SSE） |
| `/v1/sessions/{sid}/turn/stream` | POST | 200 + SSE | 流式 turn |
| `/v1/sessions/{sid}/turn/{tid}` | GET | 200 / 404 | 单 turn 详情 |
| `/v1/sessions/{sid}` | DELETE | 204 | 硬删会话 |
| `/v1/sessions` | GET | 200 | 列会话 |

**事件**：
- 发布：`SessionStarted` `TurnStarted` `TurnCompleted` `TurnFailed` `SessionClosed` `ToolCallRequested` `ToolCallCompleted`
- 订阅：`MemoryWritten`（用于上下文召回）

### 4.3.2 `tool` 工具注册与调用 ⭐

**职责**：工具定义、版本、协议适配（MCP / OpenAPI / 自定义）、调用与计量。

**领域对象**：

```python
@dataclass
class Tool:
    id: ToolId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    name: str  # 全局唯一
    version: str
    protocol: str  # mcp / openapi / custom
    spec: dict  # OpenAPI doc / MCP manifest / 自定义 schema
    auth_config: dict | None
    rate_limit: dict | None
    enabled: bool


@dataclass
class ToolCall:
    id: ToolCallId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    session_id: SessionId
    tool_name: str
    arguments: dict
    result: dict | None
    error: str | None
    status: str  # running / succeeded / failed
    started_at: datetime
    finished_at: datetime | None
    latency_ms: int | None
```

**关键端口**：

```python
class MCPRuntime(Protocol):
    async def invoke(self, server: str, method: str, params: dict, *, timeout: int) -> dict: ...


class OpenAPIRuntime(Protocol):
    async def invoke(self, tool: Tool, *, operation: str, params: dict) -> dict: ...
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/tools` | GET | 200 |
| `/v1/tools` | POST | 201 |
| `/v1/tools/{tid}` | GET | 200 / 404 |
| `/v1/tools/{tid}` | PATCH / DELETE | 200 / 204 |
| `/v1/tools/{name}/invoke` | POST | 200 / 502（上游错） / 429 |
| `/v1/tools/batch_invoke` | POST | 207（多状态） |

**事件**：`ToolInvoked` `ToolCompleted` `ToolFailed` `ToolRegistered` `ToolUpdated`。

### 4.3.3 `skill` 技能包与沙箱执行 ⭐

**职责**：技能包定义、安装、版本、RunToken 签发、沙箱执行。

**领域对象**：

```python
@dataclass
class SkillPackage:
    id: SkillPackageId
    tenant_id: TenantId
    name: str
    version: str
    description: str
    runtime: str  # python / node
    entrypoint: str  # main.py / index.js
    dependencies: dict  # requirements.txt 内容
    files: dict[str, bytes]  # 上传包内容
    signature: str  # HMAC


@dataclass
class SkillInstall:
    id: SkillInstallId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    skill_id: SkillPackageId
    version: str
    installed_at: datetime
    status: str  # active / disabled


@dataclass
class SkillInvocation:
    id: SkillInvocationId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    session_id: SessionId | None
    skill_id: SkillPackageId
    inputs: dict
    result: dict | None
    error: str | None
    status: str  # running / succeeded / failed / timeout
    started_at: datetime
    finished_at: datetime | None
    duration_ms: int | None
    run_token: str  # 一次性 HMAC
    sandbox_id: str | None  # 沙箱实例 ID
```

**关键端口**：

```python
class SandboxPort(Protocol):
    async def run(self, spec: SandboxSpec, *, timeout: int) -> RunResult: ...
    async def cancel(self, run_id: str) -> None: ...


class RunTokenIssuer(Protocol):
    def issue(
        self, *, tenant_id: TenantId, skill_id: SkillPackageId, exp_seconds: int = 300
    ) -> str: ...
    def verify(self, token: str) -> TokenClaims: ...
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/skills` | GET | 200 |
| `/v1/skills` | POST | 201 |
| `/v1/skills/{sid}` | GET / PATCH / DELETE | 200 / 200 / 204 |
| `/v1/skills/{sid}/install` | POST | 201 |
| `/v1/skills/{sid}/invoke` | POST | 200 / 502（沙箱错） / 504（超时） |
| `/v1/skills/{sid}/invocations/{iid}` | GET | 200 / 404 |
| `/v1/skills/{sid}/invocations/{iid}/cancel` | POST | 202 |

**事件**：`SkillInstalled` `SkillInvoked` `SkillCompleted` `SkillFailed` `SkillTimeout`。

### 4.3.4 `memory` 长期记忆 ⭐

**状态**：P4 ✅ 已完成（122 测试通过 / 覆盖 P4-1~P4-10）。Bench: 10k 行 P95=3.66ms（目标 ≤ 200ms）。

**职责**：记忆条目 CRUD、向量索引、检索、撤销、过期清理；接入 `agent_runtime` turn 开头 recall。

**领域对象**：

```python
@dataclass(slots=True, frozen=True)
class MemoryEntry:
    id: MemoryEntryId
    tenant_id: TenantId
    workspace_id: WorkspaceId
    owner_id: UserId
    scope: MemoryScope                # 'user' / 'agent' / 'workspace'
    content: str
    metadata: dict[str, Any]
    embedding: tuple[float, ...]      # len == 1536
    revoked: bool = False
    version_lock: int = 1
    expires_at: datetime | None
    created_at: datetime
    updated_at: datetime
```

> `revoked = True` 表示软删；查询过滤 `revoked == False`。`expires_at <= now()` 自动隐藏。

**关键端口**：

```python
class EmbeddingPort(Protocol):
    async def embed(self, texts: list[str]) -> list[list[float]]: ...

class MemoryRepository(Protocol):
    async def add(self, entry: MemoryEntry) -> None: ...
    async def get(self, *, tenant_id, memory_id) -> MemoryEntry | None: ...
    async def list(self, *, tenant_id, workspace_id, scope=None, limit=50) -> list[MemoryEntry]: ...
    async def revoke(self, *, tenant_id, memory_id) -> MemoryEntry: ...
    async def purge_expired(self, *, tenant_id, now) -> int: ...

class VectorSearchPort(Protocol):
    async def upsert(self, *, tenant_id, workspace_id, memory_id, embedding, scope=None) -> None: ...
    async def search(self, *, tenant_id, workspace_id, query_embedding, top_k, scope_filter=None) -> list[VectorSearchHit]: ...
    async def delete(self, *, tenant_id, memory_id) -> None: ...

class MemoryServicePort(Protocol):  # mirror for agent_runtime
    async def recall(self, *, tenant_id, workspace_id, query, top_k=10) -> list[MemoryHit]: ...
    async def write(self, *, tenant_id, workspace_id, owner_id, scope, content, metadata=None, expires_at=None) -> MemoryEntry: ...
    async def revoke(self, *, tenant_id, workspace_id, memory_id, actor_id) -> MemoryEntry: ...
```

**关键路由**：

| 路由 | 方法 | 状态码 |
|---|---|---|
| `/v1/memories` | POST / GET | 201 / 200 |
| `/v1/memories/recall` | POST | 200 |
| `/v1/memories/{mid}` | GET / DELETE | 200 / 200 / 404 / 409 |

**事件**：`MemoryWritten` `MemoryRevoked` `MemoryExpiredPurged`。

**agent_runtime 集成**：`RunTurnUseCase` 在 turn 开始处自动调用 `memory_port.recall()`，结果以 bullet 列表渲染进 system prompt；recall 失败不阻断 turn（log warn + fallback 到裸 prompt）。

**Embedding 后端**：`embedding_runtime` 独立 sidecar（`POST /embed` + `GET /healthz`），支持 `noop` / `openai` / `sbert` 三 provider；P4 仅文件骨架，P10 真拆 sidecar。

---

## 4.4 业务模块（扩展阶段）

### 4.4.1 `model` 模型路由（P6）

**领域对象**：`Model`（tenant-scoped + workspace-可选）`ModelCredential`（AES-GCM 加密）`RoutingPolicy`（priority / round_robin / cost_optim / latency_optim / tenant_default）`QuotaCounter`（tenant + model + window_start 复合主键）。

**关键路由**：
- `POST /v1/model-credentials` 注册加密凭据（admin）
- `GET /v1/model-credentials` 列出凭据
- `POST /v1/model-credentials/{cid}/rotate` 轮换凭据
- `POST /v1/models` 注册模型（绑 credential + routing policy）
- `GET /v1/models` 列表（workspace-scoped：tenant-wide + own-workspace）
- `GET /v1/models/{mid}` / `PATCH` / `DELETE`
- `POST /v1/models/{id}/invoke` 实际 LLM 调用（hot path）
- `POST /v1/routing-policies` / `GET` 列表

**事件**：`ModelRegistered` `ModelInvoked` `ModelQuotaExceeded` `ModelCredentialRotated`。

**安全**：`secrets_ref` 兑现路径 — `ModelCredential.api_key` 永不出现在 JSONB / log / 响应；DB 落 `nonce(12) || ciphertext || tag(16)` AES-GCM envelope；master key 仅 `EOS_MODEL_MASTER_KEY` env。

**Quota 原子增量**：`INSERT ... ON CONFLICT (tenant_id, model_id, window_start) DO UPDATE SET input_tokens = input_tokens + EXCLUDED.input_tokens ... RETURNING *` — 单 SQL 原子操作防 overshoot。

**与 agent_runtime 接线**：`LLMPort.stream(req, *, model_id=ModelId | None = None)` — P6 路径走 `ModelService.invoke`；`model_id=None` 走 legacy default（向后兼容 P0–P5）。

### 4.4.2 `channel` 渠道适配（P6）

**领域对象**：`Channel`（feishu / dingtalk / wechatwork / web）+ `ChannelDelivery`（inbound / outbound + pending / sent / delivered / failed 状态机）。

**关键路由**：
- `POST /v1/channels` 注册渠道（自动加密 webhook secret 落 `channel_secrets`）
- `GET /v1/channels` 列表
- `PATCH /v1/channels/{cid}` 启停
- `POST /v1/channels/{cid}/webhook` 接收 inbound（HMAC 签名校验 + 时间戳 skew 防重放）
- `POST /v1/channels/{cid}/send` 出站发送

**事件**：`ChannelMessageReceived` `ChannelReplySent` `ChannelWebhookRejected`。

**Webhook 签名**：`HMAC-SHA256(secret, f"{ts}.{body}")`，`X-Webhook-Timestamp` + `X-Webhook-Signature: sha256=<hex>` headers；±300s skew；`hmac.compare_digest` constant-time。

**渠道适配器**：每个渠道一对 inbound + outbound adapter，解析 / 发送由 `ChannelType` 映射到 adapter 注册表；webhook 路由在 `ChannelService.handle_webhook` 内做 verify → parse → persist delivery → publish `channel.message.received`。

### 4.4.3 `knowledge` 知识包（P7）

**领域对象**：`KnowledgePackage` / `KnowledgeAsset`（原文对象存储） / `KnowledgeChunk`（chunk + embedding）。
**关键路由**：
- `POST /v1/knowledge/packages` 创建包
- `GET /v1/knowledge/packages` 列表
- `GET /v1/knowledge/packages/{pid}` 取详情
- `POST /v1/knowledge/packages/{pid}/revoke` 撤销（级联 vector 删除）
- `POST /v1/knowledge/packages/{pid}/assets/text` 文本摄取（同步 chunk + embed）
- `POST /v1/knowledge/packages/{pid}/assets/{aid}/detach` 撤销资产
- `POST /v1/knowledge/packages/{pid}/search` 检索 top-k（默认 10，最大 50）
**端口**：`KnowledgeRepository` (SQL) / `StoragePort` (`LocalDiskKnowledgeStorage` 默认) / `VectorSearchPort` (`PgVectorStore`，表 `knowledge_chunks_vec`) / `EmbeddingPort` (复用 memory 同一套) / `ChunkerPort` (`FixedWindowChunker`)。
**事件**：`KnowledgePackageCreated` / `KnowledgeAssetUploaded` / `KnowledgeAssetIngested` / `KnowledgePackageRevoked`。
**存储**：原文落 `LocalDiskKnowledgeStorage`，DB 仅存 `storage_uri`（`knowledge-asset://{tenant}/{sha256}`）。向量表 `knowledge_chunks.embedding` (HNSW cosine) + `knowledge_chunks_vec` (eos_vector mirror)。
**Secrets**：`secrets_ref` 指向 P5 secrets 表 —— 原始 secrets 永不落 JSONB。

### 4.4.4 `orchestration` 编排（P7）

**领域对象**：`Plan` / `PlanStepDSL`（7 种 kind 的 discriminated union） / `WorkflowRun` / `StepRun`。
**Plan DSL 7 种 kind**：
- `agent`：调 `SubAgentPort.run_turn_to_completion`
- `tool`：调 `ToolDispatchPort.invoke_tool`
- `skill`：调 `SkillDispatchPort.invoke_skill`（队列 + 轮询）
- `subplan`：递归起 `WorkflowRun`
- `parallel`：`asyncio.gather(*branches, return_exceptions=...)`，可设 `fail_fast`
- `conditional`：极简 lexer 表达式（`==` / `!=` / `<` / `>` / `<=` / `>=` / `in` / `contains`），无 `eval()`
- `sequence`：顺序执行（前一步失败即终止）
**模板渲染**：`${variables.x}` + `${steps.<id>.output.<key>}`；`TemplateRendererPort` 注入（v1 = `string.Template` + 自定义 mapping）。
**关键路由**：
- `POST /v1/orchestration/plans` 创建（201）
- `GET /v1/orchestration/plans` 列表
- `GET /v1/orchestration/plans/{id}` 详情
- `POST /v1/orchestration/plans/{id}/runs` 同步起 run（返回终态 + step_runs）
- `GET /v1/orchestration/runs/{id}` 查 run
- `GET /v1/orchestration/runs` 按 plan_id 过滤
- `POST /v1/orchestration/runs/{id}/cancel` 取消
**端口**：`PlanRepository` / `WorkflowRunRepository` / `SubAgentPort`（委托给 agent_runtime `RunTurnCompletionUseCase`）/ `ToolDispatchPort` / `SkillDispatchPort` / `TemplateRendererPort` / `ConditionEvaluatorPort`。
**执行器**：`WorkflowExecutor` 维护 `step_counter` 强制 `max_total_steps`；`asyncio.wait_for` 单步超时；parallel 用 `asyncio.gather`；sequence 前一步失败终止；递归 sub-plan 共享 `step_counter`。
**幂等**：`workflow_runs.idempotency_key` partial unique `(tenant_id, idempotency_key) WHERE NOT NULL` —— 相同 key 第二次创建复用上次 run。
**事件**：`PlanCreated` / `WorkflowRunStarted` / `StepRunCompleted` / `WorkflowRunCompleted` / `WorkflowRunCancelled`。
**Channel 闭环**：`ChannelDispatchSubscriber` 订阅 `ChannelMessageReceived`，起系统 actor（`SYSTEM_USER_ID` / `SYSTEM_AGENT_ID` 确定性 UUID）跑 turn，按 `channel_type` 找 `OutboundAdapter` 回写 reply；LRU dedup（1024 window）防 webhook 重放。

### 4.4.5 `agent_factory` Agent 模板（P8）

**领域对象**：`AgentTemplate` `AgentVersion`（不可变）`Release`。
**关键路由**：`/v1/agents` CRUD（创建 Agent）`/v1/agents/{aid}/versions` 版本管理、`/v1/agents/{aid}/release` 发布。
**事件**：`AgentCreated` `AgentVersionPublished` `AgentReleased`。

### 4.4.6 `evaluation` 评测（P8）

**领域对象**：`EvalDataset` `EvalRun` `EvalScore` `EvalGate`。
**关键路由**：`/v1/eval/datasets` CRUD、`/v1/eval/runs` 启动、`/v1/eval/runs/{rid}` 报告。
**门禁**：低于 `EOS_EVAL_SCORE_MIN` 拒绝发布（返回 422 `EVAL_GATE_FAILED`）。

### 4.4.7 `platform` 平台级（P9）

**领域对象**：`Plan`（平台套餐）`Subscription` `Extension`（插件）。
**关键路由**：`/v1/platform/plans` `POST /v1/platform/subscriptions` 等。
**事件**：`SubscriptionChanged` `ExtensionEnabled`。

---

## 4.5 起步阶段模块优先级（P0-P4）

| 阶段 | 模块 | 关键交付 |
|---|---|---|
| **P0 准备** | libs 全部 + `identity` | workspace / 守则 / 多租户 |
| **P1 agent_runtime** | agent_runtime | Session / Turn / SSE 流式 |
| **P2 tool** | tool | tool 注册 / MCP 适配 / 调用 |
| **P3 skill** | skill | skill 包 / 沙箱 / RunToken |
| **P4 memory** | memory | 向量存储 / 写入 / 检索 / 撤销 |

**P4 结束时闭环**：

```
User → agent_runtime (Session/Turn/SSE) → tool / skill / memory → Response
                                                            ↓
                                            governance（policy gate）
                                                            ↓
                                            observability_module（埋点）
```

P5 起逐步补齐治理与扩展模块（详见 [12-实施计划.md](12-实施计划.md)）。

---

## 4.6 与原模板的关键差异

| 维度 | 原模板（8 模块） | Enterprise-Agent-OS（14 模块） |
|---|---|---|
| Agent 抽象 | 隐式（在 collab 中） | 独立 `agent_runtime` + `agent_factory` |
| 起步模块 | 全模块渐进 | **4 模块闭环优先** |
| 模型 | cap 内 | 独立 `model` |
| 知识 / 渠道 | cap 内 | 独立 `knowledge` / `channel` |
| 编排 | workflow | `orchestration`（可调其他 Agent） |
| 治理 | policy / audit 分离 | 合并 `governance`（审计是订阅者） |
| 横切 | observability / policy | `identity` / `governance` / `observability_module` |
| 平台化 | 无 | `platform` + `evaluation` |
| 模块数 | 8 | 14 |

---

## 4.7 错误码总表（各模块共用）

| 错误 | 状态码 | code | 触发 |
|---|---|---|---|
| `BadRequestError` | 400 | `BAD_REQUEST` | schema 校验失败 |
| `UnauthorizedError` | 401 | `UNAUTHORIZED` | 无 token / 过期 |
| `IdentityMockForbidden` | 401 | `IDENTITY_MOCK_FORBIDDEN` | mock-*-token 被禁 |
| `ForbiddenError` | 403 | `FORBIDDEN` | 角色不足 |
| `ActionDeniedError` | 403 | `ACTION_DENIED` | policy deny |
| `WorkspaceDeniedError` | 403 | `WORKSPACE_DENIED` | 跨 workspace |
| `TenantDeniedError` | 403 | `TENANT_DENIED` | 跨 tenant |
| `NotFoundError` | 404 | `NOT_FOUND` | 资源不存在 |
| `ConflictError` | 409 | `CONFLICT` | 状态冲突 |
| `GoneError` | 410 | `GONE` | 资源已硬删 |
| `PreconditionFailedError` | 412 | `PRECONDITION_FAILED` | If-Match 失败 |
| `UnprocessableError` | 422 | `UNPROCESSABLE` | 语义校验失败 |
| `EvalGateFailedError` | 422 | `EVAL_GATE_FAILED` | 评测不达标 |
| `QuotaExceededError` | 429 | `QUOTA_EXCEEDED` | 配额耗尽 |
| `RateLimitedError` | 429 | `RATE_LIMITED` | 限流 |
| `UpstreamUnavailableError` | 502 | `UPSTREAM_UNAVAILABLE` | 上游 5xx |
| `SandboxTimeoutError` | 504 | `SANDBOX_TIMEOUT` | 沙箱超时 |
| `InternalError` | 500 | `INTERNAL_ERROR` | 未捕获异常 |

详见 [06-接口规范.md](06-接口规范.md)。
