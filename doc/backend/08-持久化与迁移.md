# 08 · 持久化与迁移

> ORM 封装、UnitOfWork、Alembic 迁移、硬删语义、多租户隔离、持久化策略。

> **基于 docs/python-refactor/08-持久化与迁移.md 模板**。
> 与原模板差异：所有表强制带 `tenant_id` 列；ORM 自动注入租户过滤；Event 表分区按租户；skill / tool / knowledge 产物走对象存储。

---

## 8.1 总体策略

| 项 | 选择 |
|---|---|
| ORM | **SQLAlchemy 2.0 async**（asyncpg 驱动） |
| 迁移工具 | **Alembic**（统一脚本） |
| 会话管理 | **UnitOfWork** + `SessionFactory` |
| ORM 模型归属 | `modules/<m>/adapter/persistence/models.py`（不外露） |
| 跨模块 ID 类型 | `libs/schema` Pydantic |
| 多租户 | 所有表带 `tenant_id`；ORM 自动注入过滤 |
| 软删 | 仅 `memory_entries.revoked`；其他表硬删 |
| 硬删语义 | `DE_ENV != demo` 时，删除走 `PersistDelete(Sync)` |
| 向量存储 | pgvector（默认；同库） |
| 对象存储 | S3 兼容（MinIO dev / 阿里云 OSS 生产） |

---

## 8.2 SQLAlchemy 基础

### 8.2.1 Base

```python
# libs/persistence/src/eos_db/base.py
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column
from sqlalchemy import String


class Base(DeclarativeBase):
    pass


class TenantScopedMixin:
    """约定：所有租户相关表必须 include"""

    tenant_id: Mapped[str] = mapped_column(String, index=True, nullable=False)
```

### 8.2.2 SessionFactory

```python
# libs/persistence/src/eos_db/session_factory.py
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker


class SessionFactory:
    def __init__(self, url: str, *, pool_size=20, max_overflow=10, echo=False):
        self.engine = create_async_engine(
            url, pool_size=pool_size, max_overflow=max_overflow, echo=echo
        )
        self.sessionmaker = async_sessionmaker(self.engine, expire_on_commit=False)

    def session(self):
        return self.sessionmaker()

    async def close(self):
        await self.engine.dispose()
```

### 8.2.3 UnitOfWork

```python
# libs/persistence/src/eos_db/unit_of_work.py
class UnitOfWork:
    def __init__(self, factory: SessionFactory):
        self._factory = factory
        self.session = None

    async def __aenter__(self):
        self.session = self._factory.session()
        return self

    async def __aexit__(self, exc_type, exc, tb):
        try:
            if exc_type is None:
                await self.session.commit()
            else:
                await self.session.rollback()
        finally:
            await self.session.close()
```

---

## 8.3 多租户 ORM 守卫

### 8.3.1 自动注入 tenant_id 过滤

```python
# libs/persistence/src/eos_db/tenant_query.py
from sqlalchemy import event
from eos_kernel import current_tenant, current_workspace
from .base import TenantScopedMixin


@event.listens_for(Session, "do_orm_execute")
def _enforce_tenant(state):
    if not state.is_select:
        return
    tenant = current_tenant()
    if tenant is None:
        # 后台任务 / migration 场景可显式 bypass
        return
    for desc in state.statement.column_descriptions:
        entity = desc.get("entity")
        if entity and hasattr(entity, "tenant_id"):
            state.statement = state.statement.where(entity.tenant_id == tenant.tenant_id)
```

### 8.3.2 约定

- 所有租户相关 ORM 类必须 include `TenantScopedMixin`
- 没有 `tenant_id` 列的查询不强制注入（系统表、审计表）
- Lint 规则：CI 检查 `modules/*/adapter/persistence/models.py` 强制 import `TenantScopedMixin`

### 8.3.3 workspace 过滤

`workspace_id` 不是所有表都强制（部分表跨 workspace 共享），按需在仓储层显式 `where(workspace_id == current_workspace().id)`。

---

## 8.4 ORM 模型（agent_runtime 示例）

```python
# modules/agent_runtime/src/deos/modules/agent_runtime/adapter/persistence/models.py
from sqlalchemy import String, DateTime, ForeignKey, JSON, Index
from sqlalchemy.orm import Mapped, mapped_column, relationship
from eos_db.base import Base, TenantScopedMixin


class SessionOrm(Base, TenantScopedMixin):
    __tablename__ = "sessions"
    __table_args__ = (
        Index("ix_sessions_tenant_workspace_status", "tenant_id", "workspace_id", "status"),
        Index("ix_sessions_tenant_owner_created", "tenant_id", "owner_id", "created_at"),
    )

    id: Mapped[str] = mapped_column(String, primary_key=True)
    workspace_id: Mapped[str] = mapped_column(String, index=True)
    owner_id: Mapped[str] = mapped_column(String, index=True)
    agent_id: Mapped[str] = mapped_column(String, index=True)
    agent_version: Mapped[str] = mapped_column(String)
    status: Mapped[str] = mapped_column(String, default="open")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    closed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    metadata_: Mapped[dict] = mapped_column("metadata", JSON, default=dict)

    turns: Mapped[list["TurnOrm"]] = relationship(
        back_populates="session", cascade="all, delete-orphan"
    )


class TurnOrm(Base, TenantScopedMixin):
    __tablename__ = "turns"
    __table_args__ = (
        Index("ix_turns_tenant_session_started", "tenant_id", "session_id", "started_at"),
    )

    id: Mapped[str] = mapped_column(String, primary_key=True)
    workspace_id: Mapped[str] = mapped_column(String, index=True)
    session_id: Mapped[str] = mapped_column(String, ForeignKey("sessions.id"), index=True)
    user_input: Mapped[str] = mapped_column(String)
    status: Mapped[str] = mapped_column(String, default="running")
    started_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    finished_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    final_response: Mapped[str | None] = mapped_column(String, nullable=True)
    usage: Mapped[dict] = mapped_column(JSON, default=dict)
```

---

## 8.5 仓储示例

```python
# modules/agent_runtime/src/deos/modules/agent_runtime/adapter/persistence/session_repo.py
from sqlalchemy import select, delete
from libs.persistence import SessionFactory


class PgSessionRepo:
    def __init__(self, factory: SessionFactory):
        self._factory = factory

    async def add(self, session: Session) -> None:
        async with self._factory.session() as s:
            s.add(SessionOrm.from_entity(session))
            await s.commit()

    async def get(self, id: SessionId) -> Session | None:
        async with self._factory.session() as s:
            orm = (
                await s.execute(select(SessionOrm).where(SessionOrm.id == str(id)))
            ).scalar_one_or_none()
            return orm.to_entity() if orm else None

    async def delete(self, id: SessionId) -> None:
        """硬删：先删 turns，再删 sessions"""
        async with self._factory.session() as s:
            await s.execute(delete(TurnOrm).where(TurnOrm.session_id == str(id)))
            await s.execute(delete(SessionOrm).where(SessionOrm.id == str(id)))
            await s.commit()
```

---

## 8.6 Alembic 迁移

### 8.6.1 目录

```text
migrations/
├── env.py
├── script.py.mako
└── versions/
    ├── 0001_init.py
    ├── 0002_default_agent.py
    ├── 0003_event_partitioning.py
    └── …
```

### 8.6.2 env.py（async）

```python
# migrations/env.py
import asyncio
from logging.config import fileConfig
from sqlalchemy import pool
from sqlalchemy.ext.asyncio import async_engine_from_config
from alembic import context

from libs.persistence import Base

# 导入所有模块的 ORM 模型以注册到 Base.metadata
from modules.identity.adapter.persistence.models import (
    TenantOrm,
    WorkspaceOrm,
    UserOrm,
    APIKeyOrm,
)
from modules.agent_runtime.adapter.persistence.models import SessionOrm, TurnOrm
from modules.tool.adapter.persistence.models import ToolOrm, ToolCallOrm
from modules.skill.adapter.persistence.models import (
    SkillPackageOrm,
    SkillInstallOrm,
    SkillInvocationOrm,
)
from modules.memory.adapter.persistence.models import MemoryEntryOrm
from modules.governance.adapter.persistence.models import (
    PolicyRuleOrm,
    ApprovalOrm,
    DecisionEventOrm,
)
from modules.knowledge.adapter.persistence.models import KnowledgePackageOrm, KnowledgeAssetOrm
from modules.model.adapter.persistence.models import ModelOrm, ModelCredentialOrm
from modules.channel.adapter.persistence.models import ChannelOrm, ChannelDeliveryOrm
from modules.orchestration.adapter.persistence.models import PlanOrm, StepOrm, WorkflowRunOrm
from modules.agent_factory.adapter.persistence.models import AgentTemplateOrm, AgentVersionOrm
from modules.evaluation.adapter.persistence.models import EvalDatasetOrm, EvalRunOrm, EvalScoreOrm
from modules.observability_module.adapter.persistence.models import (
    RunRecordOrm,
    CostRecordOrm,
    QualityScoreOrm,
)
from modules.platform.adapter.persistence.models import PlanOrm as PlatformPlanOrm, SubscriptionOrm

config = context.config
fileConfig(config.config_file_name)
target_metadata = Base.metadata


# 启用 pgvector
def include_object(obj, name, type_, reflected, compare_to):
    return True


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection):
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()


async def run_migrations_online() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


if context.is_offline_mode():
    run_migrations_offline()
else:
    asyncio.run(run_migrations_online())
```

### 8.6.3 初始迁移示例

```python
# migrations/versions/0001_init.py
"""init

Revision ID: 0001
Revises:
Create Date: 2026-09-19

"""

from alembic import op
import sqlalchemy as sa
from pgvector.sqlalchemy import Vector

revision = "0001"
down_revision = None


def upgrade() -> None:
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")

    op.create_table(
        "tenants",
        sa.Column("id", sa.String, primary_key=True),
        sa.Column("name", sa.String(255), nullable=False),
        sa.Column("plan", sa.String(32), server_default="free"),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
    )

    op.create_table(
        "workspaces",
        sa.Column("id", sa.String, primary_key=True),
        sa.Column("tenant_id", sa.String, sa.ForeignKey("tenants.id"), nullable=False, index=True),
        sa.Column("name", sa.String(255), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
    )

    op.create_table(
        "users",
        sa.Column("id", sa.String, primary_key=True),
        sa.Column("tenant_id", sa.String, sa.ForeignKey("tenants.id"), nullable=False, index=True),
        sa.Column("email", sa.String(320), nullable=False),
        sa.Column("workspace_ids", sa.JSON, default=list),
        sa.Column("roles", sa.JSON, default=list),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.UniqueConstraint("tenant_id", "email", name="uq_users_tenant_email"),
    )

    op.create_table(
        "sessions",
        sa.Column("id", sa.String, primary_key=True),
        sa.Column("tenant_id", sa.String, nullable=False, index=True),
        sa.Column("workspace_id", sa.String, nullable=False, index=True),
        sa.Column("owner_id", sa.String, nullable=False, index=True),
        sa.Column("agent_id", sa.String, nullable=False, index=True),
        sa.Column("agent_version", sa.String, nullable=False),
        sa.Column("status", sa.String(32), server_default="open"),
        sa.Column("metadata", sa.JSON, default=dict),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("closed_at", sa.DateTime(timezone=True), nullable=True),
    )
    op.create_index(
        "ix_sessions_tenant_workspace_status", "sessions", ["tenant_id", "workspace_id", "status"]
    )
    op.create_index(
        "ix_sessions_tenant_owner_created", "sessions", ["tenant_id", "owner_id", "created_at"]
    )

    op.create_table(
        "memory_entries",
        sa.Column("id", sa.String, primary_key=True),
        sa.Column("tenant_id", sa.String, nullable=False, index=True),
        sa.Column("workspace_id", sa.String, nullable=False, index=True),
        sa.Column("owner_id", sa.String, nullable=False, index=True),
        sa.Column("scope", sa.String(32), nullable=False),
        sa.Column("content", sa.Text, nullable=False),
        sa.Column("embedding", Vector(1536), nullable=True),
        sa.Column("metadata", sa.JSON, default=dict),
        sa.Column("revoked", sa.Boolean, server_default=sa.text("false"), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("expires_at", sa.DateTime(timezone=True), nullable=True),
    )
    # 向量索引（IVFFlat 或 HNSW）
    op.execute(
        "CREATE INDEX ix_memory_embedding ON memory_entries USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100)"
    )


def downgrade() -> None:
    op.drop_table("memory_entries")
    op.drop_index("ix_sessions_tenant_owner_created", "sessions")
    op.drop_index("ix_sessions_tenant_workspace_status", "sessions")
    op.drop_table("sessions")
    op.drop_table("users")
    op.drop_table("workspaces")
    op.drop_table("tenants")
```

---

## 8.7 硬删语义

### 8.7.1 模式

| 环境 | 行为 |
|---|---|
| `EOS_ENV=demo` | 内存 store；不 Persist |
| `EOS_ENV=development` 及以上 | PG hydrate；Upsert 写回；硬删必须 `PersistDelete(Sync)` |

### 8.7.2 覆盖表清单

| 模块 | 主表 | 子表 |
|---|---|---|
| agent_runtime | `sessions` | `turns` `tool_calls` `skill_invocations` |
| tool | `tools` | `tool_calls` |
| skill | `skill_packages` | `skill_installs` `skill_invocations` |
| memory | `memory_entries` | （软删 `revoked`，不级联） |
| knowledge | `knowledge_packages` | `knowledge_assets` |
| governance | `policies` | `decisions` |
| channel | `channels` | `channel_deliveries` |
| orchestration | `plans` | `steps` `workflow_runs` |
| agent_factory | `agent_templates` | `agent_versions`（published 不可删） |
| evaluation | `eval_datasets` | `eval_runs` `eval_scores` |
| identity | `users` | `api_keys` `user_sessions` |
| identity | `workspaces` | （级联删除 user_workspaces 关联） |
| identity | `tenants` | 不可硬删（410 GONE） |

---

## 8.8 事务边界与发布顺序

### 8.8.1 反模式

```python
# 错误：在事务内发布事件
async def create_session(self, ctx, command):
    async with self.uow_factory() as uow:
        await self.sessions.add(session)
        await self.events.publish(SessionStarted(...))  # ❌
        await uow.commit()
```

### 8.8.2 正例

```python
# 正确：先提交事务，再发布事件
async def create_session(self, ctx, command):
    session = Session.create(...)
    async with self.uow_factory() as uow:
        await self.sessions.add(session)
        await uow.commit()
    # 事务外发布
    await self.events.publish(SessionStarted(...))
    return session
```

### 8.8.3 Outbox Pattern（远期）

预留 `event_outbox` 表 schema；本期不实现。

---

## 8.9 多副本与读写分离

```python
class SessionFactory:
    def __init__(self, url, *, replica_url=None, replica_mode="active"):
        self.engine = create_async_engine(url, ...)
        self.replica_engine = (
            create_async_engine(replica_url, ...)
            if replica_url and replica_mode == "standby"
            else None
        )

    def session(self, *, read_only=False):
        if read_only and self.replica_engine:
            return async_sessionmaker(self.replica_engine)()
        return self.sessionmaker()
```

---

## 8.10 测试与夹具

### 8.10.1 testcontainers

```python
# tests/integration/conftest.py
import pytest
from testcontainers.postgres import PostgresContainer


@pytest.fixture(scope="session")
def pg_url():
    with PostgresContainer("postgres:16") as pg:
        url = pg.get_connection_url().replace("postgresql://", "postgresql+asyncpg://")
        yield url
```

### 8.10.2 多租户测试 fixture

```python
@pytest.fixture
def tenant_context():
    from eos_kernel import tenant_var

    tenant_var.set(TenantContext(tenant_id="tnt_test", plan="pro"))
    yield
    tenant_var.set(None)
```

---

## 8.11 数据模式

| 数据类型 | 存储 | 加密 |
|---|---|---|
| 租户元信息 | `tenants` | 否 |
| 用户邮箱 / 名称 | `users` | 否 |
| API Key 哈希 | `api_keys` | argon2 |
| JWT Secret | env | secret manager |
| LLM 凭据 | `model_credentials`（加密列） | AES-GCM via libs/vault |
| Tool 凭据 | `tool_credentials`（加密列） | AES-GCM |
| 渠道凭据 | `channel_credentials`（加密列） | AES-GCM |
| Skill 产物 | 对象存储 + DB 存元数据 | 由存储侧 |
| 知识原文 | 对象存储 + DB 存元数据 | 由存储侧 |
| 附件 | 对象存储 + DB 存元数据 | 由存储侧 |
| 审计事件 | `audit_events` | 否 |
| 用量计量 | `cost_records` | 否 |
| 上下文快照 | `context_snapshots`（JSONB） | 否 |
| 临时审批 | `approvals` | 否 |
| 事件 DLQ | `eos:events:dlq`（Redis Stream） | 否 |

---

## 8.12 性能与索引

| 表 | 索引 |
|---|---|
| `sessions` | `(tenant_id, workspace_id, status, created_at)` `(tenant_id, owner_id, created_at)` |
| `turns` | `(tenant_id, session_id, started_at)` |
| `memory_entries` | `(tenant_id, workspace_id, owner_id, revoked)` + `ivfflat(embedding)` |
| `tool_calls` | `(tenant_id, workspace_id, started_at)` |
| `skill_invocations` | `(tenant_id, workspace_id, started_at)` |
| `audit_events` | `(tenant_id, occurred_at)` `(actor_id, occurred_at)` |
| `cost_records` | `(tenant_id, metric, occurred_at)` |
| `knowledge_assets` | `(tenant_id, package_id, created_at)` |

> 所有租户相关查询必须命中 `(tenant_id, ...)` 前缀索引，避免跨租户扫表。

---

## 8.13 监控

| 指标 | 来源 |
|---|---|
| `db_pool_size` | SQLAlchemy engine 事件 |
| `db_query_duration_seconds{module, table}` | 自定义埋点 |
| `db_transaction_rollback_total{module}` | 埋点 |
| `db_lock_wait_seconds` | PG `pg_locks` |
| `pgvector_index_size` | PG 系统表 |
| `cross_tenant_query_blocked_total` | ORM 守卫触发 |

---

## 8.14 与原模板的关键差异

| 维度 | 原模板 | Enterprise-Agent-OS |
|---|---|---|
| 多租户 | 仅 Workspace | **Tenant + Workspace** 强制两级 |
| ORM 守卫 | 无 | 自动注入 `tenant_id` 过滤 |
| ORM 约定 | 无 | 必须 include `TenantScopedMixin` |
| 向量 | 不在主库 | pgvector 同库起步 |
| 硬删覆盖表 | 5 张 | 13 张（含 platform / evaluation） |
| 租户表 | 不可删 | `tenants` 不可硬删（仅 410） |
