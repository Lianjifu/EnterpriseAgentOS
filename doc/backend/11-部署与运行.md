# 11 · 部署与运行

> Makefile 入口、docker-compose、环境变量、性能目标、运维手册。

> **基于 docs/python-refactor/11-部署与运行.md 模板**。
> 与原模板差异：环境变量用 `EOS_` 前缀；新增 MinIO（对象存储）/ pgvector / 远程 runtime 服务；多租户标识必填。

---

## 11.1 Makefile 入口

```makefile
# enterprise-agent-os/Makefile

.PHONY: install build run-app run-app-with-gateway run-skill-runtime \
        run-tool-runtime run-embedding-runtime run-rag \
        test test-contract smoke db-upgrade db-downgrade db-revision \
        lint import-lint type-check clean

install:
	uv sync

build:
	docker compose -f deploy/compose.yml build

# ---------- 运行 ----------

run-app:
	uv run uvicorn deos.composition.main:create_app \
		--host 0.0.0.0 --port 8100 --workers 4 --loop uvloop \
		--access-log --no-server-header

run-app-with-gateway:
	@echo "Starting eos-app on :8100 and eos-gateway on :8089"
	uv run uvicorn deos.composition.main:create_app --host 0.0.0.0 --port 8100 --workers 4 --loop uvloop &
	uv run uvicorn deos.gateway.main:create_gateway --host 0.0.0.0 --port 8089 --loop uvloop

run-skill-runtime:
	uv run uvicorn deos.runtimes.skill_runtime.app.main:app \
		--host 0.0.0.0 --port 8093 --workers 2 --loop uvloop

run-tool-runtime:
	uv run uvicorn deos.runtimes.tool_runtime.app.main:app \
		--host 0.0.0.0 --port 8094 --workers 2 --loop uvloop

run-embedding-runtime:
	uv run uvicorn deos.runtimes.embedding_runtime.app.main:app \
		--host 0.0.0.0 --port 8095 --loop uvloop

# ---------- 数据库 ----------

db-upgrade:
	uv run alembic upgrade head

db-downgrade:
	uv run alembic downgrade -1

db-revision:
	uv run alembic revision --autogenerate -m "$(name)"

db-reset-dev:
	uv run alembic downgrade base
	uv run alembic upgrade head

# ---------- 测试 ----------

test:
	uv run pytest -q --cov

test-contract:
	uv run pytest tests/contract -q

smoke:
	uv run python tests/e2e/smoke.py --base http://127.0.0.1:8100

# ---------- 守则 ----------

lint:
	uv run ruff check .
	uv run ruff format --check .

import-lint:
	uv run lint-imports --config .importlinter.ini

type-check:
	uv run mypy libs modules composition gateways runtimes

# ---------- 清理 ----------

clean:
	find . -type d -name __pycache__ -exec rm -rf {} +
	find . -type d -name .pytest_cache -exec rm -rf {} +
	find . -type d -name .mypy_cache -exec rm -rf {} +
	find . -type d -name .ruff_cache -exec rm -rf {} +
```

---

## 11.2 docker-compose

### 11.2.1 `deploy/compose.yml`

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eos
    ports: ["5432:5432"]
    volumes:
      - eos_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    restart: unless-stopped
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
    ports:
      - "9000:9000"   # API
      - "9001:9001"   # Console
    volumes:
      - eos_minio:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command:
      - uvicorn
      - deos.composition.main:create_app
      - --host=0.0.0.0
      - --port=8100
      - --workers=4
      - --loop=uvloop
    env_file: [.env]
    environment:
      EOS_DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/eos
      EOS_REDIS_URL: redis://redis:6379/0
      EOS_OBJECT_STORAGE_URL: http://minio:9000
      EOS_OBJECT_STORAGE_BUCKET: eos-artifacts
      EOS_SKILL_RUNTIME_URL: http://skill-runtime:8093
      EOS_TOOL_RUNTIME_URL: http://tool-runtime:8094
      EOS_EMBEDDING_RUNTIME_URL: http://embedding-runtime:8095
      EOS_RUNTIME_MODE: remote
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
      skill-runtime: { condition: service_started }
      tool-runtime: { condition: service_started }
      embedding-runtime: { condition: service_started }
    ports: ["8100:8100"]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8100/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5

  gateway:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command:
      - uvicorn
      - deos.gateway.main:create_gateway
      - --host=0.0.0.0
      - --port=8089
      - --loop=uvloop
    environment:
      EOS_APP_UPSTREAM: http://app:8100
      EOS_REDIS_URL: redis://redis:6379/0
    depends_on:
      app: { condition: service_healthy }
    ports: ["8089:8089"]
    profiles: ["with-gateway"]

  skill-runtime:
    build:
      context: ..
      dockerfile: runtimes/deos_skill_runtime/Dockerfile
    command: uvicorn deos.runtimes.skill_runtime.app.main:app --host 0.0.0.0 --port 8093 --workers 2
    ports: ["8093:8093"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock    # 沙箱内启动 docker
    profiles: ["with-runtimes"]

  tool-runtime:
    build:
      context: ..
      dockerfile: runtimes/deos_tool_runtime/Dockerfile
    command: uvicorn deos.runtimes.tool_runtime.app.main:app --host 0.0.0.0 --port 8094 --workers 2
    ports: ["8094:8094"]
    profiles: ["with-runtimes"]

  embedding-runtime:
    build:
      context: ..
      dockerfile: runtimes/deos_embedding_runtime/Dockerfile
    command: uvicorn deos.runtimes.embedding_runtime.app.main:app --host 0.0.0.0 --port 8095
    ports: ["8095:8095"]
    profiles: ["with-runtimes"]

volumes:
  eos_postgres:
  eos_minio:
```

### 11.2.2 启动

```bash
cd enterprise-agent-os

# 基础（含 runtimes）
docker compose -f deploy/compose.yml --profile with-runtimes up

# 含网关
docker compose -f deploy/compose.yml --profile with-runtimes --profile with-gateway up

# 验证
curl -sS http://127.0.0.1:8089/healthz  # 经网关
curl -sS http://127.0.0.1:8100/healthz  # 直连 app
```

---

## 11.3 环境变量

### 11.3.1 完整列表

| 变量 | 默认 | 说明 |
|---|---|---|
| `EOS_ENV` | `development` | `demo` / `development` / `staging` / `production` |
| `EOS_APP_HOST` | `0.0.0.0` | 监听地址 |
| `EOS_APP_PORT` | `8100` | 监听端口 |
| `EOS_INSTANCE_ID` | `local-1` | 实例标识（多副本时唯一） |
| `EOS_DATABASE_URL` | — | PG 异步连接串 |
| `EOS_REDIS_URL` | — | Redis 连接串 |
| `EOS_OBJECT_STORAGE_URL` | — | S3 兼容对象存储 URL |
| `EOS_OBJECT_STORAGE_BUCKET` | — | 默认 bucket |
| `EOS_JWT_SECRET` | `dev-secret` | JWT 签名密钥 |
| `EOS_JWT_TTL_SECONDS` | `3600` | JWT 有效期 |
| `EOS_BAN_MOCK_TOKEN` | `True` | 拒绝 `mock-*-token` |
| `EOS_ALLOW_DEMO_TOKEN` | `False` | 显式允许演示 token |
| `EOS_API_KEY_DEFAULT_RATE` | `1000` | 每分钟 API Key 默认限速 |
| `EOS_SKILL_RUNTIME_URL` | `http://127.0.0.1:8093` | 沙箱入口 |
| `EOS_TOOL_RUNTIME_URL` | `http://127.0.0.1:8094` | Tool runtime |
| `EOS_EMBEDDING_RUNTIME_URL` | `http://127.0.0.1:8095` | Embedding runtime |
| `EOS_RUNTIME_MODE` | `local` | `local` / `remote` |
| `EOS_RUNTIME_FAILOVER_LOCAL` | `False` | 非生产 remote 失败可回落 local |
| `EOS_WITH_GOVERNANCE` | `True` | 是否启用治理 |
| `EOS_APPROVAL_TTL_SECONDS` | `3600` | approval 默认过期时间 |
| `EOS_EVAL_SCORE_MIN` | `0.7` | 评测分数门禁 |
| `EOS_EVAL_RECALL_MIN` | `0.7` | 评测召回门禁 |
| `EOS_GATEWAY_PORT` | `8089` | 网关端口 |
| `EOS_APP_UPSTREAM` | `http://127.0.0.1:8100` | 网关 → app |
| `EOS_SENTRY_DSN` | — | Sentry DSN |
| `EOS_SENTRY_SAMPLE_RATE` | `0.1` | 追踪采样率 |
| `EOS_OTLP_ENDPOINT` | — | OTel exporter endpoint |
| `EOS_LOG_LEVEL` | `INFO` | 日志级别 |
| `EOS_MULTI_TENANT_ENABLED` | `True` | 多租户开关 |
| `EOS_DEFAULT_TENANT_PLAN` | `free` | 新租户默认套餐 |

### 11.3.2 `.env` 示例

```env
EOS_ENV=development
EOS_APP_PORT=8100

EOS_DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/eos
EOS_REDIS_URL=redis://127.0.0.1:6379/0
EOS_OBJECT_STORAGE_URL=http://127.0.0.1:9000
EOS_OBJECT_STORAGE_BUCKET=eos-artifacts

EOS_BAN_MOCK_TOKEN=1
EOS_ALLOW_DEMO_TOKEN=0

EOS_SKILL_RUNTIME_URL=http://127.0.0.1:8093
EOS_TOOL_RUNTIME_URL=http://127.0.0.1:8094
EOS_EMBEDDING_RUNTIME_URL=http://127.0.0.1:8095

EOS_RUNTIME_MODE=local
EOS_WITH_GOVERNANCE=1

EOS_LOG_LEVEL=INFO
```

---

## 11.4 Dockerfile

### 11.4.1 业务 Dockerfile

```dockerfile
# deploy/Dockerfile
FROM python:3.12-slim AS base

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev curl \
    && rm -rf /var/lib/apt/lists/*

RUN pip install uv

WORKDIR /app

# 先复制依赖文件以利用缓存
COPY pyproject.toml uv.lock ./
COPY libs/ ./libs/
COPY modules/ ./modules/
COPY composition/ ./composition/
COPY gateways/ ./gateways/
COPY migrations/ ./migrations/
COPY runtimes/ ./runtimes/

RUN uv sync --no-dev --frozen

EXPOSE 8100

HEALTHCHECK --interval=15s --timeout=5s --retries=5 \
    CMD curl -fsS http://localhost:8100/livez || exit 1

CMD ["uvicorn", "deos.composition.main:create_app", \
     "--host=0.0.0.0", "--port=8100", "--workers=4", "--loop=uvloop"]
```

### 11.4.2 Runtime Dockerfile（skill 示例）

```dockerfile
# runtimes/deos_skill_runtime/Dockerfile
FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1
RUN pip install uv

WORKDIR /app
COPY pyproject.toml uv.lock ./
COPY libs/ ./libs/
COPY runtimes/deos_skill_runtime ./runtimes/deos_skill_runtime
RUN uv sync --no-dev --frozen

EXPOSE 8093

CMD ["uvicorn", "deos.runtimes.skill_runtime.app.main:app", \
     "--host=0.0.0.0", "--port=8093", "--workers=2", "--loop=uvloop"]
```

---

## 11.5 本机启动路径（开发）

### 11.5.1 路径 A：纯本地（推荐开发）

```bash
# 1. 起 PG + Redis + MinIO（Docker）
bash scripts/dev-stack/ensure-docker-postgres.sh

# 2. 启动主应用
cd enterprise-agent-os
make install
make db-upgrade
make run-app          # :8100
# 另开终端
make run-skill-runtime   # :8093
make run-tool-runtime    # :8094
make run-embedding-runtime # :8095

# 3. 前端
cd ../web
pnpm install
pnpm dev
```

### 11.5.2 路径 B：LaunchAgent 常驻

`scripts/dev-stack/run-stack.sh`：

```bash
#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/../../enterprise-agent-os"

export EOS_ENV=development
export EOS_RUNTIME_MODE=local

declare -A SERVICES=(
  ["eos-app"]="uvicorn deos.composition.main:create_app --port 8100 --workers 4 --loop uvloop"
  ["eos-skill-runtime"]="uvicorn deos.runtimes.skill_runtime.app.main:app --port 8093 --workers 2 --loop uvloop"
  ["eos-tool-runtime"]="uvicorn deos.runtimes.tool_runtime.app.main:app --port 8094 --workers 2 --loop uvloop"
  ["eos-embedding-runtime"]="uvicorn deos.runtimes.embedding_runtime.app.main:app --port 8095 --loop uvloop"
)

for name in "${!SERVICES[@]}"; do
  launchctl kickstart -k "gui/$(id -u)/com.eos.${name}"
done
```

### 11.5.3 路径 C：Compose

```bash
cd enterprise-agent-os
docker compose -f deploy/compose.yml --profile with-runtimes up -d
curl -sS http://127.0.0.1:8100/healthz
```

### 11.5.4 路径 D：prod 部署（P10）

```bash
cd enterprise-agent-os

# 1. 复制 prod env（不提交）
cp deploy/env.prod.example .env.prod
$EDITOR .env.prod   # 填 vault 注入的 EOS_*_REF

# 2. 启动 stable + canary + PG / Redis / MinIO
docker compose -f deploy/docker-compose.prod.yml --profile prod up -d

# 3. 验证
curl -fsS http://127.0.0.1:8102/livez   # stable
curl -fsS -H "X-EOS-Ring: canary" http://127.0.0.1:8102/livez   # canary

# 4. k8s 等价：见 infra/k8s/README.md
```

详见 [`../../deploy/README.md`](../../deploy/README.md) +
[`../../infra/k8s/README.md`](../../infra/k8s/README.md)。

---

## 11.6 性能目标

| 指标 | 目标 | 备注 |
|---|---|---|
| 冷启动时间 | ≤ 30s | 含 ensure |
| 启动后内存 | ≤ 800 MB / worker | 4 workers × 800 MB = 3.2 GB |
| P95 单回合延迟 | ≤ 1500ms | SSE 主路径 |
| HTTP 普通接口 P95 | ≤ 200ms | |
| SSE 流式 P95（首个 chunk） | ≤ 1000ms | |
| Tool 调用 P95 | ≤ 500ms | 同步 |
| Skill 执行 P95 | ≤ 30s | 含沙箱启动 |
| Memory 检索 P95 | ≤ 200ms | top-k=10 |
| `/healthz` P95 | ≤ 10ms | |
| `/metrics` P95 | ≤ 50ms | |
| 事件总线投递 P95 | ≤ 100ms | 进程内 |

### 11.6.1 uvicorn 配置

- `workers = (2 * CPU) + 1`
- `--loop uvloop`
- `--http httptools`
- `--no-server-header`

### 11.6.2 SQLAlchemy 连接池

```python
SessionFactory(
    settings.database_url,
    pool_size=20,
    max_overflow=10,
    pool_pre_ping=True,
    pool_recycle=3600,
)
```

### 11.6.3 Redis 连接池

```python
Redis.from_url(settings.redis_url, max_connections=50, decode_responses=False)
```

---

## 11.7 多副本与扩展

### 11.7.1 水平扩展

```yaml
services:
  app:
    deploy:
      replicas: 3
    ports:
      - "8100-8102:8100"
```

### 11.7.2 切到 RedisStreamBus

```python
# composition/eos_app/container.py
from libs.messaging import RedisStreamBus

c.register_singleton(
    EventBus,
    RedisStreamBus(
        redis=c.resolve(Redis),
        stream_key_template="eos:events:{tenant_id}",  # 按租户分片
        consumer_group="eos-app",
        consumer_name=settings.instance_id,
    ),
)
```

> 多副本时 `InProcessBus` 不够用，必须切 `RedisStreamBus`。
> 按租户分片避免跨租户干扰。

---

## 11.8 监控与告警

详见 [09-可观测性 第 9.10 节](09-可观测性.md)。

补充运维关注的告警：

| 告警 | 阈值 |
|---|---|
| `app_instance_down` | `/livez` 失败超过 30s |
| `app_dependency_down` | `/healthz` 返回 503 |
| `high_error_rate` | 5xx > 5% 持续 5 分钟（按 tenant） |
| `agent_turn_p95_high` | P95 > 30s 持续 10 分钟 |
| `cost_spike` | 单租户 1h cost > $100 |
| `event_dlq_growing` | `eos:events:dlq` 长度 > 100 |
| `sse_active_streams_high` | 活跃 SSE > 5000 |
| `pg_pool_exhausted` | 连接池使用率 > 90% |
| `cross_tenant_query_blocked` | ORM 守卫触发 > 0 持续 1 分钟（**安全事件**） |
| `disk_usage_high` | 磁盘 > 85% |

---

## 11.9 备份与恢复

### 11.9.1 PG 备份

```bash
pg_dump "$EOS_DATABASE_URL" > backup-$(date +%Y%m%d).sql
```

### 11.9.2 Redis 备份

- 默认 RDB 快照
- 关键流（`eos:events:dlq`）需独立备份

### 11.9.3 对象存储备份

MinIO：`mc mirror` / 生产端 OSS：跨区复制。

### 11.9.4 恢复

```bash
psql "$EOS_DATABASE_URL" < backup-20260919.sql
make db-upgrade
```

---

## 11.10 安全清单

- [ ] `EOS_BAN_MOCK_TOKEN=1` 在所有非 demo 环境
- [ ] `EOS_JWT_SECRET` 从 secret manager 注入
- [ ] 所有 DB / Redis / 对象存储凭据从 secret manager
- [ ] HTTPS 由网关 / 反向代理终结
- [ ] API Key 在 header，不在 URL
- [ ] mTLS 仅在 staging / production（远期 SPIRE）
- [ ] 敏感字段（邮箱 / token / API Key / 凭据）在日志脱敏
- [ ] 附件 / 产物下载走签名 URL（5 分钟过期）
- [ ] 限流开启（`RateLimitMiddleware`）
- [ ] 多租户头必填（中间件强制）

---

## 11.11 故障排查速查

| 现象 | 检查 |
|---|---|
| `/healthz` 503 | PG / Redis / Vector 可达性 |
| `IDENTITY_MOCK_FORBIDDEN` | `EOS_BAN_MOCK_TOKEN=1` 且用了 `mock-*-token` |
| `TENANT_DENIED` / `WORKSPACE_DENIED` | JWT / API Key 中 tenant 与 header 不一致 |
| SSE 流式不增量 | 网关是否缓冲；显式 `X-Accel-Buffering: no` |
| 事件未触发订阅者 | EventBus 是否 `start()`；订阅是否在 `lifespan` 注册 |
| 跨租户查询被阻止 | ORM 守卫生效；租户上下文是否设置 |
| Alembic 报 "no such table" | `make db-upgrade`；CI 上 `migrations/env.py` 是否 import 了所有 ORM |
| 沙箱执行超时 | `EOS_SKILL_RUNTIME_URL` 是否可达；网络策略 |
| Tool 调 MCP 失败 | `EOS_TOOL_RUNTIME_URL` 可达；MCP server 端点正确 |
| 启动后内存暴涨 | uvicorn workers 是否过多；PG 池是否泄漏 |

---

## 11.12 升级与回滚

### 11.12.1 滚动升级

```bash
docker compose -f deploy/compose.yml pull app
docker compose -f deploy/compose.yml up -d --no-deps --scale app=4 app
docker compose -f deploy/compose.yml up -d --no-deps --scale app=1 app

# k8s
kubectl rollout restart deployment/eos-app
```

### 11.12.2 回滚

```bash
docker compose -f deploy/compose.yml pull app:v1.2.3
docker compose -f deploy/compose.yml up -d app
```

### 11.12.3 数据库迁移回滚

```bash
make db-downgrade
```

> `alembic downgrade` 必须与代码版本一致；先回代码再回迁移。

---

## 11.13 文档与运维手册

| 文档 | 用途 |
|---|---|
| 本方案 [12-实施计划.md](12-实施计划.md) | 阶段划分 |
| 本方案 [13-风险与验收.md](13-风险与验收.md) | 验收清单（含 §13.10 P10 上线验收） |
| [`../../prelaunch-checklist.md`](../../prelaunch-checklist.md) | P10 上线前 8 项 ops gate |
| [`../../runbook.md`](../../runbook.md) | P10 on-call 故障处置（5 类） |
| [`../../slo.md`](../../slo.md) | P10 SLI/SLO + 错误预算 |
| [`../../docs/adr/0016-gray-rollout.md`](../../docs/adr/0016-gray-rollout.md) | P10 上线策略决策 |
| [`../../infra/k8s/README.md`](../../infra/k8s/README.md) | k8s 部署说明 |
| [`../../deploy/README.md`](../../deploy/README.md) | docker-compose.prod 部署说明 |
| `docs/环境与数据模式.md` | `EOS_ENV` / 持久化 |
| `docs/审计-硬删PersistDelete覆盖.md` | 硬删语义 |
| ADR-014 作用域三层 | 多租户隔离 |

---

## 11.14 与原模板的关键差异

| 维度 | 原模板 | Enterprise-Agent-OS |
|---|---|---|
| 环境变量前缀 | `DE_` | `EOS_` |
| 对象存储 | 无 | MinIO（dev）+ OSS（生产） |
| 远程 Runtime | 2 个 | **3 个**（skill / tool / embedding） |
| Embedding Runtime | 无 | 独立服务，统一管理 API Key |
| Tool Runtime | 无 | 独立（统一 MCP / OpenAPI 适配） |
| 多租户故障排查 | 无 | TENANT_DENIED / 跨租户查询阻止 |
