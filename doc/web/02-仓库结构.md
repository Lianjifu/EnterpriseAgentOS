# 02 · 仓库结构（pnpm Workspace · FSD 6 层 · 8 BC 4 子目录）

> 物理骨架：enterprise-agent-os/web/
> packages/* （shared，6 个）+ web/src/{app,pages,widgets,features,entities}（FSD 切片）
> 8 BC × features/<bc>/{model,lib,api,ui}
> ESLint + import-linter 守 FSD 单向依赖
> 与后端 enterprise-agent-os/ 同仓库独立目录

> **基于 docs/web-refactor/02-仓库结构.md 模板**。
> 与原模板差异：BC 名对齐后端 modules/（agent_runtime / session / skill / tool / knowledge / memory / governance / identity）；新增 channel 前端独有 BC；新增 observability 横切 widgets；path alias 前缀 `@eos/*`（区分老 `@de/*`）。

---

## 2.1 顶层目录

```
enterprise-agent-os/
├── README.md                          # 项目入口（前后端总览）
├── pyproject.toml                     # uv workspace 根（仅管后端）
├── uv.lock
├── Makefile                           # make run-app / test / db-upgrade / web-dev
├── docker-compose.yml                 # 起后端 + PG / Redis / MinIO
├── docker-compose.dev.yml
├── .importlinter.ini                  # 后端 import-linter 守则
├── .github/workflows/                 # CI（前后端共用）
│   ├── backend-contract.yml
│   ├── backend-test.yml
│   ├── web-test.yml                   # 前端 CI
│   └── e2e-smoke.yml
├── docs/
│   ├── enterprise-agent-os/           # 后端方案（13 篇）
│   └── enterprise-agent-os/web/       # 本文档集（13 篇）
├── libs/                              # 后端共享内核
├── modules/                           # 后端 14 个能力模块
├── composition/                       # 后端组合根
├── gateways/
├── runtimes/
├── migrations/
├── tests/                             # 后端跨模块测试
├── deploy/
├── scripts/
└── web/                               # ← 前端根（pnpm workspace 成员）
    ├── package.json                   # "name": "@eos/web"
    ├── tsconfig.json
    ├── vite.config.ts
    ├── pnpm-workspace.yaml
    ├── .eslintrc.json                 # FSD 单向依赖守门
    ├── scripts/
    │   └── check-fsd-imports.sh       # 二次守门
    ├── packages/                      # shared（6 包）
    │   ├── ui/
    │   ├── api/
    │   ├── mock/
    │   ├── hooks/
    │   ├── types/
    │   └── utils/
    ├── e2e/                           # Playwright E2E
    ├── coverage/
    └── src/                           # FSD 6 层
        ├── app/
        ├── pages/
        ├── widgets/
        ├── features/                  # 8 BC + channel
        │   ├── agent_runtime/
        │   ├── session/
        │   ├── skill/
        │   ├── tool/
        │   ├── knowledge/
        │   ├── memory/
        │   ├── governance/
        │   ├── identity/
        │   └── channel/
        └── entities/                  # 跨 BC 共享
```

---

## 2.2 顶层职责

| 路径 | 职责 |
|---|---|
| `web/package.json` | web 端包；声明 6 个 workspace 依赖；scripts |
| `web/pnpm-workspace.yaml` | web 端 pnpm workspace（声明 `packages/*`） |
| `web/tsconfig.json` | TS strict；path alias `@eos/*` |
| `web/vite.config.ts` | 构建配置；chunk 切分；proxy |
| `web/.eslintrc.json` | FSD 单向依赖守门 |
| `web/scripts/check-fsd-imports.sh` | import-linter 二次守门 |
| `web/packages/{ui,api,mock,hooks,types,utils}` | shared 包（详见 [03-共享内核](03-共享内核.md)） |
| `web/src/app/` | Composition Root · DepsProvider · EventBus · Router · Observability |
| `web/src/pages/` | FSD pages 装配壳（≤ 50 行） |
| `web/src/widgets/` | 跨 BC UI · AppLayout · TopBar · GlobalSearch · Observability viewer |
| `web/src/features/<bc>/` | 8 BC × {model, lib, api, ui}（详见 [04-模块设计](04-模块设计.md)） |
| `web/src/entities/` | 跨 BC 共享（auth / user / tenant / workspace / i18n） |
| `web/e2e/` | Playwright 端到端 |

---

## 2.3 path alias（前端 7 个）

```jsonc
// web/tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@eos/web-ui/*": ["packages/ui/src/*"],
      "@eos/web-api/*": ["packages/api/src/*"],
      "@eos/web-mock/*": ["packages/mock/src/*"],
      "@eos/web-hooks/*": ["packages/hooks/src/*"],
      "@eos/web-types/*": ["packages/types/src/*"],
      "@eos/web-utils/*": ["packages/utils/src/*"],
      "@/*": ["src/*"]
    }
  }
}
```

> 区别于 `digital-employee-platform/frontend` 的 `@de/*`，本项目用 `@eos/*`。

---

## 2.4 FSD 6 层目录骨架

```
web/src/
├── app/                                # FSD app · Composition Root
│   ├── composition/
│   │   ├── adapters.ts                 # 装配所有 BC 的 Port 实现（Hex Composition Root）
│   │   ├── subscribers.ts              # 注册跨 BC Domain Event 订阅
│   │   └── DepsContext.ts              # React Context for AppDeps
│   ├── providers/
│   │   ├── DepsProvider.tsx            # 注入 AppDeps
│   │   ├── QueryProvider.tsx           # react-query 全局配置
│   │   ├── I18nProvider.tsx
│   │   └── ObservabilityProvider.tsx
│   ├── router/
│   │   ├── index.tsx                   # 路由表
│   │   ├── guards.tsx                  # Protected / Workspace / Role / Tenant / Governance
│   │   └── error-boundary.tsx
│   ├── observability/                  # trace / metric / log 上报（横切）
│   │   ├── trace.ts
│   │   ├── metric.ts
│   │   ├── log.ts
│   │   └── reporter.ts
│   ├── infrastructure/
│   │   ├── EventBus.ts                 # 全局 Domain Event 路由
│   │   └── broadcast.ts
│   ├── App.tsx
│   └── index.tsx                       # 入口（挂载 router + providers）
├── pages/                              # FSD pages · 路由装配壳 ≤ 50 行
│   ├── Home.tsx
│   ├── Login.tsx
│   ├── Agents.tsx
│   ├── Sessions.tsx
│   ├── Tools.tsx
│   ├── Skills.tsx
│   ├── Knowledge.tsx
│   ├── Memory.tsx
│   ├── Channels.tsx
│   ├── Governance.tsx
│   ├── Observability.tsx
│   ├── Settings.tsx
│   ├── Error.tsx
│   └── index.ts
├── widgets/                            # FSD widgets · 跨 BC UI
│   ├── ui/                             # L1 本应用通用
│   ├── app-layout/                     # L2 跨 BC · TopBar / Sidebar / TenantSwitcher
│   ├── global-search/                  # L2 跨 BC · CommandPalette
│   ├── auth-modal/                     # L2 跨 BC · AuthorizationModal
│   ├── error-boundary/                 # L2 横切
│   ├── observability/                  # L2 横切 · TraceViewer · CostDashboard
│   └── home/                           # 跨 BC 视图
├── features/                           # FSD features · 8 BC + channel
│   ├── agent_runtime/                  # Core · 对齐后端 modules/agent_runtime
│   ├── session/                        # Core · 对齐后端 modules/session
│   ├── skill/                          # Core · 对齐后端 modules/skill
│   ├── tool/                           # Core · 对齐后端 modules/tool
│   ├── knowledge/                      # Core · 对齐后端 modules/knowledge
│   ├── memory/                         # Supporting · 对齐后端 modules/memory
│   ├── governance/                     # Supporting · 对齐后端 modules/governance
│   ├── identity/                       # Generic · 对齐后端 modules/identity
│   └── channel/                        # Supporting · 前端独有（IM 运营配置）
└── entities/                           # FSD entities · 跨 BC 共享
    ├── auth/                           # token / user / login store
    ├── tenant/                         # 当前租户
    ├── workspace/                      # 当前工作区
    ├── user/                           # 用户基础信息
    └── i18n/                           # 国际化 store
```

---

## 2.5 8 BC × 4 子目录

每个 BC 标准结构：

```
features/<bc>/
├── model/                              # ← Hex 核心（不依赖 react / network / store）
│   ├── entities/                       # Aggregate / Entity（充血）
│   ├── value-objects/                  # VO（强类型 ID / 枚举）
│   ├── repositories/                   # Repository Port 接口
│   ├── services/                       # Domain Service
│   ├── events/                         # Domain Event 类型 + EventBus Port
│   └── acl/                            # Anti-Corruption Layer
├── lib/                                # ← Hex application（Use Case）
│   ├── use-cases/                      # 编排 + 调 Port
│   └── hooks/                          # React 包装（useDeps + react-query）
├── api/                                # ← Hex 适配器（inbound/outbound）
│   ├── adapters/
│   │   ├── HttpApiAdapter.ts           # 生产
│   │   ├── MockApiAdapter.ts           # dev/demo
│   │   └── LocalStorageAdapter.ts      # 可选
│   └── events/                         # EventBus 发布器适配器
└── ui/                                 # ← FSD widgets（BC 内部复合 UI 块）
    ├── components/                     # 视图组件
    ├── pages/                          # 视图页
    ├── store.ts                        # UI 局部状态（非领域）
    └── navigation.ts                   # 提供导航项给 AppLayout
```

---

## 2.6 FSD 单向依赖矩阵（强制）

| 层 ↓ → | app | pages | widgets | features | entities | shared |
|---|---|---|---|---|---|---|
| **app** | — | ✅ | ✅ | ✅ | ✅ | ✅ |
| **pages** | ❌ | — | ✅ | ✅ | ✅ | ✅ |
| **widgets** | ❌ | ❌ | — | ✅ | ✅ | ✅ |
| **features** | ❌ | ❌ | ❌ | —（同 BC） | ✅ | ✅ |
| **entities** | ❌ | ❌ | ❌ | ❌ | — | ✅ |
| **shared** | ❌ | ❌ | ❌ | ❌ | ❌ | — |

> **强约束**：每个层只可依赖其下方的层；同层仅可引用同 BC 或共享的 entities。

---

## 2.7 BC 内部依赖矩阵（FSD × Hex）

| ← 被依赖 \ → 依赖 | model | lib | api | ui |
|---|---|---|---|---|
| **model** | — | ❌ | ❌ | ❌ |
| **lib** | ✅（仅同 BC） | —（同 BC） | ❌ | ❌ |
| **api** | ✅（同 BC） | ✅（同 BC） | —（同 BC） | ❌ |
| **ui** | ✅（同 BC） | ✅（同 BC） | ✅（同 BC） | —（同 BC） |

> **model 永远不依赖 lib / api / ui**——这是 Hex 核心 + DDD 核心的根本。

---

## 2.8 ESLint FSD 单向依赖守门

```jsonc
// web/.eslintrc.json
{
  "extends": ["@feature-sliced/eslint-config"],
  "rules": {
    "import/no-internal-modules": [
      "error",
      {
        "allow": [
          "@eos/web-api", "@eos/web-mock", "@eos/web-utils",
          "@eos/web-ui", "@eos/web-hooks", "@eos/web-types"
        ]
      }
    ],
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["@/features/*/model", "@/features/*/model/*"],
            "message": "FSD 单向依赖：仅同 BC 的 lib/ui 可 import model；widget 不可 import 其他 BC 的 model"
          },
          {
            "group": ["@/features/*/lib"],
            "message": "FSD 单向依赖：features/lib 仅同 BC 内部与 ui 互用；widget 不可 import 其他 BC 的 lib"
          },
          {
            "group": ["@/pages", "@/pages/*"],
            "message": "pages 不可被 features 引用"
          },
          {
            "group": ["@/app", "@/app/*"],
            "message": "app 不可被 features 引用"
          },
          {
            "group": ["@/widgets", "@/widgets/*"],
            "message": "widgets 仅 features/ui 暴露；features 不可 import widgets"
          },
          {
            "group": ["@eos/web-mock", "@eos/web-mock/*"],
            "message": "业务代码禁止直接 import @eos/web-mock；mock 由 features/<bc>/api/adapters/MockApiAdapter 注入"
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": [
        "features/*/lib/use-cases/*.ts",
        "features/*/lib/hooks/*.ts",
        "features/*/api/adapters/*.ts",
        "src/app/composition/**",
        "src/app/router/**",
        "src/dev/**"
      ],
      "rules": { "no-restricted-imports": "off" }
    },
    {
      "files": ["src/pages/*.tsx"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "group": ["@/features/*/model", "@/features/*/lib"],
                "message": "FSD pages 不可依赖 model/lib；只引用 features/<bc>/ui 或 barrel"
              }
            ]
          }
        ]
      }
    }
  ]
}
```

---

## 2.9 import-linter 二次守门（CI 必跑）

```bash
# web/scripts/check-fsd-imports.sh
#!/usr/bin/env bash
set -e
ERRORS=0

# 1. pages 不能 import model/lib
if grep -rEn "from ['\"]@/features/[a-z]+/(model|lib)" src/pages/; then
  echo "❌ FSD violation: src/pages/* imports features/<bc>/{model,lib}"
  ERRORS=1
fi

# 2. features 不能 import widgets/pages/app
if grep -rEn "from ['\"]@/(widgets|pages|app)" src/features/; then
  echo "❌ FSD violation: features/* imports @/widgets or @/pages or @/app"
  ERRORS=1
fi

# 3. features 不能 import 其他 BC 的 model/lib
for bc in agent_runtime session skill tool knowledge memory governance identity channel; do
  OTHER_BC_DIRS=$(ls src/features/ | grep -v "^$bc$" | tr '\n' '|')
  if grep -rEn "from ['\"]@/features/(${OTHER_BC_DIRS%|})/(model|lib)" src/features/$bc/ 2>/dev/null; then
    echo "❌ FSD violation: features/$bc/ imports other BC's model/lib"
    ERRORS=1
  fi
done

# 4. 业务代码禁止 import @eos/web-mock
if grep -rEn "from ['\"]@eos/web-mock" src/features/ src/widgets/ src/pages/; then
  echo "❌ Hex violation: business code imports @eos/web-mock"
  ERRORS=1
fi

# 5. model 不应 import react / zustand / react-query
if grep -rEn "(from ['\"]react|from ['\"]zustand|from ['\"]@tanstack/react-query)" src/features/*/model/; then
  echo "❌ Hex violation: model/ imports react/zustand/react-query"
  ERRORS=1
fi

exit $ERRORS
```

```jsonc
// web/package.json
{
  "scripts": {
    "lint": "eslint .",
    "lint:fsd": "bash scripts/check-fsd-imports.sh",
    "ci:guard": "pnpm typecheck && pnpm lint && pnpm lint:fsd"
  }
}
```

---

## 2.10 与后端 enterprise-agent-os 的目录关系

```
enterprise-agent-os/
├── libs/                              # 后端共享内核（10 包）
├── modules/                           # 后端 14 个能力模块
│   ├── agent_runtime/                 # ← 对应前端 features/agent_runtime/
│   ├── session/                       # ← 对应前端 features/session/
│   ├── skill/                         # ← 对应前端 features/skill/
│   ├── tool/                          # ← 对应前端 features/tool/
│   ├── knowledge/                     # ← 对应前端 features/knowledge/
│   ├── memory/                        # ← 对应前端 features/memory/
│   ├── governance/                    # ← 对应前端 features/governance/
│   ├── identity/                      # ← 对应前端 features/identity/
│   ├── observability/                 # ← 对应前端 widgets/observability/ + app/observability/
│   └── ...
├── composition/
├── gateways/
├── runtimes/
├── migrations/
└── web/                               # ← 前端（pnpm workspace）
    └── src/features/<bc>/             # ← 与后端 modules/<bc>/ 一一对应
```

> **BC 名严格对齐**：`features/agent_runtime` 对应 `modules/agent_runtime`；前端 DTO 来自后端 OpenAPI Codegen；mock 镜像后端真实 handler。

---

## 2.11 子目录规模约束

| 路径 | 行数 / 文件数上限 |
|---|---|
| `src/pages/<X>.tsx` | ≤ 50 行 |
| `src/app/composition/adapters.ts` | ≤ 300 行（8 BC × 3 adapter） |
| `src/features/<bc>/model/` | 总 ≤ 1500 行（Entity / VO / Port / Event / ACL） |
| `src/features/<bc>/lib/use-cases/` | 每个 ≤ 200 行 |
| `src/features/<bc>/lib/hooks/` | 每个 ≤ 150 行 |
| `src/features/<bc>/api/adapters/<X>Adapter.ts` | ≤ 200 行 |
| `src/features/<bc>/ui/components/<X>.tsx` | ≤ 400 行 |
| `packages/api/src/client.ts` | ≤ 300 行（无 mock） |
| `packages/mock/src/handlers/<domain>.ts` | ≤ 500 行 |

---

## 2.12 命名规范

| 类型 | 命名 | 例 |
|---|---|---|
| BC（features/） | `<name>`（snake_case 与后端同） | `agent_runtime` `session` |
| Entity（model/entities/） | `PascalCase.ts` | `Agent` `Session` `Turn` |
| VO（model/value-objects/） | `PascalCase.ts` | `AgentId` `SessionStatus` |
| Repository Port（model/repositories/） | `<X>Repository.ts` | `SessionRepository` |
| Event Port（model/events/） | `<X>EventBus.ts` | `SessionEventBus` |
| Domain Service（model/services/） | `<X>Service.ts` | `AgentPlanService` |
| ACL（model/acl/） | `<other-bc>.ts`（映射源 BC） | `session.ts` |
| Use Case（lib/use-cases/） | `<Action>UseCase.ts` | `SendTurnUseCase` |
| Hook（lib/hooks/） | `use<Action>.ts` | `useSendTurn` |
| Adapter（api/adapters/） | `<Impl><X>Adapter.ts` | `HttpApiSessionAdapter` |
| EventPublisher（api/events/） | `<Impl><X>EventPublisher.ts` | `InMemorySessionEventPublisher` |
| 视图组件（ui/components/） | `PascalCase.tsx` | `CopilotPanel` |
| 视图页（ui/pages/） | `<X>Page.tsx` | `CopilotPage` |
| UI store（ui/store.ts） | `use<X>UIStore` | `useCopilotUIStore` |

---

## 2.13 barrel 守门

### 2.13.1 packages/* barrel

```ts
// packages/ui/src/index.tsx
export * from './primitives';
export * from './patterns';
```

```ts
// packages/api/src/index.ts
export { api } from './client';
export * from './errors';
```

### 2.13.2 features/<bc> barrel

```ts
// features/session/index.ts
export { SessionPage } from './ui/pages/SessionPage';
export { CopilotPanel } from './ui/components/CopilotPanel';
export { useSendTurn } from './lib/hooks/useSendTurn';
export { useCurrentSession } from './lib/hooks/useCurrentSession';
// model 不 export 到 barrel（仅同 BC 内部）
```

### 2.13.3 widgets barrel

```ts
// widgets/index.ts
export { AppLayout } from './app-layout';
export { GlobalSearch } from './global-search';
export { TraceViewer } from './observability';
// ...
```

---

## 2.14 不在本节范围

- 共享包内部结构 → 见 [03-共享内核](03-共享内核.md)
- BC 内部详细设计 → 见 [04-模块设计](04-模块设计.md)
- 跨 BC 协作 → 见 [05-跨模块协作](05-跨模块协作.md)
- 路由表 → 见 [07-组合根与启动](07-组合根与启动.md)