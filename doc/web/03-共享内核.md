# 03 · 共享内核（packages/* · FSD shared 层 · Hex SDK）

> 6 个 shared packages：`@eos/web-ui` `@eos/web-api` `@eos/web-mock` `@eos/web-types` `@eos/web-hooks` `@eos/web-utils`
> FSD shared 层定位；Hex SDK 定位（api 是 HttpApiAdapter 底层 SDK；mock 是 MockApiAdapter 底层 SDK）
> 业务代码通过 features/<bc>/api/adapters/ 间接使用；**不直接 import mock**
> DTO 类型与后端 OpenAPI 一一对应（TypeScript Codegen）

> **基于 docs/web-refactor/03-共享包.md 模板**。
> 与原模板差异：包名 `@eos/*`（区别于 `@de/*`）；DTO 来自后端 OpenAPI Codegen；mock 镜像后端真实 handler。

---

## 3.1 顶层结构

```
web/packages/
├── ui/                                 # L0 通用组件库
├── api/                                # HttpApiAdapter 底层 SDK
├── mock/                               # MockApiAdapter 底层 SDK
├── hooks/                              # 通用 hooks
├── types/                              # DTO 类型（与后端 OpenAPI 对齐）
└── utils/                              # 工具函数
```

| 包 | 定位 | FSD | Hex |
|---|---|---|---|
| `@eos/web-ui` | 通用组件 + Tailwind preset | shared（primitives） | UI 通用基础 |
| `@eos/web-api` | fetch client + interceptor + 后端 API 客户端 | shared（HTTP SDK） | HttpApiAdapter 底层 SDK |
| `@eos/web-mock` | mock handlers + fixtures（镜像后端） | shared（mock SDK） | MockApiAdapter 底层 SDK |
| `@eos/web-types` | DTO 类型（来自后端 OpenAPI Codegen） | shared（Shared Kernel） | 类型契约 |
| `@eos/web-hooks` | 通用 hooks（useDebounce / useThrottle） | shared | — |
| `@eos/web-utils` | cn / storage / featureFlags | shared | — |

---

## 3.2 `@eos/web-ui`（L0 通用组件库）

### 3.2.1 目录

```
packages/ui/src/
├── primitives/                         # 基础原子
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── Modal.tsx
│   ├── Drawer.tsx
│   ├── ConfirmDialog.tsx
│   ├── EmptyState.tsx
│   ├── Skeleton.tsx
│   ├── Toast.tsx
│   └── ...
├── patterns/                           # 业务模式（无业务语义）
│   ├── KpiCard.tsx
│   ├── PageHeader.tsx
│   ├── DataTable.tsx
│   ├── FormField.tsx
│   └── ...
├── icons/                              # 图标
│   └── ...
├── styles/
│   ├── tailwind-preset.ts              # Tailwind preset
│   └── globals.css
└── index.tsx                           # barrel
```

### 3.2.2 约束

- **不依赖** `@eos/web-api` `@eos/web-mock`
- **不依赖**任何 `features/` `entities/` `widgets/`
- **不依赖**业务类型（`Session` `Agent` 等）
- 可独立发版

---

## 3.3 `@eos/web-api`（HttpApiAdapter 底层 SDK）

### 3.3.1 目录

```
packages/api/src/
├── client.ts                           # ApiClient（fetch + interceptor）
├── errors.ts                           # ApiError + 业务 error code 映射
├── auth.ts                             # Bearer + Tenant/Workspace 头
├── ws-headers.ts                       # SSE 头
├── domains/                            # 后端 API 客户端（与后端 modules/ 一一对应）
│   ├── agent_runtime.ts
│   ├── session.ts
│   ├── skill.ts
│   ├── tool.ts
│   ├── knowledge.ts
│   ├── memory.ts
│   ├── governance.ts
│   ├── identity.ts
│   └── observability.ts
├── tracing/                            # trace_id 注入
│   └── header.ts
└── index.ts                            # barrel：导出 api client；无 mock 痕迹
```

### 3.3.2 ApiClient（精简 · 无 mock）

```ts
// packages/api/src/client.ts
export interface ApiClientConfig {
  baseUrl: string;
  getToken: () => string | null;
  getTenantId: () => string | null;
  getWorkspaceId: () => string | null;
}

export class ApiClient {
  constructor(private readonly config: ApiClientConfig) {}

  async request<T>(opts: {
    method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
    path: string;
    body?: unknown;
    query?: Record<string, string>;
    headers?: Record<string, string>;
    signal?: AbortSignal;
  }): Promise<T> {
    const url = new URL(this.config.baseUrl + opts.path);
    if (opts.query) Object.entries(opts.query).forEach(([k, v]) => url.searchParams.set(k, v));

    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
      ...opts.headers,
    };
    const token = this.config.getToken();
    if (token) headers['Authorization'] = `Bearer ${token}`;
    const tenantId = this.config.getTenantId();
    if (tenantId) headers['X-Tenant-Id'] = tenantId;
    const workspaceId = this.config.getWorkspaceId();
    if (workspaceId) headers['X-Workspace-Id'] = workspaceId;

    const traceId = getCurrentTraceId();
    if (traceId) headers['X-Trace-Id'] = traceId;

    const res = await fetch(url.toString(), {
      method: opts.method ?? 'GET',
      headers,
      body: opts.body ? JSON.stringify(opts.body) : undefined,
      signal: opts.signal,
    });
    if (!res.ok) {
      const body = await res.json().catch(() => ({}));
      throw mapStatusToError(res.status, body);
    }
    return this.parseResponse<T>(res);
  }

  /** SSE 流式 */
  async *stream(opts: {
    method: 'POST';
    path: string;
    body?: unknown;
    signal?: AbortSignal;
  }): AsyncIterable<unknown> {
    const res = await this.fetchStream(opts);
    const reader = res.body!.getReader();
    const decoder = new TextDecoder();
    let buffer = '';
    while (true) {
      const { value, done } = await reader.read();
      if (done) break;
      buffer += decoder.decode(value, { stream: true });
      const lines = buffer.split('\n\n');
      buffer = lines.pop() ?? '';
      for (const line of lines) {
        if (line.startsWith('data:')) {
          const payload = line.slice(5).trim();
          if (payload && payload !== '[DONE]') yield JSON.parse(payload);
        }
      }
    }
  }

  private async fetchStream(opts: { method: string; path: string; body?: unknown; signal?: AbortSignal }): Promise<Response> {
    const headers = this.buildHeaders(opts);
    return fetch(this.config.baseUrl + opts.path, {
      method: opts.method,
      headers: { ...headers, Accept: 'text/event-stream' },
      body: opts.body ? JSON.stringify(opts.body) : undefined,
      signal: opts.signal,
    });
  }
}
```

> **关键**：ApiClient **纯粹是 HttpApiAdapter 底层 SDK**；**没有 mock 调用**。

### 3.3.3 业务域客户端（与后端 modules/ 一一对应）

```ts
// packages/api/src/domains/session.ts
import type { SessionDTO, TurnChunkDTO } from '@eos/web-types';
import type { ApiClient } from '../client';

export function createSessionApi(client: ApiClient) {
  return {
    listSessions: (workspaceId: string) =>
      client.request<SessionDTO[]>({ path: `/v1/sessions`, query: { workspaceId } }),

    getSession: (id: string) =>
      client.request<SessionDTO>({ path: `/v1/sessions/${id}` }),

    createSession: (input: { agentVersionId: string; workspaceId: string }) =>
      client.request<SessionDTO>({ method: 'POST', path: `/v1/sessions`, body: input }),

    closeSession: (id: string) =>
      client.request<SessionDTO>({ method: 'DELETE', path: `/v1/sessions/${id}` }),

    streamTurn: function* (id: string, input: { content: string; traceId: string }) {
      return client.stream({
        method: 'POST',
        path: `/v1/sessions/${id}/turns`,
        body: input,
      }) as AsyncIterable<TurnChunkDTO>;
    },
  };
}

export type SessionApi = ReturnType<typeof createSessionApi>;
```

> **对应后端 modules/session/* RESTful + SSE 路由**（详见后端 `06-接口规范`）。

### 3.3.4 barrel

```ts
// packages/api/src/index.ts
import { ApiClient } from './client';
import { createAgentRuntimeApi } from './domains/agent_runtime';
import { createSessionApi } from './domains/session';
// ...

export const api = (() => {
  const client = new ApiClient({
    baseUrl: import.meta.env.VITE_API_BASE_URL,
    getToken: () => localStorage.getItem('token'),
    getTenantId: () => localStorage.getItem('tenantId'),
    getWorkspaceId: () => localStorage.getItem('workspaceId'),
  });
  return {
    client,
    agent_runtime: createAgentRuntimeApi(client),
    session: createSessionApi(client),
    skill: createSkillApi(client),
    tool: createToolApi(client),
    knowledge: createKnowledgeApi(client),
    memory: createMemoryApi(client),
    governance: createGovernanceApi(client),
    identity: createIdentityApi(client),
    observability: createObservabilityApi(client),
  };
})();
```

> 业务代码 `import { api } from '@eos/web-api'`；**无 mock 痕迹**。

---

## 3.4 `@eos/web-mock`（MockApiAdapter 底层 SDK · 镜像后端）

### 3.4.1 目录

```
packages/mock/src/
├── router.ts                           # method + path → handler 路由分发
├── handlers/                           # 与 api 同形态
│   ├── agent_runtime.ts
│   ├── session.ts
│   ├── skill.ts
│   ├── tool.ts
│   ├── knowledge.ts
│   ├── memory.ts
│   ├── governance.ts
│   ├── identity.ts
│   └── observability.ts
├── fixtures/                           # 静态种子（与后端 fixtures 镜像）
│   ├── agent_runtime.ts
│   ├── session.ts
│   └── ...
├── utils/
│   ├── sse.ts                          # mock SSE 流
│   └── seed.ts
└── index.ts                            # barrel：导出 mock 命名空间 + dispatchMockRequest
```

### 3.4.2 关键约束

- **不依赖** `@eos/web-api`（与 api 平行）
- **不依赖**任何 `features/` `entities/` `widgets/`
- 与 api 的方法签名 / 数据形态**完全一致**（共用 `@eos/web-types`）
- 镜像后端真实 handler（fixtures 由后端测试 fixtures 导出）

### 3.4.3 mock handler 示例（与 api 同形态）

```ts
// packages/mock/src/handlers/session.ts
import type { SessionDTO, TurnChunkDTO } from '@eos/web-types';
import { sessionFixtures } from '../fixtures/session';

export const sessionHandlers = {
  listSessions: async (workspaceId: string): Promise<SessionDTO[]> =>
    sessionFixtures.sessions.filter(s => s.workspaceId === workspaceId),

  getSession: async (id: string): Promise<SessionDTO | null> =>
    sessionFixtures.sessions.find(s => s.id === id) ?? null,

  createSession: async (input: { agentVersionId: string; workspaceId: string }): Promise<SessionDTO> => ({
    id: `ses_${Math.random().toString(36).slice(2, 10)}`,
    agentVersionId: input.agentVersionId,
    workspaceId: input.workspaceId,
    status: 'idle',
    createdAt: new Date().toISOString(),
    turns: [],
  }),

  closeSession: async (id: string): Promise<SessionDTO> => {
    const s = sessionFixtures.sessions.find(s => s.id === id);
    if (!s) throw new Error(`Session ${id} not found`);
    return { ...s, status: 'closed' };
  },

  streamTurn: async function* (id: string, input: { content: string }): AsyncIterable<TurnChunkDTO> {
    yield { type: 'message', delta: '正在思考...' };
    await delay(300);
    yield { type: 'tool_call', toolName: 'search', args: { q: input.content } };
    await delay(500);
    yield { type: 'message', delta: '已找到结果：' };
    yield { type: 'message', delta: input.content.split('').reverse().join('') };
    yield { type: 'done', usage: { input: 10, output: 20 } };
  },
};

function delay(ms: number) { return new Promise(r => setTimeout(r, ms)); }
```

### 3.4.4 路由分发（mock-bridge 兼容旧 fetch 路径）

```ts
// packages/mock/src/router.ts
export async function dispatchMockRequest(method: string, path: string, body?: unknown): Promise<unknown> {
  // 与后端真实路由路径完全一致
  if (method === 'GET' && path.startsWith('/v1/sessions')) return sessionHandlers.listSessions(/* extract */);
  if (method === 'POST' && path === '/v1/sessions') return sessionHandlers.createSession(body);
  // ...
}
```

### 3.4.5 barrel

```ts
// packages/mock/src/index.ts
import { agentRuntimeHandlers } from './handlers/agent_runtime';
import { sessionHandlers } from './handlers/session';
// ...

export const mock = {
  agent_runtime: agentRuntimeHandlers,
  session: sessionHandlers,
  skill: skillHandlers,
  tool: toolHandlers,
  knowledge: knowledgeHandlers,
  memory: memoryHandlers,
  governance: governanceHandlers,
  identity: identityHandlers,
  observability: observabilityHandlers,
};

export { dispatchMockRequest } from './router';
```

---

## 3.5 `@eos/web-types`（DTO 类型 · Shared Kernel）

### 3.5.1 目录

```
packages/types/src/
├── agent.ts                            # AgentDTO / AgentVersionDTO
├── session.ts                          # SessionDTO / TurnDTO / TurnChunkDTO / MessageDTO
├── skill.ts                            # SkillPackageDTO / SkillInvocationDTO / RunTokenDTO
├── tool.ts                             # ToolDTO / ToolCallDTO / ToolProtocol
├── knowledge.ts                        # KnowledgePackageDTO / AssetDTO / RetrieverDTO
├── memory.ts                           # MemoryEntryDTO / RecallResultDTO
├── governance.ts                       # PolicyRuleDTO / ApprovalDTO / AuditLogDTO
├── identity.ts                         # TenantDTO / WorkspaceDTO / UserDTO / RoleDTO
├── channel.ts                          # ChannelDTO / ChannelDeliveryDTO
├── observability.ts                    # TraceDTO / MetricDTO / LogDTO / CostDTO
├── common.ts                           # Page / Pagination / ErrorCode / Brand<T,K>
├── brand-ids.ts                        # 强类型 ID 工厂（运行时校验）
└── index.ts                            # barrel
```

### 3.5.2 与后端 OpenAPI 对齐

```yaml
# 与后端 openapi.yaml 1:1 对应
# 后端：modules/session/openapi.yaml
# 前端：packages/types/src/session.ts（TypeScript Codegen 生成）
```

```ts
// packages/types/src/session.ts（来自后端 Codegen）
export interface SessionDTO {
  id: string;
  agent_version_id: string;
  workspace_id: string;
  status: 'idle' | 'running' | 'closed';
  created_at: string;
  turns: TurnDTO[];
}

export interface TurnDTO {
  id: number;
  role: 'user' | 'assistant' | 'tool';
  content: string;
  tool_calls?: ToolCallDTO[];
  created_at: string;
}

export interface TurnChunkDTO {
  type: 'message' | 'tool_call' | 'tool_result' | 'done' | 'error';
  delta?: string;
  tool_name?: string;
  args?: Record<string, unknown>;
  result?: unknown;
  usage?: { input: number; output: number };
}
```

> **Codegen 命令**：`pnpm codegen`（从后端 `openapi.yaml` 生成 → `packages/types/src/`）

### 3.5.3 品牌 ID（运行时校验）

```ts
// packages/types/src/brand-ids.ts
import { z } from 'zod';

export const AgentIdSchema = z.string().regex(/^agt_[a-z0-9]{8,}$/);
export const SessionIdSchema = z.string().regex(/^ses_[a-z0-9]{8,}$/);
export const SkillPackageIdSchema = z.string().regex(/^skp_[a-z0-9]{8,}$/);
// ...

export type AgentId = z.infer<typeof AgentIdSchema>;
export type SessionId = z.infer<typeof SessionIdSchema>;

export function toAgentId(value: string): AgentId {
  return AgentIdSchema.parse(value);
}
```

> **DTO 用 string + 品牌类型**（type-level）；VO 在 BC `model/value-objects/` 用 class + 运行时校验。

---

## 3.6 `@eos/web-hooks`（通用 hooks）

```
packages/hooks/src/
├── use-debounce.ts
├── use-throttle.ts
├── use-media-query.ts
├── use-online.ts
├── use-trace-id.ts                     # trace_id 管理
├── use-feature-flag.ts
└── index.ts
```

约束：不依赖业务类型；不依赖 features。

---

## 3.7 `@eos/web-utils`（工具函数）

```
packages/utils/src/
├── cn.ts                               # className 合并
├── storage.ts                          # localStorage 包装（带 schema 校验）
├── feature-flags.ts
├── format/
│   ├── date.ts
│   ├── number.ts
│   └── duration.ts
├── trace.ts                            # trace_id 生成 + 上下文
└── index.ts
```

约束：纯函数；不依赖 react / network。

---

## 3.8 包之间的边界

| 包 | 可以依赖 | 不可以依赖 |
|---|---|---|
| `@eos/web-ui` | `@eos/web-utils` | `@eos/web-api` `@eos/web-mock` `features/` |
| `@eos/web-api` | `@eos/web-types` `@eos/web-utils` | `@eos/web-mock` `features/` |
| `@eos/web-mock` | `@eos/web-types` `@eos/web-utils` | `@eos/web-api` `features/` |
| `@eos/web-types` | （无） | 其他包 |
| `@eos/web-hooks` | `@eos/web-utils` `@eos/web-types` | `@eos/web-api` `@eos/web-mock` `features/` |
| `@eos/web-utils` | （无） | 其他包 |

> **api 与 mock 完全平行**：谁也不依赖谁；都依赖 `@eos/web-types`。

---

## 3.9 包安装与依赖

```jsonc
// packages/ui/package.json
{
  "name": "@eos/web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.tsx",
  "peerDependencies": { "react": "^18.0.0", "react-dom": "^18.0.0" },
  "dependencies": {
    "@eos/web-utils": "workspace:*",
    "@radix-ui/react-dialog": "^1.0.0",
    "clsx": "^2.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "tailwindcss": "^3.4.0"
  }
}
```

```jsonc
// packages/api/package.json
{
  "name": "@eos/web-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "dependencies": {
    "@eos/web-types": "workspace:*",
    "@eos/web-utils": "workspace:*"
  }
}
```

```jsonc
// packages/mock/package.json
{
  "name": "@eos/web-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "dependencies": {
    "@eos/web-types": "workspace:*",
    "@eos/web-utils": "workspace:*",
    "msw": "^2.0.0"
  }
}
```

```jsonc
// packages/types/package.json
{
  "name": "@eos/web-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "dependencies": {
    "zod": "^3.22.0"
  }
}
```

---

## 3.10 包与 BC 的关系

```
features/<bc>/
├── api/adapters/HttpApiAdapter.ts      → import { api } from '@eos/web-api'
├── api/adapters/MockApiAdapter.ts      → import { mock } from '@eos/web-mock'
├── api/adapters/LocalStorageAdapter.ts → import { storage } from '@eos/web-utils'
├── model/entities/<X>.ts               → import { AgentIdSchema } from '@eos/web-types'
└── ui/components/<X>.tsx               → import { Button } from '@eos/web-ui'
```

> **BC 的适配器 import shared 包的 SDK**；BC 不直接 import 其它 BC 的代码。

---

## 3.11 DTO 边界（DTO ↔ Entity）

### 3.11.1 DTO 在 shared 层

```ts
// packages/types/src/session.ts
export interface SessionDTO {
  id: string;
  agent_version_id: string;
  workspace_id: string;
  status: 'idle' | 'running' | 'closed';
  created_at: string;
  turns: TurnDTO[];
}
```

> **DTO 是传输格式**（snake_case + 字段名扁平）；来自后端 OpenAPI。

### 3.11.2 Entity 在 BC model 层

```ts
// features/session/model/entities/Session.ts
import type { SessionDTO } from '@eos/web-types';
import { SessionId } from '../value-objects/SessionId';
import { Turn } from './Turn';

export class Session {
  private _status: SessionStatus = 'idle';
  private readonly _turns: Turn[] = [];

  constructor(private readonly _id: SessionId) {}

  get id(): SessionId { return this._id; }
  get status(): SessionStatus { return this._status; }
  get turns(): readonly Turn[] { return this._turns; }

  appendTurn(turn: Turn): void {
    this._status = 'running';
    this._turns.push(turn);
  }

  close(): void {
    this.assertOpen();
    this._status = 'closed';
  }

  private assertOpen(): void {
    if (this._status === 'closed') throw new Error(`Session ${this._id.value} is closed`);
  }

  /** 适配器层调用 */
  static fromDTO(dto: SessionDTO): Session {
    const s = new Session(new SessionId(dto.id));
    if (dto.status === 'closed') s.close();
    dto.turns.forEach(t => s.appendTurn(Turn.fromDTO(t)));
    return s;
  }

  toDTO(): SessionDTO {
    return {
      id: this._id.value,
      agent_version_id: '',
      workspace_id: '',
      status: this._status,
      created_at: '',
      turns: this._turns.map(t => t.toDTO()),
    };
  }
}
```

> **DTO → Entity 在 adapter 边界**；Entity 含业务规则；DTO 是传输格式。

---

## 3.12 mock 与 api 形态对齐表

| api 客户端方法 | mock handler 方法 | Hex 视角 |
|---|---|---|
| `api.agent_runtime.listAgents(workspaceId)` | `mock.agent_runtime.listAgents(workspaceId)` | 都被 `AgentRepository.list()` 包装 |
| `api.session.getSession(id)` | `mock.session.getSession(id)` | 都被 `SessionRepository.findById()` 包装 |
| `api.session.createSession(input)` | `mock.session.createSession(input)` | 都被 `SessionRepository.save()` 包装 |
| `api.session.streamTurn(id, input)` | `mock.session.streamTurn(id, input)` | 都被 `SessionStreamPort.stream()` 包装 |
| `api.tool.invoke(toolName, args)` | `mock.tool.invoke(toolName, args)` | 都被 `ToolInvocationPort.invoke()` 包装 |

> **约定**：api 与 mock 的方法签名 / 数据形态完全一致（共用 `@eos/web-types`），但实现路径独立。

---

## 3.13 包的大小约束

| 包 | 单文件上限 | 总规模上限 |
|---|---|---|
| `@eos/web-ui` | 200 行 | — |
| `@eos/web-api` | 300 行（client） | ≤ 5000 行 |
| `@eos/web-mock` | 500 行（handler） | ≤ 8000 行 |
| `@eos/web-types` | — | ≤ 3000 行 |
| `@eos/web-hooks` | 100 行 | ≤ 1000 行 |
| `@eos/web-utils` | 200 行 | ≤ 1500 行 |

---

## 3.14 不在本节范围

- 6 个包的具体实现细节
- Tailwind preset 配置（远期）
- MSW 集成（mock server 化）
- Codegen 流程（远期 P5）