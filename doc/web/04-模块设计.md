# 04 · 模块设计（8 BC × FSD × Hex · 对齐后端 modules/）

> 8 个 Bounded Context 与后端 modules/ 1:1 对齐（agent_runtime / session / skill / tool / knowledge / memory / governance / identity）+ channel 前端独有；
> 每个 BC 内 `model/lib/api/ui` 四子目录；
> FSD × DDD × Hex 三视角融合；
> 充血 Entity · Repository Port · Use Case 编排 · 三类适配器（HttpApi/Mock/LocalStorage）；
> 与后端 `docs/enterprise-agent-os/04-模块设计.md` 平行阅读。

> **基于 docs/web-refactor/04-BC模块设计.md 模板**。
> 与原模板差异：BC 名对齐后端（agent_runtime / session / skill / tool / knowledge / memory / governance / identity）；新增 channel；新增 observability 横切 widgets；DTO 来自后端 OpenAPI Codegen；mock 镜像后端真实 handler。

---

## 4.1 8 BC 总览（对齐后端）

| BC | FSD 落点 | 后端 modules/ | 类型 | 业务语言 | 起步范围 |
|---|---|---|---|---|---|
| **`agent_runtime`** | `features/agent_runtime/` | `modules/agent_runtime/` | Core | Agent / AgentVersion / Plan / Response | W2 起步 |
| **`session`** | `features/session/` | `modules/session/` | Core | Session / Turn / Message / Stream | W2 起步 |
| **`tool`** | `features/tool/` | `modules/tool/` | Core | Tool / ToolCall / ToolProtocol | W2 起步 |
| **`skill`** | `features/skill/` | `modules/skill/` | Core | SkillPackage / SkillInvocation / RunToken | W2 起步 |
| **`knowledge`** | `features/knowledge/` | `modules/knowledge/` | Core | KnowledgePackage / Asset / Retriever | W3 增量 |
| **`memory`** | `features/memory/` | `modules/memory/` | Supporting | MemoryEntry / Recall | W3 增量 |
| **`governance`** | `features/governance/` | `modules/governance/` | Supporting | PolicyRule / Approval / AuditLog | W3 增量 |
| **`identity`** | `features/identity/` | `modules/identity/` | Generic | Tenant / Workspace / User / Role | W1 起步 |
| **`channel`**（前端独有） | `features/channel/` | （后端无对应） | Supporting | Channel / ChannelDelivery | W3 增量 |
| **`observability`**（横切） | `web/src/widgets/observability/` + `app/observability/` | `modules/observability/` | Supporting | TraceViewer / CostDashboard | W3 增量 |

---

## 4.2 BC 标准结构（FSD × Hex）

```
features/<bc>/
├── model/                              # ← Hex 核心（不依赖 react / network / store）
│   ├── entities/                       # Aggregate / Entity（充血）
│   ├── value-objects/                  # VO（强类型 ID / 枚举）
│   ├── repositories/                   # Port 接口
│   ├── services/                       # Domain Service
│   ├── events/                         # Domain Event 类型 + EventBus Port
│   └── acl/                            # Anti-Corruption Layer
├── lib/                                # ← Hex application（Use Case）
│   ├── use-cases/                      # 编排 + 调 Port
│   └── hooks/                          # React 包装（useDeps + react-query）
├── api/                                # ← Hex 适配器（inbound/outbound）
│   ├── adapters/
│   │   ├── HttpApiAdapter.ts
│   │   ├── MockApiAdapter.ts
│   │   └── LocalStorageAdapter.ts
│   └── events/                         # EventBus 发布器适配器
└── ui/                                 # ← FSD widgets（BC 内部）
    ├── components/
    ├── pages/
    ├── store.ts                        # UI 局部状态
    └── navigation.ts                   # 提供导航项
```

---

## 4.3 BC × FSD × Hex 依赖矩阵

| FSD 子层 \ Hex | model | lib | api | ui |
|---|---|---|---|---|
| **model** | — | ❌ | ❌ | ❌ |
| **lib** | ✅（同 BC） | — | ❌ | ❌ |
| **api** | ✅（同 BC） | ✅（同 BC） | — | ❌ |
| **ui** | ✅（同 BC） | ✅（同 BC） | ✅（同 BC） | — |

---

## 4.4 `session` BC（核心 · W2 起步）

> **对齐后端 `modules/session/`**：管理 Session / Turn / Message / Stream

### 4.4.1 model 层

```ts
// features/session/model/entities/Session.ts
import type { SessionDTO, TurnChunkDTO } from '@eos/web-types';
import { SessionId } from '../value-objects/SessionId';
import { SessionStatus } from '../value-objects/SessionStatus';
import { Turn } from './Turn';

export class Session {
  private _status: SessionStatus = 'idle';
  private readonly _turns: Turn[] = [];

  constructor(
    private readonly _id: SessionId,
    private readonly _agentVersionId: string,
    private readonly _workspaceId: string,
  ) {}

  get id(): SessionId { return this._id; }
  get agentVersionId(): string { return this._agentVersionId; }
  get workspaceId(): string { return this._workspaceId; }
  get status(): SessionStatus { return this._status; }
  get turns(): readonly Turn[] { return this._turns; }

  /** 业务方法：append turn（含不变量） */
  appendTurn(turn: Turn): void {
    this.assertOpen();
    this._status = 'running';
    this._turns.push(turn);
  }

  /** 业务方法：append chunk 到当前 turn */
  appendChunk(turnId: number, chunk: TurnChunkDTO): void {
    this.assertOpen();
    const turn = this._turns.find(t => t.id === turnId);
    if (!turn) throw new Error(`Turn ${turnId} not found`);
    turn.append(chunk);
  }

  close(): void {
    this.assertOpen();
    this._status = 'closed';
  }

  private assertOpen(): void {
    if (this._status === 'closed') throw new Error(`Session ${this._id.value} is closed`);
  }

  static fromDTO(dto: SessionDTO): Session {
    const s = new Session(
      new SessionId(dto.id),
      dto.agent_version_id,
      dto.workspace_id,
    );
    if (dto.status === 'closed') s.close();
    dto.turns.forEach(t => s.appendTurn(Turn.fromDTO(t)));
    return s;
  }

  toDTO(): SessionDTO {
    return {
      id: this._id.value,
      agent_version_id: this._agentVersionId,
      workspace_id: this._workspaceId,
      status: this._status,
      created_at: '',
      turns: this._turns.map(t => t.toDTO()),
    };
  }
}
```

```ts
// features/session/model/value-objects/SessionId.ts
import { SessionIdSchema } from '@eos/web-types';

export class SessionId {
  constructor(public readonly value: string) {
    const result = SessionIdSchema.safeParse(value);
    if (!result.success) throw new Error(`Invalid SessionId: ${value}`);
  }
  equals(other: SessionId): boolean { return this.value === other.value; }
}
```

```ts
// features/session/model/value-objects/SessionStatus.ts
export type SessionStatus = 'idle' | 'running' | 'closed';
```

```ts
// features/session/model/repositories/SessionRepository.ts（Port）
import type { Session } from '../entities/Session';
import type { SessionId } from '../value-objects/SessionId';

export interface SessionRepository {
  findById(id: SessionId): Promise<Session | null>;
  list(workspaceId: string): Promise<readonly Session[]>;
  save(session: Session): Promise<void>;
  create(input: { agentVersionId: string; workspaceId: string }): Promise<Session>;
}

/** 流式 Port */
export interface SessionStreamPort {
  stream(id: SessionId, input: { content: string; traceId: string }): AsyncIterable<unknown>;
}
```

```ts
// features/session/model/events/SessionEvents.ts
import type { SessionId } from '../value-objects/SessionId';

export interface SessionCreatedEvent {
  readonly type: 'session.created';
  readonly occurredAt: string;
  readonly sessionId: SessionId;
  readonly agentVersionId: string;
  readonly workspaceId: string;
}

export interface SessionTurnStartedEvent {
  readonly type: 'session.turn.started';
  readonly occurredAt: string;
  readonly sessionId: SessionId;
  readonly turnId: number;
}

export interface SessionTurnCompletedEvent {
  readonly type: 'session.turn.completed';
  readonly occurredAt: string;
  readonly sessionId: SessionId;
  readonly turnId: number;
  readonly usage: { input: number; output: number };
}

export interface SessionClosedEvent {
  readonly type: 'session.closed';
  readonly occurredAt: string;
  readonly sessionId: SessionId;
}

export type SessionEvent =
  | SessionCreatedEvent
  | SessionTurnStartedEvent
  | SessionTurnCompletedEvent
  | SessionClosedEvent;
```

```ts
// features/session/model/events/SessionEventBus.ts（Hex outbound Port）
import type { SessionEvent } from './SessionEvents';

export interface SessionEventBus {
  publish(event: SessionEvent): Promise<void>;
}
```

```ts
// features/session/model/acl/agent_runtime.ts（ACL）
import type { AgentVersionSummaryDTO } from '@eos/web-types';

/** agent_runtime → session：把 AgentVersionDTO 翻译为 Session 创建输入 */
export function mapAgentVersionToSessionInput(
  agentVersion: AgentVersionSummaryDTO,
  workspaceId: string,
): { agentVersionId: string; workspaceId: string } {
  return { agentVersionId: agentVersion.id, workspaceId };
}
```

### 4.4.2 lib 层（Use Case）

```ts
// features/session/lib/use-cases/SendTurnUseCase.ts
import type { SessionRepository, SessionStreamPort } from '../../model/repositories/SessionRepository';
import type { SessionEventBus } from '../../model/events/SessionEventBus';
import type { SessionId } from '../../model/value-objects/SessionId';

export interface SendTurnDeps {
  sessionRepo: SessionRepository;
  streamPort: SessionStreamPort;
  eventBus: SessionEventBus;
}

export class SendTurnUseCase {
  constructor(private readonly deps: SendTurnDeps) {}

  /** 流式执行 turn */
  async *execute(sessionId: SessionId, content: string): AsyncIterable<unknown> {
    await this.deps.eventBus.publish({
      type: 'session.turn.started',
      occurredAt: new Date().toISOString(),
      sessionId,
      turnId: Date.now(),
    });

    let turnId = 0;
    let usage: { input: number; output: number } | null = null;
    for await (const chunk of this.deps.streamPort.stream(sessionId, {
      content,
      traceId: crypto.randomUUID(),
    })) {
      yield chunk;
      if (typeof chunk === 'object' && chunk !== null) {
        const c = chunk as { type?: string; usage?: { input: number; output: number } };
        if (c.type === 'message' && c.usage) usage = c.usage;
        if (c.type === 'done' && c.usage) usage = c.usage;
      }
      turnId++;
    }

    await this.deps.eventBus.publish({
      type: 'session.turn.completed',
      occurredAt: new Date().toISOString(),
      sessionId,
      turnId,
      usage: usage ?? { input: 0, output: 0 },
    });
  }
}
```

```ts
// features/session/lib/hooks/useSendTurn.ts
import { useCallback } from 'react';
import { useMutation } from '@tanstack/react-query';
import { useDeps } from '@/app/composition/useDeps';
import { SendTurnUseCase } from '../use-cases/SendTurnUseCase';
import { SessionId } from '../../model/value-objects/SessionId';

export function useSendTurn() {
  const { session } = useDeps();
  const useCase = new SendTurnUseCase({
    sessionRepo: session.sessionRepo,
    streamPort: session.streamPort,
    eventBus: session.eventBus,
  });

  return useMutation({
    mutationFn: async (vars: { sessionId: string; content: string }) => {
      const chunks: unknown[] = [];
      for await (const chunk of useCase.execute(new SessionId(vars.sessionId), vars.content)) {
        chunks.push(chunk);
      }
      return chunks;
    },
  });
}
```

### 4.4.3 api 层（Hex 适配器）

```ts
// features/session/api/adapters/HttpApiSessionAdapter.ts（生产）
import type { SessionRepository, SessionStreamPort } from '../../model/repositories/SessionRepository';
import type { Session } from '../../model/entities/Session';
import type { SessionId } from '../../model/value-objects/SessionId';
import { Session as SessionEntity } from '../../model/entities/Session';
import { api } from '@eos/web-api';

export class HttpApiSessionAdapter implements SessionRepository, SessionStreamPort {
  async findById(id: SessionId): Promise<Session | null> {
    try {
      const dto = await api.session.getSession(id.value);
      return SessionEntity.fromDTO(dto);
    } catch (e) {
      if ((e as { status?: number }).status === 404) return null;
      throw e;
    }
  }

  async list(workspaceId: string): Promise<readonly Session[]> {
    const dtos = await api.session.listSessions(workspaceId);
    return dtos.map(SessionEntity.fromDTO);
  }

  async save(session: Session): Promise<void> {
    await api.session.updateSession(session.id.value, session.toDTO());
  }

  async create(input: { agentVersionId: string; workspaceId: string }): Promise<Session> {
    const dto = await api.session.createSession(input);
    return SessionEntity.fromDTO(dto);
  }

  async *stream(id: SessionId, input: { content: string; traceId: string }): AsyncIterable<unknown> {
    yield* api.session.streamTurn(id.value, input);
  }
}
```

```ts
// features/session/api/adapters/MockApiSessionAdapter.ts（dev/demo）
import type { SessionRepository, SessionStreamPort } from '../../model/repositories/SessionRepository';
import type { Session } from '../../model/entities/Session';
import type { SessionId } from '../../model/value-objects/SessionId';
import { Session as SessionEntity } from '../../model/entities/Session';
import { mock } from '@eos/web-mock';

export class MockApiSessionAdapter implements SessionRepository, SessionStreamPort {
  async findById(id: SessionId): Promise<Session | null> {
    const dto = await mock.session.getSession(id.value);
    return dto ? SessionEntity.fromDTO(dto) : null;
  }
  async list(workspaceId: string): Promise<readonly Session[]> {
    const dtos = await mock.session.listSessions(workspaceId);
    return dtos.map(SessionEntity.fromDTO);
  }
  async save(session: Session): Promise<void> {
    await mock.session.updateSession(session.id.value, session.toDTO());
  }
  async create(input: { agentVersionId: string; workspaceId: string }): Promise<Session> {
    const dto = await mock.session.createSession(input);
    return SessionEntity.fromDTO(dto);
  }
  async *stream(id: SessionId, input: { content: string; traceId: string }): AsyncIterable<unknown> {
    yield* mock.session.streamTurn(id.value, { content: input.content });
  }
}
```

```ts
// features/session/api/adapters/LocalStorageSessionAdapter.ts（可选 · 离线）
import type { SessionRepository } from '../../model/repositories/SessionRepository';
import type { Session } from '../../model/entities/Session';
import type { SessionId } from '../../model/value-objects/SessionId';
import { Session as SessionEntity } from '../../model/entities/Session';
import { storage } from '@eos/web-utils/storage';
import type { SessionDTO } from '@eos/web-types';

export class LocalStorageSessionAdapter implements SessionRepository {
  async findById(id: SessionId): Promise<Session | null> {
    const dto = storage.get<SessionDTO>(`session:${id.value}`);
    return dto ? SessionEntity.fromDTO(dto) : null;
  }
  async list(_workspaceId: string): Promise<readonly Session[]> {
    return storage.list<SessionDTO>('session:').map(SessionEntity.fromDTO);
  }
  async save(session: Session): Promise<void> {
    storage.set(`session:${session.id.value}`, session.toDTO());
  }
  async create(input: { agentVersionId: string; workspaceId: string }): Promise<Session> {
    const dto: SessionDTO = {
      id: `ses_${Math.random().toString(36).slice(2, 10)}`,
      agent_version_id: input.agentVersionId,
      workspace_id: input.workspaceId,
      status: 'idle',
      created_at: new Date().toISOString(),
      turns: [],
    };
    storage.set(`session:${dto.id}`, dto);
    return SessionEntity.fromDTO(dto);
  }
}
```

```ts
// features/session/api/events/InMemorySessionEventPublisher.ts
import { eventBus } from '@/app/infrastructure/EventBus';
import type { SessionEvent } from '../../model/events/SessionEvents';
import type { SessionEventBus } from '../../model/events/SessionEventBus';

export class InMemorySessionEventPublisher implements SessionEventBus {
  async publish(event: SessionEvent): Promise<void> {
    void eventBus.publish(event);  // fire-and-forget
  }
}
```

### 4.4.4 ui 层

```tsx
// features/session/ui/components/CopilotPanel.tsx
import type { Session } from '../../model/entities/Session';
import type { TurnChunkDTO } from '@eos/web-types';
import { TurnNarrative } from './TurnNarrative';
import { Composer } from './Composer';

export interface CopilotPanelProps {
  session: Session | null;
  streaming: boolean;
  chunks: readonly TurnChunkDTO[];
  onSend: (input: string) => void;
  onAbort?: () => void;
}

export function CopilotPanel({ session, streaming, chunks, onSend, onAbort }: CopilotPanelProps) {
  return (
    <div className="flex flex-col h-full">
      <TurnNarrative turns={session?.turns ?? []} chunks={chunks} />
      <Composer onSend={onSend} disabled={streaming} onAbort={onAbort} />
    </div>
  );
}
```

```tsx
// features/session/ui/pages/SessionPage.tsx
import { useState } from 'react';
import { useCurrentSession } from '../../lib/hooks/useCurrentSession';
import { useSendTurn } from '../../lib/hooks/useSendTurn';
import { CopilotPanel } from '../components/CopilotPanel';
import type { TurnChunkDTO } from '@eos/web-types';

export function SessionPage({ sessionId }: { sessionId: string }) {
  const { data: session } = useCurrentSession(sessionId);
  const sendTurn = useSendTurn();
  const [chunks, setChunks] = useState<TurnChunkDTO[]>([]);

  const onSend = (input: string) => {
    setChunks([]);
    sendTurn.mutate(
      { sessionId, content: input },
      {
        onSuccess: (allChunks) => setChunks(allChunks as TurnChunkDTO[]),
      },
    );
  };

  return (
    <CopilotPanel
      session={session ?? null}
      streaming={sendTurn.isPending}
      chunks={chunks}
      onSend={onSend}
    />
  );
}
```

### 4.4.5 barrel

```ts
// features/session/index.ts
export { SessionPage } from './ui/pages/SessionPage';
export { CopilotPanel } from './ui/components/CopilotPanel';
export { useSendTurn } from './lib/hooks/useSendTurn';
export { useCurrentSession } from './lib/hooks/useCurrentSession';
```

---

## 4.5 `agent_runtime` BC（核心 · W2 起步）

> **对齐后端 `modules/agent_runtime/`**：Agent / AgentVersion / Plan

### 4.5.1 关键 Entity

```ts
// features/agent_runtime/model/entities/Agent.ts
export class Agent {
  constructor(
    private readonly _id: AgentId,
    private _name: string,
    private _description: string,
    private readonly _tenantId: TenantId,
    private readonly _versions: AgentVersion[] = [],
  ) {}

  get id(): AgentId { return this._id; }
  get name(): string { return this._name; }
  get description(): string { return this._description; }
  get tenantId(): TenantId { return this._tenantId; }
  get versions(): readonly AgentVersion[] { return this._versions; }

  /** 业务方法：发布新版本（不变量：版本号自增） */
  publishVersion(input: { config: AgentConfig; prompt: string }): AgentVersion {
    const next = (this._versions.at(-1)?.versionNumber ?? 0) + 1;
    const v = new AgentVersion(
      new AgentVersionId(`agv_${this._id.value}_${next}`),
      this._id,
      next,
      input.config,
      input.prompt,
    );
    this._versions.push(v);
    return v;
  }

  static fromDTO(dto: AgentDTO): Agent { /* ... */ }
}
```

### 4.5.2 关键 Use Case

- `ListAgentsUseCase`
- `GetAgentUseCase`
- `PublishAgentVersionUseCase`
- `CompareAgentVersionsUseCase`
- `InvokeAgentUseCase`（调用 session BC）

---

## 4.6 `tool` BC（核心 · W2 起步）

> **对齐后端 `modules/tool/`**：Tool / ToolCall / ToolProtocol

### 4.6.1 关键 Entity

```ts
// features/tool/model/entities/Tool.ts
export class Tool {
  constructor(
    private readonly _id: ToolId,
    private _name: string,
    private _protocol: ToolProtocol,
    private _schema: JSONSchema,
    private _enabled: boolean = true,
  ) {}

  get id(): ToolId { return this._id; }
  get name(): string { return this._name; }
  get protocol(): ToolProtocol { return this._protocol; }
  get schema(): JSONSchema { return this._schema; }
  get enabled(): boolean { return this._enabled; }

  /** 业务方法：禁用工具 */
  disable(): void { this._enabled = false; }

  /** 业务方法：启用 */
  enable(): void { this._enabled = true; }

  /** 业务方法：执行（Domain Service 委托） */
  async invoke(args: Record<string, unknown>): Promise<ToolCallResult> {
    if (!this._enabled) throw new Error(`Tool ${this._name} is disabled`);
    return this._protocol.invoke(this._id, args);
  }
}
```

### 4.6.2 关键 Use Case

- `ListToolsUseCase`
- `InvokeToolUseCase`
- `DisableToolUseCase`

---

## 4.7 `skill` BC（核心 · W2 起步）

> **对齐后端 `modules/skill/`**：SkillPackage / SkillInvocation / RunToken

### 4.7.1 关键 Entity

```ts
// features/skill/model/entities/SkillPackage.ts
export class SkillPackage {
  constructor(
    private readonly _id: SkillPackageId,
    private _name: string,
    private _version: string,
    private _runtime: 'python' | 'node',
    private _entrypoint: string,
    private _requiredTools: readonly ToolId[],
    private _outputSpec: JSONSchema,
  ) {}

  get id(): SkillPackageId { return this._id; }
  get name(): string { return this._name; }
  get runtime(): 'python' | 'node' { return this._runtime; }
  get entrypoint(): string { return this._entrypoint; }
  get requiredTools(): readonly ToolId[] { return this._requiredTools; }
  get outputSpec(): JSONSchema { return this._outputSpec; }
}
```

```ts
// features/skill/model/entities/SkillInvocation.ts
export class SkillInvocation {
  constructor(
    private readonly _id: SkillInvocationId,
    private readonly _skillPackageId: SkillPackageId,
    private _status: 'pending' | 'running' | 'completed' | 'failed',
    private _result?: unknown,
    private _error?: string,
    private _startedAt?: string,
    private _finishedAt?: string,
  ) {}

  get id(): SkillInvocationId { return this._id; }
  get status(): 'pending' | 'running' | 'completed' | 'failed' { return this._status; }
  get result(): unknown { return this._result; }
  get error(): string | undefined { return this._error; }

  /** 业务方法：开始 */
  start(): void {
    if (this._status !== 'pending') throw new Error(`Cannot start: status=${this._status}`);
    this._status = 'running';
    this._startedAt = new Date().toISOString();
  }

  /** 业务方法：完成 */
  complete(result: unknown): void {
    if (this._status !== 'running') throw new Error(`Cannot complete: status=${this._status}`);
    this._status = 'completed';
    this._result = result;
    this._finishedAt = new Date().toISOString();
  }

  /** 业务方法：失败 */
  fail(error: string): void {
    if (this._status !== 'running') throw new Error(`Cannot fail: status=${this._status}`);
    this._status = 'failed';
    this._error = error;
    this._finishedAt = new Date().toISOString();
  }
}
```

### 4.7.2 关键 Use Case

- `ListSkillPackagesUseCase`
- `RunSkillUseCase`
- `GetSkillInvocationUseCase`

---

## 4.8 `knowledge` BC（核心 · W3 增量）

> **对齐后端 `modules/knowledge/`**：KnowledgePackage / Asset / Retriever

关键 Entity：`KnowledgePackage`、`Asset`、`Retriever`
关键 Use Case：`IngestDocumentUseCase`、`SearchKnowledgeUseCase`、`ListKnowledgePackagesUseCase`

---

## 4.9 `memory` BC（Supporting · W3 增量）

> **对齐后端 `modules/memory/`**：MemoryEntry / Recall

关键 Entity：`MemoryEntry`、`RecallResult`
关键 Use Case：`RecordMemoryUseCase`、`RecallMemoryUseCase`、`ForgetMemoryUseCase`（GDPR）

---

## 4.10 `governance` BC（Supporting · W3 增量）

> **对齐后端 `modules/governance/`**：PolicyRule / Approval / AuditLog

关键 Entity：`PolicyRule`、`Approval`、`AuditLog`
关键 Use Case：`CheckPolicyUseCase`（拦截所有调用）、`CreateApprovalUseCase`、`SignApprovalUseCase`、`ListAuditLogsUseCase`

**特殊**：governance 是**横切 BC**——其他 BC 的 Use Case 必须先过 governance 拦截（在 lib/use-cases/ 编排中）。

---

## 4.11 `identity` BC（Generic · W1 起步）

> **对齐后端 `modules/identity/`**：Tenant / Workspace / User / Role

关键 Entity：`Tenant`、`Workspace`、`User`、`Role`
关键 Use Case：`LoginUseCase`、`GetCurrentUserUseCase`、`SwitchTenantUseCase`、`SwitchWorkspaceUseCase`、`ListUsersUseCase`

**特殊**：identity 是**前置 BC**——auth / tenant / workspace state 在 `web/src/entities/`，路由守卫横切所有 BC。

---

## 4.12 `channel` BC（前端独有 · W3 增量）

> **后端无对应模块**；前端独有的 IM 渠道运营配置。

关键 Entity：`Channel`（飞书 / 钉钉 / 企微 / Web）、`ChannelDelivery`
关键 Use Case：`ListChannelsUseCase`、`ConfigureChannelUseCase`、`ListDeliveriesUseCase`

---

## 4.13 `observability`（横切 · widgets 层）

> **对齐后端 `modules/observability/`**：Trace / Metric / Log / Cost

非 BC，是**横切 widgets**：
- `web/src/app/observability/`：trace_id 管理 + 上报（与 api client 集成）
- `web/src/widgets/observability/`：TraceViewer / CostDashboard / QualityDashboard 视图

```ts
// web/src/app/observability/reporter.ts
import type { TraceDTO, MetricDTO, LogDTO } from '@eos/web-types';
import { api } from '@eos/web-api';

export class ObservabilityReporter {
  reportTrace(trace: TraceDTO): void {
    void api.observability.ingestTrace(trace);
  }
  reportMetric(metric: MetricDTO): void {
    void api.observability.ingestMetric(metric);
  }
  reportLog(log: LogDTO): void {
    void api.observability.ingestLog(log);
  }
}
```

---

## 4.14 跨 BC 通信三选项

### 4.14.1 读其他 BC 数据（经本 BC Repository Port）

```ts
// 例：session BC 需要查 agent_runtime 的 AgentVersion
// 在 session/lib/use-cases/ 编排中注入 AgentVersionRepository Port
import type { AgentVersionRepository } from '@/features/agent_runtime/model/repositories/AgentVersionRepository';

export interface CreateSessionDeps {
  agentVersionRepo: AgentVersionRepository;  // ← 跨 BC 依赖（经 Port）
  sessionRepo: SessionRepository;
}

export class CreateSessionUseCase {
  constructor(private readonly deps: CreateSessionDeps) {}

  async execute(input: { agentVersionId: string; workspaceId: string }): Promise<Session> {
    const av = await this.deps.agentVersionRepo.findById(new AgentVersionId(input.agentVersionId));
    if (!av) throw new Error('AgentVersion not found');
    return this.deps.sessionRepo.create({ agentVersionId: av.id.value, workspaceId: input.workspaceId });
  }
}
```

> **关键**：session 通过 Port 调 agent_runtime —— **Use Case 不感知 agent_runtime 的具体实现**。

### 4.14.2 写其他 BC（Domain Event + ACL）

```ts
// session BC 发布事件
await this.deps.eventBus.publish({
  type: 'session.closed',
  occurredAt: new Date().toISOString(),
  sessionId: session.id,
});

// governance BC 订阅（在 app/composition/subscribers.ts 注册）
import { eventBus } from '@/app/infrastructure/EventBus';

export function registerGovernanceSubscribers(): () => void {
  const off1 = eventBus.on('session.closed', async (e: SessionClosedEvent) => {
    await appendAuditLog({ action: 'session.closed', sessionId: e.sessionId.value, occurredAt: e.occurredAt });
  });
  const off2 = eventBus.on('tool.invoked', async (e: ToolInvokedEvent) => {
    await checkPolicy({ action: 'tool.invoke', toolId: e.toolId.value, args: e.args });
  });
  return () => { off1(); off2(); };
}
```

### 4.14.3 共享数据（Shared Kernel · @eos/web-types）

```ts
// 多个 BC 共用 SessionDTO
import type { SessionDTO } from '@eos/web-types';
// session / observability / governance / home 共用
```

---

## 4.15 ACL 翻译规则

每个 BC 内 `model/acl/<other-bc>.ts`：

```ts
// features/session/model/acl/agent_runtime.ts
import type { AgentVersionSummaryDTO } from '@eos/web-types';

export function mapAgentVersionToSessionInput(
  av: AgentVersionSummaryDTO,
  workspaceId: string,
): { agentVersionId: string; workspaceId: string } {
  if (av.status !== 'published') {
    throw new Error(`AgentVersion ${av.id} is not published (status=${av.status})`);
  }
  return { agentVersionId: av.id, workspaceId };
}
```

```ts
// features/governance/model/acl/session.ts
import type { SessionSummaryDTO } from '@eos/web-types';

export function mapSessionToAuditEntry(
  session: SessionSummaryDTO,
  action: string,
): AuditEntryInput {
  return {
    actor: 'system',
    target: { type: 'session', id: session.id },
    action,
    metadata: { workspaceId: session.workspace_id },
  };
}
```

> **ACL 函数在 model 层**；**翻译规则也是业务规则**——必须有测试。

---

## 4.16 Domain Event 标准

```ts
// features/<bc>/model/events/<X>Events.ts
export interface <X>Event {
  readonly type: '<bc>.<action>';
  readonly occurredAt: string;
  // ... payload
}

export type <X>AllEvents = <X>Event | <Y>Event;
```

```ts
// features/<bc>/model/events/<X>EventBus.ts
export interface <X>EventBus {
  publish(event: <X>AllEvents): Promise<void>;
}
```

```ts
// features/<bc>/api/events/InMemory<X>EventPublisher.ts
export class InMemory<X>EventPublisher implements <X>EventBus {
  async publish(event: <X>AllEvents): Promise<void> {
    void eventBus.publish(event);
  }
}
```

---

## 4.17 8 BC × FSD 复杂度

| BC | Entity | VO | Port | Use Case | 视图组件 | 复杂度 |
|---|---|---|---|---|---|---|
| `agent_runtime` | 3 | 5 | 3 | 5 | 6 | 高 |
| `session` | 4 | 5 | 2 | 6 | 8 | 高 |
| `tool` | 2 | 4 | 2 | 3 | 4 | 中 |
| `skill` | 2 | 4 | 2 | 3 | 5 | 中 |
| `knowledge` | 3 | 4 | 2 | 4 | 5 | 中 |
| `memory` | 1 | 2 | 2 | 3 | 3 | 低 |
| `governance` | 3 | 4 | 3 | 5 | 6 | 高（横切） |
| `identity` | 4 | 6 | 3 | 5 | 5 | 中（前置） |
| `channel` | 2 | 3 | 2 | 3 | 4 | 低 |

---

## 4.18 不在本节范围

- 跨 BC 协作细节 → 见 [05-跨模块协作](05-跨模块协作.md)
- 接口契约 → 见 [06-接口规范](06-接口规范.md)
- 持久化策略 → 见 [08-持久化与迁移](08-持久化与迁移.md)
- 测试覆盖 → 见 [10-测试与CI](10-测试与CI.md)