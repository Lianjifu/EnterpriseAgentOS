# 06 · 接口规范（RESTful · SSE · MCP Tool 协议 · DTO 类型契约 · Error Code）

> 前端与后端 Enterprise-Agent-OS 的契约：
> - **RESTful** 严格状态码（与后端 `06-接口规范` 平行）
> - **SSE** Turn 流式协议
> - **MCP** Tool 协议（前端在 tool_call 渲染时使用）
> - **DTO** 类型契约（TypeScript Codegen）
> - **Error Code** 业务错误码

> **基于 docs/web-refactor/06-组件分层.md + 07-状态管理.md 模板**。
> 与原模板差异：聚焦前端视角的接口契约（REST + SSE + MCP）；DTO 来自后端 OpenAPI Codegen；Error Code 镜像后端业务错误码；与后端 `docs/enterprise-agent-os/06-接口规范.md` 平行阅读。

---

## 6.1 总体契约

| 类别 | 协议 | 传输 |
|---|---|---|
| **RESTful** | HTTP/1.1 + JSON | `@eos/web-api` ApiClient |
| **SSE** | text/event-stream | `@eos/web-api` ApiClient.stream |
| **MCP Tool** | JSON-RPC over SSE | tool BC 适配器 |
| **WebSocket**（远期） | ws | 暂未启用 |
| **认证** | Bearer Token + Tenant/Workspace 头 | ApiClient 注入 |

---

## 6.2 RESTful 严格状态码（与后端对齐）

### 6.2.1 状态码使用

| 状态码 | 含义 | 前端处理 |
|---|---|---|
| **200** | 成功 | 正常 |
| **201** | 创建成功 | 正常 |
| **204** | 删除成功（无 body） | 正常 |
| **400** | 请求格式错误 | 表单错误提示 |
| **401** | 未认证 | 跳转 /login |
| **403** | 无权限 | 跳转 /403 |
| **404** | 资源不存在 | 空状态 |
| **409** | 资源冲突 | 业务提示 |
| **422** | 业务校验失败 | 表单字段错误 |
| **429** | 限流 | 重试（指数退避） |
| **500** | 服务器错误 | 通用错误页 |
| **502** | 网关错误 | 重试 |
| **503** | 服务不可用 | 降级（切 MockApiAdapter） |

### 6.2.2 前端错误映射

```ts
// packages/api/src/errors.ts
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly code: string,
    public readonly detail: unknown,
  ) {
    super(`${status} ${code}`);
  }
}

export function mapStatusToError(status: number, body: unknown): ApiError {
  const code = (body as { code?: string })?.code ?? 'UNKNOWN';
  const detail = (body as { detail?: unknown })?.detail ?? body;
  return new ApiError(status, code, detail);
}
```

### 6.2.3 业务错误码（来自后端）

```ts
// packages/types/src/error-codes.ts（来自后端 OpenAPI Codegen）
export const ErrorCodes = {
  // session
  SESSION_NOT_FOUND: 'session.not_found',
  SESSION_CLOSED: 'session.closed',
  SESSION_QUOTA_EXCEEDED: 'session.quota_exceeded',

  // tool
  TOOL_NOT_FOUND: 'tool.not_found',
  TOOL_DISABLED: 'tool.disabled',
  TOOL_INVOKE_FAILED: 'tool.invoke_failed',

  // skill
  SKILL_NOT_FOUND: 'skill.not_found',
  SKILL_RUN_FAILED: 'skill.run_failed',
  SKILL_RUNTIME_TIMEOUT: 'skill.runtime_timeout',

  // agent_runtime
  AGENT_NOT_FOUND: 'agent.not_found',
  AGENT_VERSION_NOT_PUBLISHED: 'agent_version.not_published',

  // knowledge
  KNOWLEDGE_PACKAGE_NOT_FOUND: 'knowledge.not_found',
  KNOWLEDGE_INGEST_FAILED: 'knowledge.ingest_failed',

  // memory
  MEMORY_NOT_FOUND: 'memory.not_found',
  MEMORY_FORBIDDEN_GDPR: 'memory.gdpr_forbidden',

  // governance
  POLICY_VIOLATED: 'governance.policy_violated',
  APPROVAL_REQUIRED: 'governance.approval_required',
  APPROVAL_REJECTED: 'governance.approval_rejected',
  QUOTA_EXCEEDED: 'governance.quota_exceeded',

  // identity
  AUTH_INVALID_TOKEN: 'auth.invalid_token',
  AUTH_TOKEN_EXPIRED: 'auth.token_expired',
  AUTH_WORKSPACE_FORBIDDEN: 'auth.workspace_forbidden',

  // generic
  INTERNAL_ERROR: 'internal.error',
  RATE_LIMITED: 'rate_limited',
} as const;

export type ErrorCode = typeof ErrorCodes[keyof typeof ErrorCodes];
```

### 6.2.4 错误处理决策表

| 错误码 | 前端行为 | UI |
|---|---|---|
| `SESSION_NOT_FOUND` | 不重试 | "会话已不存在" |
| `SESSION_QUOTA_EXCEEDED` | 不重试 | "会话配额已满" |
| `POLICY_VIOLATED` | 不重试 | 跳审批 modal |
| `APPROVAL_REQUIRED` | 不重试 | 跳审批 modal |
| `QUOTA_EXCEEDED` | 不重试 | "配额已满，请升级" |
| `AUTH_TOKEN_EXPIRED` | 重试（refresh token） | 静默 |
| `RATE_LIMITED` | 重试（指数退避） | 静默 |
| `TOOL_INVOKE_FAILED` | 重试 1 次 | "工具调用失败" |
| `SKILL_RUN_FAILED` | 不重试 | "Skill 执行失败" |
| `INTERNAL_ERROR` | 不重试 | 通用错误页 + trace_id |

---

## 6.3 SSE Turn 流式协议

### 6.3.1 端点

```
POST /v1/sessions/{id}/turns
Content-Type: application/json
Accept: text/event-stream
Authorization: Bearer {token}
X-Tenant-Id: {tenantId}
X-Workspace-Id: {workspaceId}
X-Trace-Id: {traceId}

{
  "content": "用户输入",
  "stream": true
}
```

### 6.3.2 响应（SSE）

```
event: message
data: {"type": "message", "delta": "正在思考..."}

event: tool_call
data: {"type": "tool_call", "id": "tc_001", "tool_name": "search", "args": {"q": "..."}}

event: tool_result
data: {"type": "tool_result", "id": "tc_001", "result": {...}}

event: message
data: {"type": "message", "delta": "已找到结果："}

event: message
data: {"type": "message", "delta": "..."}

event: done
data: {"type": "done", "usage": {"input": 10, "output": 20}, "trace_id": "tr_001"}

event: error
data: {"type": "error", "code": "tool.invoke_failed", "message": "..."}
```

### 6.3.3 Chunk 类型

```ts
// packages/types/src/session.ts（来自后端 Codegen）
export type TurnChunkDTO =
  | { type: 'message'; delta: string }
  | { type: 'tool_call'; id: string; tool_name: string; args: Record<string, unknown> }
  | { type: 'tool_result'; id: string; result: unknown }
  | { type: 'skill_invocation'; id: string; skill_name: string; status: 'started' | 'completed' | 'failed' }
  | { type: 'memory_write'; id: string; memory_type: 'short' | 'long' }
  | { type: 'knowledge_search'; query: string; hits: number }
  | { type: 'usage'; input: number; output: number }
  | { type: 'done'; usage: { input: number; output: number }; trace_id: string }
  | { type: 'error'; code: string; message: string };
```

### 6.3.4 前端 SSE 客户端

```ts
// packages/api/src/client.ts（已在 03-共享内核 3.3.2 给出）
async *stream(opts: { method: 'POST'; path: string; body?: unknown; signal?: AbortSignal }): AsyncIterable<unknown> {
  const res = await this.fetchStream(opts);
  const reader = res.body!.getReader();
  const decoder = new TextDecoder();
  let buffer = '';
  while (true) {
    const { value, done } = await reader.read();
    if (done) break;
    buffer += decoder.decode(value, { stream: true });
    const lines = buffer.split('\n\n');
    buffer = lines.pop() ?? '';
    for (const line of lines) {
      if (line.startsWith('data:')) {
        const payload = line.slice(5).trim();
        if (payload && payload !== '[DONE]') yield JSON.parse(payload);
      }
    }
  }
}
```

### 6.3.5 前端 chunk 渲染分支

```tsx
// features/session/ui/components/TurnNarrative.tsx
import type { TurnChunkDTO } from '@eos/web-types';

export function TurnNarrative({ chunks }: { chunks: readonly TurnChunkDTO[] }) {
  return (
    <>
      {chunks.map((chunk, i) => (
        <ChunkRenderer key={i} chunk={chunk} />
      ))}
    </>
  );
}

function ChunkRenderer({ chunk }: { chunk: TurnChunkDTO }) {
  switch (chunk.type) {
    case 'message':
      return <p>{chunk.delta}</p>;
    case 'tool_call':
      return <ToolCallCard toolName={chunk.tool_name} args={chunk.args} status="running" />;
    case 'tool_result':
      return <ToolCallCard toolName="..." result={chunk.result} status="completed" />;
    case 'skill_invocation':
      return <SkillInvocationCard skillName={chunk.skill_name} status={chunk.status} />;
    case 'memory_write':
      return <MemoryWriteBadge memoryType={chunk.memory_type} />;
    case 'knowledge_search':
      return <KnowledgeSearchBadge query={chunk.query} hits={chunk.hits} />;
    case 'usage':
      return <UsageBadge input={chunk.input} output={chunk.output} />;
    case 'done':
      return <TurnCompleteBadge usage={chunk.usage} />;
    case 'error':
      return <ErrorBadge code={chunk.code} message={chunk.message} />;
  }
}
```

---

## 6.4 MCP Tool 协议

### 6.4.1 协议定义

```ts
// packages/types/src/tool.ts
export type ToolProtocol =
  | MCPProtocol        // Model Context Protocol
  | OpenAPIProtocol    // OpenAPI 3.x
  | JSONRPCProtocol    // JSON-RPC 2.0
  | CustomProtocol;    // 自定义

export interface MCPProtocol {
  kind: 'mcp';
  serverUrl: string;
  transport: 'sse' | 'stdio';
  capabilities: { tools: boolean; resources: boolean; prompts: boolean };
}

export interface OpenAPIProtocol {
  kind: 'openapi';
  specUrl: string;
  baseUrl: string;
}

export interface JSONRPCProtocol {
  kind: 'jsonrpc';
  endpoint: string;
  method: string;
}

export interface CustomProtocol {
  kind: 'custom';
  schema: JSONSchema;
  invoke: 'http' | 'function';
}
```

### 6.4.2 Tool 调用（前端展示）

```ts
// features/tool/lib/hooks/useInvokeTool.ts
import { useMutation } from '@tanstack/react-query';
import { api } from '@eos/web-api';
import { useDeps } from '@/app/composition/useDeps';

export function useInvokeTool() {
  const { tool } = useDeps();
  return useMutation({
    mutationFn: (input: { toolName: string; args: Record<string, unknown> }) =>
      api.tool.invoke(input.toolName, input.args),
  });
}
```

### 6.4.3 MCP Tool 列表

```ts
// packages/api/src/domains/tool.ts
export function createToolApi(client: ApiClient) {
  return {
    listTools: (workspaceId: string) =>
      client.request<ToolDTO[]>({ path: '/v1/tools', query: { workspaceId } }),

    getTool: (name: string) =>
      client.request<ToolDTO>({ path: `/v1/tools/${name}` }),

    invoke: (name: string, args: Record<string, unknown>) =>
      client.request<ToolResultDTO>({
        method: 'POST',
        path: `/v1/tools/${name}/invoke`,
        body: { args },
      }),

    disable: (name: string) =>
      client.request<void>({ method: 'DELETE', path: `/v1/tools/${name}` }),

    enable: (name: string) =>
      client.request<void>({ method: 'POST', path: `/v1/tools/${name}/enable` }),
  };
}
```

---

## 6.5 DTO 类型契约（Shared Kernel）

### 6.5.1 Codegen 流程

```bash
# 从后端 openapi.yaml 生成 TypeScript 类型
cd web
pnpm codegen

# 内部实现（远期）：
# 1. 从后端下载 openapi.yaml
# 2. openapi-typescript 生成 types
# 3. 输出到 packages/types/src/
```

```jsonc
// web/package.json
{
  "scripts": {
    "codegen": "tsx scripts/codegen.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.0.0"
  }
}
```

```ts
// web/scripts/codegen.ts
import { generateTypes } from 'openapi-typescript';
import { readFileSync, writeFileSync } from 'fs';

const openapiYaml = readFileSync('../backend/openapi.yaml', 'utf8');
const types = await generateTypes(openapiYaml);
writeFileSync('packages/types/src/generated.ts', types);
```

### 6.5.2 DTO 形态（与后端对齐）

```ts
// packages/types/src/session.ts（来自后端 modules/session/openapi.yaml）
export interface SessionDTO {
  id: string;
  agent_version_id: string;
  workspace_id: string;
  status: 'idle' | 'running' | 'closed';
  created_at: string;
  closed_at: string | null;
  turns: TurnDTO[];
  metadata: Record<string, string>;
}

export interface TurnDTO {
  id: number;
  role: 'user' | 'assistant' | 'tool' | 'system';
  content: string;
  tool_calls?: ToolCallDTO[];
  tool_call_id?: string;
  created_at: string;
  usage?: { input: number; output: number };
}

export interface ToolCallDTO {
  id: string;
  tool_name: string;
  args: Record<string, unknown>;
  result?: unknown;
  error?: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  started_at: string;
  finished_at?: string;
}
```

### 6.5.3 DTO ↔ Entity 转换

```ts
// features/session/model/entities/Session.ts
import type { SessionDTO } from '@eos/web-types';
import { SessionId } from '../value-objects/SessionId';
import { Turn } from './Turn';

export class Session {
  static fromDTO(dto: SessionDTO): Session {
    const s = new Session(
      new SessionId(dto.id),
      dto.agent_version_id,
      dto.workspace_id,
    );
    if (dto.status === 'closed') s.close();
    dto.turns.forEach(t => s.appendTurn(Turn.fromDTO(t)));
    return s;
  }
  toDTO(): SessionDTO {
    return {
      id: this._id.value,
      agent_version_id: this._agentVersionId,
      workspace_id: this._workspaceId,
      status: this._status,
      created_at: '',
      closed_at: null,
      turns: this._turns.map(t => t.toDTO()),
      metadata: {},
    };
  }
}
```

---

## 6.6 认证头与 Tenant / Workspace 头

### 6.6.1 必备头

| 头 | 来源 | 说明 |
|---|---|---|
| `Authorization` | auth.store.token | `Bearer {token}` |
| `X-Tenant-Id` | tenant.store.current | 租户 ID |
| `X-Workspace-Id` | workspace.store.current | 工作区 ID |
| `X-Trace-Id` | traceContext.get | 当前 trace_id |
| `Content-Type` | ApiClient | `application/json` |
| `Accept` | ApiClient | `application/json` 或 `text/event-stream` |

### 6.6.2 ApiClient 注入

```ts
// packages/api/src/client.ts
async request<T>(opts: { ... }): Promise<T> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...opts.headers,
  };
  const token = this.config.getToken();
  if (token) headers['Authorization'] = `Bearer ${token}`;
  const tenantId = this.config.getTenantId();
  if (tenantId) headers['X-Tenant-Id'] = tenantId;
  const workspaceId = this.config.getWorkspaceId();
  if (workspaceId) headers['X-Workspace-Id'] = workspaceId;
  const traceId = traceContext.get();
  if (traceId) headers['X-Trace-Id'] = traceId;
  // ...
}
```

---

## 6.7 API 路径规范（与后端对齐）

### 6.7.1 命名约定

| 类别 | 前缀 | 例 |
|---|---|---|
| **identity** | `/v1/identity/*` | `/v1/identity/me` `/v1/identity/tenants` |
| **agent_runtime** | `/v1/agent-runtime/*` 或 `/v1/agents/*` | `/v1/agents/{id}` `/v1/agents/{id}/versions` |
| **session** | `/v1/sessions/*` | `/v1/sessions` `/v1/sessions/{id}/turns` |
| **tool** | `/v1/tools/*` | `/v1/tools/{name}/invoke` |
| **skill** | `/v1/skills/*` | `/v1/skills/{id}/run` |
| **knowledge** | `/v1/knowledge/*` | `/v1/knowledge/packages` `/v1/knowledge/search` |
| **memory** | `/v1/memory/*` | `/v1/memory/entries` `/v1/memory/recall` |
| **governance** | `/v1/governance/*` | `/v1/governance/policies` `/v1/governance/approvals` |
| **channel** | `/v1/channels/*` | `/v1/channels` `/v1/channels/{id}/deliveries` |
| **observability** | `/v1/observability/*` | `/v1/observability/traces` `/v1/observability/costs` |

### 6.7.2 8 BC × Endpoint 表

| BC | 端点 | 方法 | 用途 |
|---|---|---|---|
| **identity** | `/v1/identity/me` | GET | 当前用户 |
| | `/v1/identity/tenants` | GET | 当前用户可见租户列表 |
| | `/v1/identity/workspaces` | GET | 当前租户下工作区列表 |
| | `/v1/identity/users` | GET | 用户列表 |
| | `/v1/identity/login` | POST | 登录 |
| | `/v1/identity/logout` | POST | 登出 |
| **agent_runtime** | `/v1/agents` | GET | Agent 列表 |
| | `/v1/agents` | POST | 创建 Agent |
| | `/v1/agents/{id}` | GET | Agent 详情 |
| | `/v1/agents/{id}/versions` | GET | 版本列表 |
| | `/v1/agents/{id}/versions` | POST | 发布新版本 |
| | `/v1/agents/{id}/versions/compare` | POST | 对比版本 |
| **session** | `/v1/sessions` | GET | Session 列表 |
| | `/v1/sessions` | POST | 创建 Session |
| | `/v1/sessions/{id}` | GET | Session 详情 |
| | `/v1/sessions/{id}` | DELETE | 关闭 Session |
| | `/v1/sessions/{id}/turns` | POST（SSE） | 发起 Turn |
| | `/v1/sessions/{id}/turns` | GET | Turn 历史 |
| **tool** | `/v1/tools` | GET | 工具列表 |
| | `/v1/tools/{name}` | GET | 工具详情 |
| | `/v1/tools/{name}/invoke` | POST | 调用工具 |
| | `/v1/tools/{name}` | DELETE | 禁用 |
| | `/v1/tools/{name}/enable` | POST | 启用 |
| **skill** | `/v1/skills` | GET | Skill 包列表 |
| | `/v1/skills/{id}` | GET | Skill 包详情 |
| | `/v1/skills/{id}/run` | POST | 运行 Skill |
| | `/v1/skills/{id}/invocations/{inv_id}` | GET | Skill 调用状态 |
| **knowledge** | `/v1/knowledge/packages` | GET | 知识包列表 |
| | `/v1/knowledge/packages` | POST | 创建知识包 |
| | `/v1/knowledge/ingest` | POST | 摄取文档 |
| | `/v1/knowledge/search` | POST | 检索 |
| **memory** | `/v1/memory/entries` | GET | 记忆列表 |
| | `/v1/memory/entries` | POST | 记录记忆 |
| | `/v1/memory/recall` | POST | 召回 |
| | `/v1/memory/entries/{id}` | DELETE | 遗忘（GDPR） |
| **governance** | `/v1/governance/policies` | GET | 策略列表 |
| | `/v1/governance/policies` | POST | 创建策略 |
| | `/v1/governance/approvals` | GET | 审批列表 |
| | `/v1/governance/approvals` | POST | 请求审批 |
| | `/v1/governance/approvals/{id}/sign` | POST | 签署 |
| | `/v1/governance/audit-logs` | GET | 审计日志 |
| **channel** | `/v1/channels` | GET | 渠道列表 |
| | `/v1/channels/{id}/config` | PUT | 配置渠道 |
| | `/v1/channels/{id}/deliveries` | GET | 投递日志 |
| **observability** | `/v1/observability/traces/{id}` | GET | 获取 trace |
| | `/v1/observability/costs` | GET | 成本统计 |
| | `/v1/observability/quality` | GET | 质量评估 |
| | `/v1/observability/ingest/trace` | POST | 上报 trace（前端） |
| | `/v1/observability/ingest/metric` | POST | 上报 metric（前端） |

---

## 6.8 数据格式

### 6.8.1 时间格式

```ts
// packages/types/src/common.ts
// 所有时间字段统一 ISO 8601（UTC）
export type ISODateString = string;  // e.g., "2026-09-19T08:00:00Z"
```

### 6.8.2 ID 命名规范（来自后端）

| 资源 | 前缀 | 例 |
|---|---|---|
| Tenant | `tnt_` | `tnt_acme` |
| Workspace | `ws_` | `ws_engineering` |
| User | `usr_` | `usr_alice` |
| Agent | `agt_` | `agt_sales_bot` |
| AgentVersion | `agv_` | `agv_001` |
| Session | `ses_` | `ses_001` |
| Turn | （自增） | `1` `2` |
| ToolCall | `tc_` | `tc_001` |
| SkillPackage | `skp_` | `skp_pdf_parser` |
| SkillInvocation | `ski_` | `ski_001` |
| KnowledgePackage | `kp_` | `kp_company_handbook` |
| MemoryEntry | `mem_` | `mem_001` |
| PolicyRule | `pol_` | `pol_data_export` |
| Approval | `apr_` | `apr_001` |
| AuditLog | `aud_` | `aud_001` |
| Channel | `chn_` | `chn_feishu_main` |
| Trace | `tr_` | `tr_001` |

### 6.8.3 分页

```ts
export interface Page<T> {
  items: T[];
  total: number;
  page: number;
  page_size: number;
  next_cursor?: string;
}
```

---

## 6.9 Mock 与 API 一致性保证

### 6.9.1 形态对齐

```ts
// api 客户端
api.session.getSession('ses_001')           // → SessionDTO

// mock handler
mock.session.getSession('ses_001')           // → SessionDTO（同名同形态）

// BC 适配器
class HttpApiSessionAdapter implements SessionRepository {
  async findById(id) { return Session.fromDTO(await api.session.getSession(id.value)); }
}
class MockApiSessionAdapter implements SessionRepository {
  async findById(id) { return Session.fromDTO(await mock.session.getSession(id.value)); }
}
```

### 6.9.2 Contract Test

```ts
// tests/contract/session.contract.test.ts
import { mock } from '@eos/web-mock';

describe('session contract', () => {
  it('getSession returns SessionDTO with required fields', async () => {
    const dto = await mock.session.getSession('ses_001');
    expect(dto).toHaveProperty('id');
    expect(dto).toHaveProperty('agent_version_id');
    expect(dto).toHaveProperty('workspace_id');
    expect(dto).toHaveProperty('status');
    expect(dto).toHaveProperty('turns');
    expect(dto.turns).toBeInstanceOf(Array);
  });
});
```

> **关键**：mock 与 api 共享 `@eos/web-types` 类型契约；前端 CI 跑契约测试保证一致性。

---

## 6.10 上传 / 下载

### 6.10.1 文档上传（knowledge ingest）

```ts
const formData = new FormData();
formData.append('file', file);
formData.append('package_id', 'kp_001');
await api.knowledge.ingest(formData);
```

### 6.10.2 产物下载（skill result）

```ts
const blob = await api.skill.downloadArtifact('ski_001', 'output.json');
const url = URL.createObjectURL(blob);
window.open(url);
```

---

## 6.11 不在本节范围

- 后端 OpenAPI 详细规范 → 见后端 `docs/enterprise-agent-os/06-接口规范.md`
- 后端 MCP 实现细节 → 见后端 `modules/tool/`
- 后端 SSE 内部实现 → 见后端 `modules/session/`
- 错误码实现细节 → 见后端 `modules/shared/error_codes.py`