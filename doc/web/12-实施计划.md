# 12 · 实施计划（4 周交付 · 与后端 10 周计划并行）

> 前端 4D 架构实施计划，与后端 `docs/enterprise-agent-os/12-实施计划.md` 平行：
> - **前端周期**：4 周（含 1 周 buffer）+ 后续迭代
> - **起点**：0（与后端同步启动）
> - **关键里程碑**：Week 1 骨架 → Week 2 BC + Mock → Week 3 集成联调 → Week 4 验收
> - **依赖后端**：Week 1 openapi.yaml 可用 → Week 3 后端 staging 可用

> **基于 docs/web-refactor/12-实施计划.md 模板**。
> 与原模板差异：4 周时间盒（前端精简）；BC 顺序与后端一致；与后端 10 周时间盒平行；与后端 `docs/enterprise-agent-os/12-实施计划.md` 平行阅读。

---

## 12.1 时间轴总览

```mermaid
gantt
  title 前端 4 周交付计划
  dateFormat YYYY-MM-DD
  axisFormat W%V
  section Week 1 · 骨架
  工作区初始化                   :w1a, 2026-W37, 7d
  6 个 shared package 落地       :w1b, after w1a, 5d
  FSD + ESLint + import-linter    :w1c, after w1b, 4d
  启动顺序与依赖装配               :w1d, after w1c, 4d
  section Week 2 · BC + Mock
  8 BC 骨架 + ACL + Mock           :w2a, 2026-W38, 5d
  4 个核心 BC (session, agent, tool, skill) :w2b, after w2a, 4d
  3 个支撑 BC (knowledge, memory, identity, governance) :w2c, after w2b, 3d
  跨 BC EventBus + ACL 翻译       :w2d, after w2c, 3d
  section Week 3 · 集成联调
  与后端 staging 联调              :w3a, 2026-W39, 4d
  SSE Turn 流式协议              :w3b, after w3a, 3d
  Observability + 可观测面板        :w3c, after w3b, 3d
  Channel + 跨端体验              :w3d, after w3c, 2d
  section Week 4 · 验收 + 上线
  性能 + Bundle + 视觉验收         :w4a, 2026-W40, 3d
  E2E + Contract + Lint           :w4b, after w4a, 2d
  Staging 灰度 + Buffer           :w4c, after w4b, 2d
```

---

## 12.2 Week 1 · 工作区 + 6 个共享 package + FSD 守门

**主题**：从 0 起步到能 `pnpm dev` 跑起来 + 强制单向后端接口守门。

### 12.2.1 任务清单

| # | 任务 | 输出 | 工时 |
|---|---|---|---|
| 1.1 | 初始化 pnpm workspace monorepo | `web/package.json` `pnpm-workspace.yaml` | 0.5d |
| 1.2 | 6 个 shared package 骨架 | `packages/{api,mock,types,hooks,ui,utils}` | 1d |
| 1.3 | `@eos/web-types` 接 OpenAPI Codegen | `scripts/codegen.ts` | 0.5d |
| 1.4 | `@eos/web-api` ApiClient（含 SSE 客户端） | `packages/api/src/client.ts` | 1d |
| 1.5 | `@eos/web-mock` 启动模板 | `packages/mock/src/server.ts` | 0.5d |
| 1.6 | `@eos/web-ui` 基础组件 | `Button Modal Table Tree` | 0.5d |
| 1.7 | FSD 6 层目录 + path alias | `web/tsconfig.json` | 0.5d |
| 1.8 | ESLint FSD 单向依赖 | `.eslintrc.json` | 0.5d |
| 1.9 | `scripts/check-fsd-imports.sh` | FSD 二次守门 | 0.5d |
| 1.10 | Composition Root 骨架 | `app/composition/adapters.ts` | 0.5d |
| 1.11 | CI 跑 typecheck + lint | `web-test.yml` | 0.5d |

### 12.2.2 验收标准（Day 5）

- ✅ `pnpm dev` 启动并显示空壳 SPA
- ✅ `pnpm test` 跑通（空测试套件）
- ✅ `pnpm lint && pnpm lint:fsd` 通过
- ✅ `pnpm build` 成功产出 dist
- ✅ CI 完整跑通（typecheck + lint + test + build）
- ✅ 6 个 shared package 互相 import 关系符合 `03-共享内核.md` 边界表

### 12.2.3 交付物

- `pnpm-workspace.yaml`
- 6 个 shared package（含 index.ts + 1 个 demo）
- `web/src/app/composition/adapters.ts`（骨架）
- `web/.eslintrc.json` + `web/scripts/check-fsd-imports.sh`
- `.github/workflows/web-test.yml`

---

## 12.3 Week 2 · 8 BC 骨架 + ACL + Mock

**主题**：每个 BC 落地 `model + lib + api + ui` 四切片；Mock 全联通；后端接口未就绪也能跑业务流。

### 12.3.1 任务清单

| # | 任务 | 输出 | 工时 |
|---|---|---|---|
| 2.1 | BC 目录模板生成脚本 | `scripts/create-bc.sh` | 0.5d |
| 2.2 | `session` BC（含 SendTurnUseCase + 三个 Adapter） | `features/session/` | 1.5d |
| 2.3 | `agent_runtime` BC | `features/agent_runtime/` | 1d |
| 2.4 | `tool` BC | `features/tool/` | 1d |
| 2.5 | `skill` BC | `features/skill/` | 1d |
| 2.6 | `knowledge` BC | `features/knowledge/` | 0.5d |
| 2.7 | `memory` BC | `features/memory/` | 0.5d |
| 2.8 | `identity` BC | `features/identity/` | 1d |
| 2.9 | `governance` BC | `features/governance/` | 0.5d |
| 2.10 | `channel` BC | `features/channel/` | 0.5d |
| 2.11 | `observability` BC | `features/observability/` | 0.5d |
| 2.12 | 8 个 MockApiAdapter 实现 | `features/<bc>/api/adapters/MockApiAdapter.ts` | 1d |
| 2.13 | ACL 翻译（agent_runtime → session） | `features/session/model/acl/agent_runtime.ts` | 0.5d |
| 2.14 | EventBus 全局与发布订阅 | `app/infrastructure/EventBus.ts` | 0.5d |
| 2.15 | mock-bridge 接入（dev only） | `app/composition/mock-bridge.ts` | 0.5d |

### 12.3.2 验收标准（Day 10）

- ✅ 8 个 BC 都有 Entity / UseCase / Adapter
- ✅ Mock 全联通：能登录（mock token）→ 创建 Session → 发 Turn（mock stream）→ 收到 mock 完整事件链
- ✅ 4 个核心 BC（session / agent_runtime / tool / skill）覆盖率 ≥ 80%
- ✅ ESLint FSD 单向依赖 0 报错
- ✅ Mock token 路由到 MockApiAdapter（`isMockToken`）

### 12.3.3 交付物

- `web/src/features/<8 BC>/`
- `web/src/app/composition/mock-bridge.ts`
- `web/src/app/infrastructure/EventBus.ts`
- 单元测试覆盖 8 BC

---

## 12.4 Week 3 · 与后端联调 + SSE + Observability

**主题**：从前端纯 Mock 过渡到能跟后端 staging 真接口跑通；SSE Turn 流式协议端到端；可观测面板初版。

### 12.4.1 任务清单

| # | 任务 | 输出 | 工时 |
|---|---|---|---|
| 3.1 | 与后端 openapi.yaml 同步 | `pnpm codegen` 通过 | 0.5d |
| 3.2 | HttpApiAdapter 接入（替代 MockApiAdapter） | `features/<bc>/api/adapters/HttpApiAdapter.ts` | 1d |
| 3.3 | SSE Turn 流式客户端稳定 | `packages/api/src/client.ts` | 1d |
| 3.4 | 8 chunk 类型渲染器 | `features/session/ui/components/ChunkRenderer.tsx` | 1d |
| 3.5 | `observability` TraceViewer 面板 | `features/observability/ui/TraceViewer.tsx` | 1d |
| 3.6 | `observability` CostDashboard | `features/observability/ui/CostDashboard.tsx` | 0.5d |
| 3.7 | `observability` QualityDashboard | `features/observability/ui/QualityDashboard.tsx` | 0.5d |
| 3.8 | 前端 reporter 上报链路 | `web/src/observability/Reporter.ts` | 1d |
| 3.9 | Channel 跨端体验（feishu/teams/webhook） | `features/channel/ui/ChannelConfigPanel.tsx` | 1d |
| 3.10 | 与后端 staging 联调（User → Session → Agent → Tool/Skill → Response 全链路） | 联调记录 | 2d |
| 3.11 | 错误处理决策表落地 | `packages/api/src/errors.ts` | 0.5d |

### 12.4.2 验收标准（Day 15）

- ✅ HTTP 模式下能登录 → 创建 Session → 发 Turn → 看到真实 chunk 流
- ✅ SSE 错误重连 / 取消正常
- ✅ TraceViewer 显示当前 session 的 trace 链
- ✅ CostDashboard 显示 token 与成本
- ✅ Channel 至少 1 个渠道（feishu）能正常配置与投递
- ✅ 前端 reporter 上报到后端 collector 通路打通

### 12.4.3 交付物

- 8 BC 的 HttpApiAdapter（含 SSE）
- ChunkRenderer 与 8 种 chunk 渲染
- Observability 三个面板
- 前端 reporter（trace / metric / log / cost）
- Channel 配置 UI

---

## 12.5 Week 4 · 性能 + Bundle + 验收上线

**主题**：把 4D 架构产物打磨成可上线状态；CI 完整；性能达标；staging 灰度。

### 12.5.1 任务清单

| # | 任务 | 输出 | 工时 |
|---|---|---|---|
| 4.1 | Bundle 拆分（react-vendor / ui-vendor / query-vendor） | `vite.config.ts` `manualChunks` | 0.5d |
| 4.2 | Bundle 大小门禁（CI 检查） | `web-test.yml` | 0.5d |
| 4.3 | mock 关键字检查（CI 检查） | `web-test.yml` | 0.5d |
| 4.4 | Lighthouse CI 接入 | `.github/workflows/lighthouse.yml` | 0.5d |
| 4.5 | Web Vitals 达标 | LC≤2.5s FID≤100ms CLS≤0.1 | 0.5d |
| 4.6 | E2E（Playwright）关键路径 | `e2e/login.spec.ts` `e2e/turn.spec.ts` | 1d |
| 4.7 | Contract Test（mock vs api 形态） | `tests/contract/*.test.ts` | 0.5d |
| 4.8 | 视觉回归（Chromatic 远期 P5 / 暂用截图） | `tests/visual/*.spec.ts` | 0.5d |
| 4.9 | Dockerfile + nginx.conf 生产化 | `web/Dockerfile` | 0.5d |
| 4.10 | docker-compose staging | `docker-compose.staging.yml` | 0.5d |
| 4.11 | 灰度发布 + Buffer | K8s canary ingress | 0.5d |
| 4.12 | 13 节文档校对 | 链接 / 截图 / 索引 | 0.5d |

### 12.5.2 验收标准（Day 20）

- ✅ Bundle 总 gzipped ≤ 2 MB
- ✅ Lighthouse: LCP≤2.5s FID≤100ms CLS≤0.1
- ✅ E2E 关键路径 5 用力通过
- ✅ Contract Test 100% 通过
- ✅ Mock 不进生产 bundle（grep 验证）
- ✅ staging 灰度 10% → 50% → 100%
- ✅ 13 节文档完成（00-12 + 风险）
- ✅ 与后端 enterprise-agent-os 同集群运行

### 12.5.3 交付物

- 生产 Dockerfile + nginx.conf
- docker-compose staging
- E2E / Contract / Lighthouse
- 13 节文档 + 索引

---

## 12.6 后续迭代（Week 5+）

| 周期 | 主题 | 范围 |
|---|---|---|
| **Week 5** | WebSocket 接入 | 实时推送（替换部分 SSE） |
| **Week 6** | 多租户切换 | TenantSwitcher + 跨租户数据隔离 |
| **Week 7** | RBAC 完善 | 按钮级权限 + 资源级 ACL |
| **Week 8** | 离线模式强化 | LocalStorageAdapter 优化 + 同步 |
| **Week 9** | 视觉回归 | Chromatic 接入 |
| **Week 10** | 性能优化 | virtual scroll + code split |

---

## 12.7 风险与依赖

### 12.7.1 后端依赖

| 时间 | 依赖 | 风险 |
|---|---|---|
| Week 1 Day 1 | `openapi.yaml` 骨架 | 后端未就绪 → 暂用 mock fixture |
| Week 1 Day 3 | `/v1/identity/login` 接口 | mock token 暂时 |
| Week 2 Day 1 | 8 BC 核心端点 mock fixture | 前端独立 mock |
| Week 3 Day 1 | staging 后端可达 | 推迟到 Week 3.5 |
| Week 3 Day 3 | SSE Turn 协议稳定 | 后端流式协议可能变 → adapter 适配 |
| Week 3 Day 5 | observability ingest 端点 | 后端 collector 未就绪 → 暂走 console |

### 12.7.2 技术风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| FSD 单向依赖在 widget 层被破坏 | 阻断合并 | import-linter 二次守门 |
| mock-bridge 漏到生产 bundle | 阻断合并 | CI grep 检查 |
| SSE 浏览器兼容性（Edge/Safari） | 流式中断 | 重连 + 心跳 |
| OpenAPI Codegen 失败 | 类型断裂 | 兜底手写类型 + e2e |
| 后端 trace_id 未注入响应 | 上报失联 | 后端 Header 中间件 |

### 12.7.3 资源风险

| 风险 | 缓解 |
|---|---|
| Week 1-2 8 BC 工作量大 | 并行：4 个核心 BC 并行 → 4 个支撑 BC 串行 |
| Week 3 SSE 联调时间紧 | 提前 mock SSE 协议 + 后端同步协议 |
| Week 4 性能调优耗时 | 预留 1 天 buffer |

---

## 12.8 团队分工（建议）

| 角色 | 人数 | Week 1 | Week 2 | Week 3 | Week 4 |
|---|---|---|---|---|---|
| **架构师** | 1 | 工作区 / 守门 | FSD 校准 | 联调对接 | 验收 |
| **BC 主程 ×2** | 2 | 6 shared package | 4 核心 BC | HttpApiAdapter + SSE | 性能 + E2E |
| **BC 主程 ×2** | 2 | 同上 | 4 支撑 BC | Observability 面板 | Contract + Bundle |
| **联调 ×1** | 1 | mock | mock 全联通 | 后端联调 | staging 灰度 |
| **测试 ×0.5** | 0.5 | 测试基础设施 | 单测覆盖率 | 集成测试 | E2E + Contract |
| **设计 ×0.5** | 0.5 | FSD tokens | UI 组件库 | Observability 面板 | 视觉回归 |

---

## 12.9 关键里程碑

| 里程碑 | 日期 | 标志 |
|---|---|---|
| **M0** | Week 1 Day 1 | pnpm workspace 跑起来 |
| **M1** | Week 1 Day 5 | CI 全绿 + 6 package 完成 |
| **M2** | Week 2 Day 5 | 8 BC 骨架 + Mock 全联通 |
| **M3** | Week 2 Day 10 | 4 核心 BC ≥ 80% 覆盖率 |
| **M4** | Week 3 Day 5 | 与后端 staging 真接口跑通 |
| **M5** | Week 3 Day 10 | Observability 面板完成 |
| **M6** | Week 4 Day 5 | 性能 / Bundle / E2E 达标 |
| **M7** | Week 4 Day 10 | staging 灰度完成 |

---

## 12.10 计划变更机制

| 触发 | 处理 |
|---|---|
| 后端开放接口延期 > 2 天 | 启动 mock 后端兜底 |
| FSD 单向依赖频繁被破坏 | 复盘 + 强制 code review |
| Bundle 超预算 > 20% | 拆分 / 按需加载 / 推迟 |
| Lighthouse 不达标 | Week 4 buffer 优先解决 |
| E2E 关键路径失败 | 阻塞合并 → 修复 |

---

## 12.11 与后端 10 周计划并行

| 后端周 | 后端里程碑 | 前端周 | 前端里程碑 |
|---|---|---|---|
| W1 | Monorepo + 模块骨架 | W1 | pnpm + 6 package + FSD |
| W2 | identity + agent_runtime | W2 | 8 BC 骨架 + Mock |
| W3 | session + skill | W2-W3 | HttpApiAdapter + SSE |
| W4 | tool + knowledge + memory | W3 | 联调 + Observability |
| W5 | governance + channel | W3-W4 | Channel UI + 验收 |
| W6 | observability 后端 | W3 | 前端 reporter + 面板 |
| W7 | 集成 + 压测 | W4 | 性能 + Bundle |
| W8 | staging 上线 | W4 | staging 灰度 |
| W9 | 生产蓝绿 | W5+ | WebSocket / RBAC |
| W10 | 1.0 验收 | W6+ | 持续迭代 |

> **关键**：前端 4 周 + buffer 与后端 W1-W8 重叠；前端在 W4 灰度上线后进入迭代节奏。

---

## 12.12 不在本节范围

- 后端实施计划 → 见后端 `docs/enterprise-agent-os/12-实施计划.md`
- 团队 OKR / KPI → 公司层面
- 资源调度 / 招聘 → HR 层
- 详细风险管理 → [13-风险与验收](13-风险与验收.md)