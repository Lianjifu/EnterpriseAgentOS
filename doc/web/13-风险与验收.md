# 13 · 风险与验收（5 类风险 · 12 维度验收清单 · ~80 验收项）

> 4D 架构风险盘点与验收清单，与后端 `docs/enterprise-agent-os/13-风险与验收.md` 平行：
> - **5 类风险**：架构 / 技术 / 集成 / 安全 / 业务
> - **12 维度验收**：FSD / DDD / Hex / Mock / Type / SSE / Performance / CI / Security / i18n / A11y / Doc
> - **~80 验收项**：覆盖 4 周交付关键路径
> - **准入准出**：每个验收项有明确通过标准与责任人

> **基于 docs/web-refactor/13-风险与验收.md 模板**。
> 与原模板差异：5 类风险聚焦前端 4D 架构；12 维度验收与后端对齐；BC 名与后端 1:1 对齐；mock 进生产作为红色风险；与后端 `docs/enterprise-agent-os/13-风险与验收.md` 平行阅读。

---

## 13.1 5 类风险

### 13.1.1 架构风险

| ID | 风险 | 等级 | 触发条件 | 缓解 | 负责人 |
|---|---|---|---|---|---|
| AR-01 | FSD 单向依赖被破坏 | 🟡 中 | widget import 其他 BC 的 model | import-linter 二次守门 + code review | 架构师 |
| AR-02 | Hex 三类适配器混入 | 🟠 高 | `MockApi` 引用进 `HttpApi` 适配层 | CI grep `MockApi\|LocalStorage.*Adapter\|installMockBridge\|isMockToken` | 架构师 |
| AR-03 | DDD 跨 BC 直接 import | 🟠 高 | BC A 直接引用 BC B 的 Entity | 强制 Repository Port / Domain Event / ACL | BC 主程 |
| AR-04 | 路径别名 `@de/*` 与 `@eos/*` 混用 | 🟡 中 | 老代码迁移残留 | 强制新代码 `@eos/*` + 重构计划 | 架构师 |
| AR-05 | Composition Root 体积膨胀 | 🟡 中 | `app/composition/adapters.ts` > 500 行 | 拆分子文件 `app/composition/<bc>/` | 架构师 |
| AR-06 | BC 数量膨胀失控 | 🟡 中 | BC 超过 12 个 | 评估合并 / 拆包 | 架构师 |

### 13.1.2 技术风险

| ID | 风险 | 等级 | 触发条件 | 缓解 | 负责人 |
|---|---|---|---|---|---|
| TR-01 | OpenAPI Codegen 失败 | 🟠 高 | 后端 openapi.yaml 变更 | 兜底手写类型 + e2e + Codegen retry | BC 主程 |
| TR-02 | SSE 浏览器兼容性 | 🟠 高 | Edge / Safari 中断 | 重连 + 心跳 + polyfill | BC 主程 |
| TR-03 | zustand persist 序列化失败 | 🟡 中 | Map / Set / Date 序列化 | 显式 schema 校验 + 升级函数 | BC 主程 |
| TR-04 | queryClient 缓存与 server 不同步 | 🟡 中 | 写后读不到 | `invalidateQueries` + ETag | BC 主程 |
| TR-05 | SSE 解析大消息 OOM | 🟡 中 | 巨型 tool_result | 流式 chunk + 截断 | BC 主程 |
| TR-06 | Vite 构建慢 | 🟡 中 | bundle 增长 | `manualChunks` + `dynamic import` | 架构师 |
| TR-07 | PWA Service Worker 缓存陈旧 | 🟡 中 | 新版本不可用 | `autoUpdate` + 手动 skipWaiting | BC 主程 |
| TR-08 | Monaco / Editor 包体过大 | 🟠 高 | 首屏加载慢 | `dynamic import` + `manualChunks` | BC 主程 |
| TR-09 | 跨域 CORS | 🟡 中 | staging/prod 域名变更 | nginx 配置 + 后端 CORS middleware | 架构师 |

### 13.1.3 集成风险

| ID | 风险 | 等级 | 触发条件 | 缓解 | 负责人 |
|---|---|---|---|---|---|
| IR-01 | 后端 openapi.yaml 变更未通知 | 🟠 高 | DTO 字段消失 / 类型变更 | Codegen 周跑 + 类型检查 | BC 主程 |
| IR-02 | 后端 SSE 协议变 | 🔴 极高 | chunk 类型新增 / 删除 | adapter 抽象 + 协议版本 | BC 主程 |
| IR-03 | 后端 trace_id 未注入响应 | 🟡 中 | 上报失联 | 后端 Header 中间件 | 联调 |
| IR-04 | 后端 rate-limit 不一致 | 🟡 中 | 客户端触发 429 | 指数退避 + 提示 | BC 主程 |
| IR-05 | mock 与 api 形态偏离 | 🟠 高 | 一边更新一边没改 | Contract Test + OpenAPI 单一源 | 测试 |
| IR-06 | staging 环境不稳定 | 🟡 中 | 联调卡住 | 本地 docker-compose 后端兜底 | 联调 |
| IR-07 | 后端 CORS 不允许 staging 域名 | 🟡 中 | staging 部署失败 | 后端 CORS middleware 配置 | 架构师 |
| IR-08 | Observability 后端 collector 未就绪 | 🟡 中 | 上报丢失 | fallback console + 队列 | BC 主程 |

### 13.1.4 安全风险

| ID | 风险 | 等级 | 触发条件 | 缓解 | 负责人 |
|---|---|---|---|---|---|
| SR-01 | token 泄漏到 localStorage | 🟠 高 | XSS 攻击 | HttpOnly cookie 远期 / 短期清理 | 架构师 |
| SR-02 | CSP 配置过松 | 🟠 高 | XSS | nginx CSP（见 11.5.2） | 架构师 |
| SR-03 | Sentry PII 泄漏 | 🟡 中 | 用户 id / ip 上报 | `beforeSend` sanitize | BC 主程 |
| SR-04 | SSE 端点无鉴权 | 🔴 极高 | 越权访问 | Bearer token 强制 + 401 redirect | BC 主程 |
| SR-05 | mock-bridge 漏到生产 | 🔴 极高 | 安全降级 | CI grep 阻断合并 | 架构师 |
| SR-06 | 用户数据未脱敏上报 | 🟡 中 | OTLP PII | `sanitize` 函数过滤 | BC 主程 |
| SR-07 | tenant_id / workspace_id 误传 | 🟠 高 | 跨租户越权 | ApiClient 强注入 + header 校验 | BC 主程 |
| SR-08 | npm 依赖漏洞 | 🟡 中 | 旧版本 | `npm audit` + CI | 架构师 |
| SR-09 | 离线 localStorage 数据陈旧 | 🟡 中 | 多端数据冲突 | schema 版本 + 升级 | BC 主程 |

### 13.1.5 业务风险

| ID | 风险 | 等级 | 触发条件 | 缓解 | 负责人 |
|---|---|---|---|---|---|
| BR-01 | 与后端 BC 名映射错位 | 🟠 高 | 团队认知错位 | 文档强制对齐 + ADR | 架构师 |
| BR-02 | 老 `@de/*` 用户拒绝迁移 | 🟡 中 | 业务阻塞 | 双路径阶段切换 | 架构师 |
| BR-03 | 业务方不接受 SSE Turn 流 | 🟡 中 | 习惯非流式 | fallback 全量响应 | BC 主程 |
| BR-04 | Observability 面板无业务价值 | 🟡 中 | 业务方不用 | 聚焦 CostDashboard + TraceViewer | BC 主程 |
| BR-05 | Channel 跨端体验不一致 | 🟡 中 | feishu/teams 体验差异 | 统一 Channel UI 抽象 | BC 主程 |
| BR-06 | 多人协作冲突 | 🟠 高 | 同 session 多人并发 | `X-Workspace-Id` 隔离 + lock | BC 主程 |
| BR-07 | 多语言 i18n 漏翻译 | 🟡 中 | 海外用户 | `i18next` + 翻译覆盖率 | BC 主程 |
| BR-08 | 无障碍 A11y 不达标 | 🟡 中 | WCAG | Lighthouse + axe-core | 测试 |

---

## 13.2 12 维度验收清单

### 13.2.1 FSD 验收（13 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| FSD-01 | `web/src/` 6 层目录存在 | app/pages/widgets/features/entities/shared | CI |
| FSD-02 | path alias 配置正确 | `@eos/*` 7 个别名 + `@/` | `tsconfig.json` |
| FSD-03 | FSD 单向依赖 ESLint 通过 | `pnpm lint` 0 报错 | CI |
| FSD-04 | import-linter 守门通过 | `pnpm lint:fsd` 0 报错 | CI |
| FSD-05 | pages 不依赖 model/lib | `pages/*` 无 model/lib import | grep |
| FSD-06 | features 不依赖 widgets/pages/app | `features/*` 无上述 import | grep |
| FSD-07 | features 不依赖其他 BC 的 model/lib | 跨 BC 无 model/lib import | grep |
| FSD-08 | widgets 仅依赖 features/ui barrel | `widgets/*` 无 features/*/model | grep |
| FSD-09 | model 不依赖 react / zustand / react-query | `model/*` 无副作用 | grep |
| FSD-10 | lib 不依赖 react UI 组件 | `lib/*` 仅纯类/函数 | grep |
| FSD-11 | ui 组件按 BC 组织 | `features/<bc>/ui/components` | 目录检查 |
| FSD-12 | FSD 命名覆盖 DDD BC | 8 BC 1:1 对应后端 | 文档对照 |
| FSD-13 | FSD 文档（02-仓库结构）与代码一致 | 文档与实现一致 | review |

### 13.2.2 DDD 验收（11 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| DDD-01 | 8 BC 存在且命名对齐后端 | agent_runtime/session/skill/tool/knowledge/memory/governance/identity | 目录检查 |
| DDD-02 | 每个 BC 有 model/lib/api/ui 四切片 | 4 子目录齐全 | CI |
| DDD-03 | Entity 是充血模型 | 业务方法在 Entity 上 | 单测 |
| DDD-04 | VO 带类型校验 | 构造时正则/zod 校验 | 单测 |
| DDD-05 | Repository 是 Port 接口 | model 层 interface | review |
| DDD-06 | ACL 函数存在 | 跨 BC 调用通过 ACL | grep |
| DDD-07 | Domain Event 标准结构 | `type/payload/timestamp/traceId` | 单测 |
| DDD-08 | Use Case 是 Hex application | lib/use-cases/ | review |
| DDD-09 | 事件链完整 | 8 BC 间事件连通 | E2E |
| DDD-10 | BC 上下文地图明确 | CS/SK/SE/ACL 关系 | 文档 |
| DDD-11 | 8 BC 互不依赖 model/lib | 仅通过 Repository Port / Domain Event | grep |

### 13.2.3 Hex 验收（10 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| HEX-01 | 三个 Adapter 并存 | HttpApi / MockApi / LocalStorage | 目录 |
| HEX-02 | 三个 Adapter 实现同一 Port | implements 同一 interface | review |
| HEX-03 | Adapter 在 api/ 而非 model/ | 适配器在 api/adapters/ | 目录 |
| HEX-04 | Composition Root 在 app 层 | `app/composition/adapters.ts` | 目录 |
| HEX-05 | `buildDeps(mode)` 三模式可选 | mode: 'http' / 'mock' / 'local' | 单测 |
| HEX-06 | mock 不被业务代码直接引用 | features 0 import `@eos/web-mock` | grep |
| HEX-07 | mock-bridge 在生产被剔除 | `import.meta.env.PROD` 检查 | CI grep |
| HEX-08 | EventBus 是 Port + Adapter | interface 在 model，实现可替换 | review |
| HEX-09 | ACL 是纯函数 | 无 react / zustand | review |
| HEX-10 | Use Case 通过注入的 Port 工作 | 构造器注入 | 单测 |

### 13.2.4 Mock 验收（8 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| MOCK-01 | mock token 路由正确 | `mock-*` token → MockApiAdapter | 单测 |
| MOCK-02 | mock 与 api 共享 DTO 类型 | 来自 `@eos/web-types` | review |
| MOCK-03 | Contract Test 通过 | mock 形态 = api 形态 | CI |
| MOCK-04 | mock fixture 与后端 handlers 镜像 | 一致 | review |
| MOCK-05 | mock 全联通 | 8 BC 全流程可走 | E2E |
| MOCK-06 | mock SSE 协议稳定 | 8 chunk 类型全覆盖 | 单测 |
| MOCK-07 | mock 不进生产 bundle | `grep -r "MockApi" dist/` 失败 | CI |
| MOCK-08 | mock-bridge 不可被生产触发 | `VITE_FEATURE_MOCK_BRIDGE=false` 阻断 | CI |

### 13.2.5 Type & API 验收（10 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| TYPE-01 | OpenAPI Codegen 跑通 | `pnpm codegen` 成功 | CI |
| TYPE-02 | DTO 类型 100% 覆盖 | 8 BC 都有 DTO | review |
| TYPE-03 | brand ID 类型 | `SessionId`, `TenantId` 等 | 单测 |
| TYPE-04 | ErrorCode 镜像后端 | 业务错误码一致 | 单测 |
| TYPE-05 | API 路径命名规范 | `/v1/<bc>/*` | review |
| TYPE-06 | 状态码使用严格 | 200/201/204/400/401/403/404/409/422/429/500/502/503 | review |
| TYPE-07 | 鉴权头四件套 | Authorization + X-Tenant-Id + X-Workspace-Id + X-Trace-Id | 单测 |
| TYPE-08 | DTO ↔ Entity 转换完整 | `fromDTO` / `toDTO` | 单测 |
| TYPE-09 | API 错误处理决策表 | 12 错误码 → 行为 | 文档 |
| TYPE-10 | SSE 协议文档完整 | chunk 8 类型 + 协议规范 | 文档 |

### 13.2.6 SSE & 流式验收（6 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| SSE-01 | POST /v1/sessions/{id}/turns 返回 text/event-stream | curl 验证 | E2E |
| SSE-02 | 8 chunk 类型解析正确 | 渲染器覆盖 | 单测 |
| SSE-03 | 流式渲染无延迟 | chunk 到 → 立即渲染 | E2E |
| SSE-04 | SSE 中断可重连 | 自动重连 | 单测 |
| SSE-05 | SSE 取消可清理 | AbortController | 单测 |
| SSE-06 | SSE trace_id 一致 | X-Trace-Id 透传 | E2E |

### 13.2.7 Performance 验收（10 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| PERF-01 | Lighthouse LCP ≤ 2.5s | staging | CI |
| PERF-02 | Lighthouse FID ≤ 100ms | staging | CI |
| PERF-03 | Lighthouse CLS ≤ 0.1 | staging | CI |
| PERF-04 | Lighthouse TTI ≤ 3.5s | staging | CI |
| PERF-05 | Lighthouse TBT ≤ 300ms | staging | CI |
| PERF-06 | Bundle 总 gzipped ≤ 2 MB | CI 检查 | CI |
| PERF-07 | react-vendor gzipped ≤ 200 KB | CI 检查 | CI |
| PERF-08 | ui-vendor gzipped ≤ 400 KB | CI 检查 | CI |
| PERF-09 | SSE 首个 chunk ≤ 200ms | staging | E2E |
| PERF-10 | Long Task 0 | staging | Lighthouse |

### 13.2.8 CI 验收（8 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| CI-01 | TypeScript 通过 | `pnpm typecheck` 0 报错 | CI |
| CI-02 | ESLint 通过 | `pnpm lint` 0 报错 | CI |
| CI-03 | FSD lint 通过 | `pnpm lint:fsd` 0 报错 | CI |
| CI-04 | 单测 + 集成通过 | `pnpm test --run` 0 失败 | CI |
| CI-05 | 覆盖率门禁通过 | ≥ 95/90/80/70/60 各层 | CI |
| CI-06 | 生产构建成功 | `pnpm build` 成功 | CI |
| CI-07 | Bundle 检查通过 | mock 关键字 grep 0 命中 | CI |
| CI-08 | E2E 通过 | 5 用力 Playwright 0 失败 | CI |

### 13.2.9 Security 验收（8 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| SEC-01 | token 不写入非 localStorage | 仅 localStorage | review |
| SEC-02 | CSP 头设置 | nginx CSP 见 11.5.2 | smoke |
| SEC-03 | Sentry PII sanitize | ip/user 字段过滤 | 单测 |
| SEC-04 | SSE 端点鉴权 | 401 → /login | E2E |
| SEC-05 | mock 不进生产 | CI grep 0 | CI |
| SEC-06 | 用户数据上报脱敏 | sensitive keys 过滤 | 单测 |
| SEC-07 | tenant/workspace 头强校验 | ApiClient 注入 | 单测 |
| SEC-08 | npm audit 0 high | npm audit | CI |

### 13.2.10 i18n & A11y 验收（6 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| I18N-01 | 关键文案 i18n 化 | zh-CN / en-US 切换 | review |
| I18N-02 | i18next 配置正确 | 语言包加载 | 单测 |
| I18N-03 | 翻译覆盖率 ≥ 90% | i18n 扫描 | CI |
| A11Y-01 | Lighthouse A11y ≥ 95 | staging | CI |
| A11Y-02 | axe-core 通过 | 关键页面 | CI |
| A11Y-03 | 键盘可达 | Tab 切换合理 | E2E |

### 13.2.11 Doc 验收（5 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| DOC-01 | 13 节文档完成 | 00-12 + 风险 | 文件检查 |
| DOC-02 | BC 名与后端 1:1 | 8 BC 全等 | 对照 |
| DOC-03 | 路径别名 `@eos/*` | 文档与代码一致 | grep |
| DOC-04 | 与后端文档平行阅读 | 引用关系齐备 | review |
| DOC-05 | ADR 关键决策 | FSD/DDD/Hex/Codegen/mock | 文件检查 |

### 13.2.12 上线验收（5 项）

| ID | 验收项 | 通过标准 | 检查方式 |
|---|---|---|---|
| GO-01 | staging 灰度 100% 通过 | canary 100% | smoke |
| GO-02 | 性能达标 | Lighthouse + Bundle | CI |
| GO-03 | 可观测面板正常 | Trace + Cost + Quality | staging |
| GO-04 | 1.0 上线完成 | production deploy | release |
| GO-05 | 与后端 enterprise-agent-os 同集群 | 同 K8s cluster | 部署 |

---

## 13.3 准入 / 准出清单

### 13.3.1 准入（开始 Week 2 BC 开发）

- [ ] Week 1 全部验收项通过（M1）
- [ ] FSD 守门 0 报错
- [ ] CI 全绿（typecheck/lint/lint:fsd/test/build）
- [ ] 6 个 shared package 完成
- [ ] 路径别名 `@eos/*` 7 个生效

### 13.3.2 准入（Week 3 联调）

- [ ] 8 BC 骨架完整（M2）
- [ ] Mock 全联通（M3）
- [ ] 后端 staging 可达
- [ ] `openapi.yaml` 稳定
- [ ] 4 个核心 BC 覆盖率 ≥ 80%

### 13.3.3 准入（Week 4 性能优化）

- [ ] 与后端 staging 真接口跑通（M4）
- [ ] SSE Turn 流式协议稳定（M5）
- [ ] Observability 三个面板完成
- [ ] 前端 reporter 上报通路打通

### 13.3.4 准入（Week 4 灰度）

- [ ] 13 节文档完成（00-12 + 风险）
- [ ] Bundle 大小达标（≤ 2 MB gzipped）
- [ ] Lighthouse 性能达标
- [ ] E2E 关键路径通过
- [ ] Contract Test 通过
- [ ] mock 不进生产
- [ ] Security 8 项通过

### 13.3.5 准出（1.0 上线）

- [ ] staging 灰度 100% 通过 ≥ 3 天
- [ ] 0 高危 / 0 critical bug
- [ ] 可观测面板稳定运行
- [ ] 1.0 版本 tag
- [ ] 与后端 enterprise-agent-os 同步上线

---

## 13.4 风险等级汇总

| 等级 | 数量 | 红色（≥ 极高） |
|---|---|---|
| 🔴 极高 | 2 | IR-02 SSE 协议变 / SR-04 SSE 鉴权 / SR-05 mock 漏生产 |
| 🟠 高 | 11 | TR-01/02/08 / IR-01/05 / SR-01/02/07 / BR-01/06 |
| 🟡 中 | 24 | 其他 |
| 🟢 低 | 0 | — |

---

## 13.5 监控与告警

| 监控 | 阈值 | 告警 |
|---|---|---|
| 前端错误率（Sentry） | > 1% | Slack |
| LCP p95 | > 2.5s | Slack |
| Bundle 增长 | > 10% | Slack |
| Mock 命中 | 0 | 无（mock 应被剔除） |
| SSE 连接失败 | > 5% | Slack |
| Token 401 | > 10/min | 自动 lock |
| trace 上报失败 | > 5% | Slack |

---

## 13.6 升级机制

| 触发 | 升级 |
|---|---|
| mock 进生产 bundle | P0 立即修复 |
| SSE 中断（生产） | P0 立即回滚 |
| Security 高危漏洞 | P0 24h 内修复 |
| 性能不达标（≥ 20%） | P1 Week 4 buffer 解决 |
| 覆盖率不达标 | P1 必修复 |
| 文档不一致 | P2 当周修 |

---

## 13.7 与后端风险平行

| 后端风险 | 前端风险 |
|---|---|
| BR-数据库迁移失败 | TR-03 zustand persist |
| BR-SSE 后端不稳定 | TR-02 SSE 兼容性 |
| BR-OpenAPI 不稳定 | TR-01 Codegen 失败 |
| BR-权限漏洞 | SR-04 SSE 鉴权 / SR-07 tenant 头 |
| SR-token 泄漏 | SR-01 token localStorage |
| AR-monorepo 边界 | AR-01/02 FSD 单向 |

> **关键**：前后端风险点互相独立；CI 平行但互不依赖；staging 同集群。

---

## 13.8 不在本节范围

- 后端风险验收 → 见后端 `docs/enterprise-agent-os/13-风险与验收.md`
- 法律合规 → 公司层
- 业务 KPI → 公司层
- 详细 ADR → `docs/adr/`