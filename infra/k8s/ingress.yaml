# Header-based canary routing.
# Primary rule (no annotation) → stable. Canary rule (annotations)
# activates when the request carries `X-EOS-Ring: canary`.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eos-app
  namespace: eos-prod
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "32m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_input_headers "X-Forwarded-Proto: https";
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
spec:
  ingressClassName: nginx
  tls:
    - hosts: [eos.example.com]
      secretName: eos-app-tls
  rules:
    - host: eos.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eos-app-stable
                port:
                  number: 80
---
# Canary ingress — only matches when X-EOS-Ring: canary is set.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eos-app-canary
  namespace: eos-prod
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-by-header: "X-EOS-Ring"
    nginx.ingress.kubernetes.io/canary-by-header-value: "canary"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [eos.example.com]
      secretName: eos-app-tls
  rules:
    - host: eos.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eos-app-canary
                port:
                  number: 80
